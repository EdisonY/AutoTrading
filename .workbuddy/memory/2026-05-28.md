# 2026-05-28 工作记录

## 上午复盘和问题排查
- 发现 A/v11 694 次 OPEN_FAILED (-1109 Invalid account)
- 发现 B/v16 CLOSE 事件 pnl=None（实际数据在 payload_json.raw 子字段）
- **-1109 根因**：Testnet 账户在 05-27 17:50~18:02 之间被平台标记无效。最早出现在 EVICT_FAILED（驱逐弱仓），非代码异常。推测 Testnet 定期清理不活跃账户。
- 根因：crypto-scanner.service 缺少 EnvironmentFile，修复后发现 trading.env 权限问题（-rw------- root:root）
- 修复权限为 640 root:ubuntu，重启三个 scanner service

## A/v11 -1109 深入排查
- API key 已正确加载到进程（/proc/PID/environ 确认）
- 用 env 文件中的 key 直接调用 Binance Testnet API 成功
- 结论：key 已加载但 scanner.py 进程中 os.environ.get() 仍返回空
- 最终发现：SSH shell 环境和 systemd 服务环境是隔离的，Python 测试在 SSH 中运行不继承 systemd 环境
- 实际进程内环境变量正确，-1109 是 Testnet 账户状态问题

## SQLite 膨胀问题
- event_store.sqlite3 在腾讯膨胀到 1.7GB
- 主要原因：SENTINEL_SCANNED 事件约 97 万条，每条 payload_json 含完整扫描数据
- 写了 cleanup_event_store.py：只删 30 天前的 SENTINEL_SCANNED/EVENT/SIGNAL，保护所有交易事件
- 腾讯服务器因 portal_dashboard.py 在 1.7GB SQLite 上运行导致 SSH 超时

## 阿里云流水线修复
- aliyun_analysis_refresh.sh 移除 set -euo pipefail
- sync 步骤添加 timeout 600，各步骤用 || echo WARN 代替直接退出
- portal_dashboard.py 修复 {{}} f-string 语法错误
- 分析脚本上传到腾讯本地运行（数据在哪计算在哪）

## 架构调整方向
- 1.7GB SQLite 无法每 2 小时同步到阿里云
- 方案：分析脚本在腾讯本地运行，只同步轻量报告文件到阿里云
- 待执行：等腾讯恢复后运行 cleanup_event_store.py 清理数据库

## Git 提交
- 32d58b2 docs: Record A/v11 key update, DB cleanup, pipeline migration
- 9443dba feat: Sentinel scans in dedicated table with daily partitioning
- f6d2224 fix: SQLite cleanup script, portal syntax error, pipeline resilience
- 110d311 fix: A/v11 OPEN_FAILED -1109 caused by missing EnvironmentFile
- d6c8555 feat: Attention API server and portal confirmation buttons
