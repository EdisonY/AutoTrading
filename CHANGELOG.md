# AutoTrading Change Ledger

This is the durable reason-and-outcome ledger for every material design, code, configuration, deployment, rollback, optimization, or live operational change.

## 2026-05-31 20:05 CST - Add post-approval live windows to evolution gate
- Trigger / reason: Continue N4 after rollback-watch deployment. Approved full-live candidates need explicit 24h/72h/7d real live windows, not only shadow/experiment evidence.
- Completed: `strategy_evolution_gate.py` now locates the event-store SQLite DB, reads full-live approvals, and builds post-approval live windows for 24h, 72h, and 168h per approved candidate. The DB locator prefers the Tencent mirror under `server_logs_tencent/runtime/event_store.sqlite3` and validates the `events` table before use, avoiding Aliyun's non-event runtime DB. The windows de-duplicate event-store mirror rows and count `OPEN`, `CLOSE`, `FORCED_CLOSE`, `OPEN_FAILED`, `CLOSE_FAILED`/`FORCED_CLOSE_FAILED`, `OPEN_SKIPPED`, latest event time, and realized close PnL. Close-confirm failures in any post-approval window now trigger `rollback_required/P0`; 24h `OPEN_FAILED` above threshold triggers `rollback_watch/P1`. Gate JSON/MD/HTML now expose the post-approval live-window summary.
- Not completed / remaining: Regime split, stricter window PnL/PF quality thresholds, and automatic code rollback are still future N4 work.
- Verification: Local `py_compile` passed. Local gate generation succeeded. A synthetic full-live candidate with one `24h` close-confirm failure produced `rollback_required/P0` with blocker `24h 关闭确认失败 1`. First remote refresh exposed that Aliyun `runtime/event_store.sqlite3` is not the event-store DB; the DB locator was fixed to prefer and validate `server_logs_tencent/runtime/event_store.sqlite3`, then redeployed. Remote gate then generated `decisions=13`, `P0=0/P1=0/P2=9`, `full_live_watch_count=4`, `rollback_watch_count=0`, and `post_approval_window_count=4`. Current post-approval 24h windows show A/v11 approved trailing candidates `opens=83/closes=73/forced_closes=4/open_failed=0/close_failed=0/open_skipped=98`; B/v16 approved candidates are still maturing and show `opens=55/closes=54/forced_closes=5/open_failed=3/close_failed=0/open_skipped=70`. Reverse sync uploaded 16 key files to Tencent. Final local `pull_live_context.py` at `2026-05-31T20:13:15+08:00` showed all six Tencent services active, account unrealized PnL `+37.2142 USDT`, `11` positions, and attention `P0=0/P1=0/P2=5`.
- Live impact / deployment: Deployed Aliyun `shadow` releases `20260531-200456-shadow-f83d66d` and bugfix `20260531-201101-shadow-ce2709c`. Read-side gate/report behavior only; no scanner, order, threshold, or risk behavior changes.
- Files / release / commit: `部署工具/strategy_evolution_gate.py`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commits `f83d66d` and `ce2709c` plus follow-up deployment-ledger commit.

## 2026-05-31 19:58 CST - Bound Aliyun hourly sync and daily fallback
- Trigger / reason: While verifying the N4 rollback-watch deploy, the full chained remote run stalled because the 3-day Tencent→Aliyun shadow sync exceeded the bounded window. A 1-day bounded sync with smaller sentinel/account limits completed quickly and produced fresh data.
- Completed: `aliyun_analysis_refresh.sh` now uses a 1-day bounded sync for the every-2-hour command-center refresh: 180s timeout, `--sqlite-days 1`, `--sentinel-limit 5000`, `--account-limit 500`, and `--max-age-hours 8`. Its research-store export/query window now matches the 1-day refresh mirror. `aliyun_shadow_review.sh` still tries the deeper 3-day sync first, but now wraps it in a 300s timeout and falls back to the same 1-day bounded sync if the deeper pull fails or times out.
- Not completed / remaining: This is a reliability fix, not the final cross-cloud data warehouse. The long-window daily experiment path still needs a better incremental/streaming sync so 3/7/14/30-day research windows do not depend on a slow full pull.
- Verification: Remote `bash -n` passed for `aliyun_analysis_refresh.sh` and `run_shadow_review.sh`. A full hourly refresh run completed in about 148s: 1-day sync produced SQLite `quick_check ok`, `events=7163`, `sentinel_scans=5000`, `account_snapshots=500`, all about `0.01h` fresh; research store export/query, counterfactual, evolution gate, decision attention, portal generation, and reverse sync all completed. Gate output remained `P0=0/P1=0/P2=9`, `full_live_watch_count=4`, `rollback_watch_count=0`; attention remained `P0=0/P1=0/P2=5`. Local `pull_live_context.py` initially hit one Tencent SSH timeout, then retry at `2026-05-31T19:58:17+08:00` succeeded with all six services active, account unrealized PnL `+32.4077 USDT`, `11` positions, and attention `P0=0/P1=0/P2=5`.
- Live impact / deployment: Deployed Aliyun `shadow` release `20260531-195428-shadow-fc46cd0`. Analysis/report scripts only; no scanner, order, threshold, or risk behavior changes.
- Files / release / commit: `部署工具/aliyun_analysis_refresh.sh`, `部署工具/aliyun_shadow_review.sh`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit `fc46cd0` plus follow-up deployment-ledger commit.

## 2026-05-31 19:21 CST - Add full-live rollback watch to evolution gate
- Trigger / reason: Continue the next-stage optimization goal. N4 needed approved live changes to remain monitored for degradation instead of disappearing from the decision surface after manual approval.
- Completed: `strategy_evolution_gate.py` now loads full-live approval records, marks approved candidates with `approved_full_live`, and classifies them as `full_live_monitoring` unless rollback triggers fire. Added rollback states: `rollback_required` → P0 and `rollback_watch` → P1. Triggers include sizing violation, hard-stop risk, large strategy-account loss, fee-adjusted/shadow underperformance versus original, hard-stop increase, and OPEN_FAILED pressure. `decision_attention.py` still suppresses already approved upgrade items, but no longer suppresses approved candidates when they become rollback items; rollback attention items use `rollback:<candidate>` ids and `策略回滚` category. `portal_dashboard.py` labels top rollback findings as `策略回滚观察` instead of upgrade opportunity.
- Not completed / remaining: This is the first N4 rollback-watch layer. It still relies on current experiment/account fields; deeper regime split, explicit 24h/72h/7d post-approval live windows, close-confirm failure as an independent rollback metric, and automatic code rollback remain future work. A 3-day Aliyun shadow sync still timed out in this verification window; the 1-day bounded sync succeeded and should be used until the cross-cloud sync path is further optimized.
- Verification: Local `py_compile` passed for `strategy_evolution_gate.py`, `decision_attention.py`, and `portal_dashboard.py`. Local gate generation succeeded. A synthetic approved full-live degradation case produced `rollback_watch`, priority `P1`, with blocker `影子PnL 50.00 < 原版 100.00 × 0.8`. Local `decision_attention.py` and portal generation succeeded. Remote compile passed on Aliyun and Tencent. Aliyun 1-day bounded sync passed with fresh `events`, `sentinel_scans`, and `account_snapshots` all about `0.04h` old. Remote gate generated `decisions=13`, `P0=0/P1=0/P2=9`, `full_live_watch_count=4`, `rollback_watch_count=0`; the four approved A/v11/B/v16 candidates are now `full_live_monitoring`. Decision attention and portal regenerated, reverse sync uploaded 16 key files to Tencent. Final local `pull_live_context.py` at `2026-05-31T19:51:06+08:00` showed all six Tencent services active, account unrealized PnL `+25.2107 USDT`, `10` positions, and attention `P0=0/P1=0/P2=5`.
- Live impact / deployment: Deployed Aliyun `shadow` release `20260531-192602-shadow-27bc0ab` and Tencent `portal` release `20260531-192602-portal-27bc0ab`. Tencent `crypto-system-alerts.service` restarted successfully and remained `active/running`. Read-side gate/attention/report behavior only; no scanner, order, threshold, or risk behavior changes.
- Files / release / commit: `部署工具/strategy_evolution_gate.py`, `部署工具/decision_attention.py`, `部署工具/portal_dashboard.py`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit `27bc0ab` plus follow-up deployment-ledger commit.

## 2026-05-31 17:20 CST - Make shadow SQLite sync bounded and freshness-checked
- Trigger / reason: Continue the N3 data warehouse goal. The previous report recovery proved the manual tiny SQLite snapshot works, but the normal Tencent→Aliyun sync path still needed a permanent non-stalling default.
- Completed: Updated `shadow_sync_from_tencent.py` fast bundle mode so SQLite mirror size now follows `--sqlite-days`/`--days` instead of a hard-coded 30-day event window. It keeps `SIGNAL` rows for research-store funnel metrics, limits newest `sentinel_scans` and `account_snapshots`, and validates the extracted mirror with `pragma quick_check`, table row counts, and max-age freshness checks before analysis can consume it. Added CLI controls: `--sqlite-days`, `--sentinel-limit`, `--account-limit`, and `--max-age-hours`. Fixed deployed-root detection so the flat Aliyun script writes to `/opt/crypto-shadow-lab/server_logs_tencent` instead of `/opt/server_logs_tencent`. Updated `sync_aliyun_reports_to_tencent.py` so default upload uses one bounded OpenSSH/base64 upload per file instead of long-lived SFTP/Paramiko; it now syncs index/counterfactual/research-store reports before less critical files, defaults to short per-file timeout, low error budget, and one retry per file, and keeps bulky market-review files behind `--include-optional`.
- Not completed / remaining: Full replay/live parity and evolution gray/rollback automation are still future N2/N4 work. Local machine still lacks `pyarrow/duckdb`, so local Parquet/DuckDB query validation remains limited; Aliyun is the authoritative analysis runtime for those steps.
- Verification: Local `py_compile` passed. `shadow_sync_from_tencent.py --days 3 --max-age-hours 6` completed with a 6.6MB remote bundle, SQLite `quick_check ok`, `events=28994`, `sentinel_scans=20000`, `account_snapshots=1500`, and all three tables fresher than 0.1h. Local counterfactual on the fresh mirror ran successfully with `events=537`, `primary_complete_samples=515`, simulated PnL `+946.27 USDT`, and `kline_errors=0`. Local JSONL export of the research store succeeded. Aliyun verification: bounded sync wrote `/opt/crypto-shadow-lab/server_logs_tencent/runtime/event_store.sqlite3` with `quick_check ok`, `events=28999`, `sentinel_scans=20000`, `account_snapshots=1500`; Parquet export/query succeeded; counterfactual ran with `events=526`, `primary_complete_samples=506`, simulated PnL `-0.7935 USDT`, `kline_errors=0`; portal generation succeeded. Reverse sync default run finished in 101.7s and uploaded 16 key files to Tencent. Final local `pull_live_context.py` at `2026-05-31T18:03:10+08:00` showed all six Tencent services active, account unrealized PnL `+19.1463 USDT`, 13 positions, attention `P0=0/P1=0/P2=5`; pulled `reports/index.html` shows `研究仓/DuckDB`, `研究仓样本漏斗`, and counterfactual sample `506` / PnL `-0.79`.
- Live impact / deployment: Deployed Aliyun shadow releases `20260531-171945-shadow-3aa7cf2`, `20260531-172348-shadow-b4f2045`, `20260531-173636-shadow-f9d5367`, `20260531-174056-shadow-52f5f19`, `20260531-174653-shadow-284c0f9`, `20260531-175038-shadow-4c5c8ce`, and `20260531-180038-shadow-ebe4373` while iterating the sync fix. Read-side sync/analysis/report behavior only; no scanner, order, threshold, or risk behavior changes.
- Files / release / commit: `部署工具/shadow_sync_from_tencent.py`, `部署工具/sync_aliyun_reports_to_tencent.py`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this final verification update.

## 2026-05-31 12:23 CST - Deploy replay report chain and restore fresh counterfactual samples
- Trigger / reason: After deploying the replay/research report changes, remote verification showed Aliyun's mirrored Tencent SQLite was stale at 2026-05-29 while Tencent live DB was current at 2026-05-31. The full `shadow_sync_from_tencent.py` run hit the bounded timeout, so the report chain needed a non-stalling recovery path before final verification.
- Completed: Deployed the report/replay changes to Aliyun shadow, Tencent research, and Tencent portal. Found and fixed the live-source mismatch in `counterfactual_open_skips.py`: current de-duplicated OPEN_SKIPPED rows are stored under `*/events`, not the old `*/decisions` mirrors. Built a verified Tencent SQLite slim snapshot containing recent `events`, latest `sentinel_scans`, latest `account_snapshots`, and attention tables; transferred it to Aliyun using bounded SSH/base64 text streaming after scp/SFTP stalled; regenerated Parquet research-store export/query, counterfactual OPEN_SKIPPED evaluation, and the portal entry page; synced the key generated reports/runtime JSON back to Tencent with bounded SSH/base64 streaming.
- Not completed / remaining: The normal full `shadow_sync_from_tencent.py` data-sync path still needs a permanent slimming/streaming fix so manual tiny-snapshot recovery is not needed. Full replay/live parity is still not complete; live A/B/C scanner gates remain to be extracted into pure replay-callable functions.
- Verification: Remote `py_compile` passed on Aliyun and Tencent for `counterfactual_open_skips.py`, `portal_dashboard.py`, and `core/replay.py`. Aliyun slim DB quick_check was `ok`; generated research store at `2026-05-31T12:16:52+08:00` with `events=30182`, `sentinel_scans=20000`, `account_snapshots=1500`. Fixed counterfactual then ran successfully: `events=474`, `primary_complete_samples=464`, 60m simulated win rate `36.42%`, simulated PnL `+905.10 USDT`, `kline_errors=0`. Aliyun portal grep confirmed `研究仓样本漏斗` and `研究仓/DuckDB`; local `pull_live_context.py` at `2026-05-31T12:22:53+08:00` pulled the synced report and showed all six Tencent services active, account unrealized PnL `+46.9980 USDT`, 13 positions, attention `P0=0/P1=0/P2=2`. Local pulled `reports/index.html` shows the fresh counterfactual sample/PnL and research-store section; `reports/counterfactual_open_skips_latest.json` contains `replay_schema=core.replay/v1`.
- Live impact / deployment: Deployed Aliyun `shadow` release `20260531-115105-shadow-4a718c7`, Tencent `research` release `20260531-115126-research-4a718c7`, Tencent `portal` release `20260531-115228-portal-4a718c7`, then follow-up Aliyun `shadow` release `20260531-121850-shadow-62c9b96` and Tencent `research` release `20260531-121850-research-62c9b96`. Tencent portal release restarted only `crypto-system-alerts.service` successfully. No scanner/order/threshold behavior changed.
- Files / release / commit: `CHANGELOG.md`, `PROJECT_STATE.md`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `记忆文档/MEMORY.md`; Git commit to contain this deployment ledger.

## 2026-05-31 11:50 CST - Wire replay gates into counterfactual and surface research store
- Trigger / reason: Continue the next-stage optimization goal. The decision report already had a cleaner executive layer, but counterfactual OPEN_SKIPPED evaluation still used its own grouping path and the new DuckDB/Parquet research-store summary was not visible from the main report.
- Completed: Updated `counterfactual_open_skips.py` so OPEN_SKIPPED rows are normalized through `core.replay.ReplayEvent` and grouped first by `ReplayDecision.gate`; the DB payload and JSON report now carry `replay_decision`, `replay_gate`, and `replay_schema=core.replay/v1`. Also fixed its live-source filter after SQLite de-duplication: it now reads `strategy in ('A/v11','B/v16','C/v14')` instead of only old `*/decisions` mirror sources, so current `*/events` OPEN_SKIPPED rows are included. Updated `portal_dashboard.py` to read `runtime/research_store_summary_latest.json`, add a `研究仓/DuckDB` function-status card, add a `研究仓漏斗` detail route, and show strategy funnel / OPEN_SKIPPED gate tables in the report. Updated future-plan and memory docs to mark N1 complete and N2/N3 partial progress.
- Not completed / remaining: A/B/C live scanner gate logic is still not extracted into pure replay-callable functions. Counterfactual price-path simulation still has its own execution model until the full replay engine is built. SQLite retention and Parquet watermark automation remain N3 follow-ups.
- Verification: `py_compile` passed for `counterfactual_open_skips.py`, `portal_dashboard.py`, and `core/replay.py`. Local portal generation succeeded. Local one-hour counterfactual smoke passed with zero current local OPEN_SKIPPED events and no script error. Remote verification later found the old source filter missed current live `OPEN_SKIPPED` rows because they now live under `*/events`; the filter was fixed before final deployment.
- Live impact / deployment: This is read-side analysis/reporting only; no strategy threshold, order execution, scanner service, or live risk behavior changes. Final release and live verification are recorded in the follow-up deployment ledger entry below.
- Files / release / commit: `部署工具/counterfactual_open_skips.py`, `部署工具/portal_dashboard.py`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-31 11:05 CST - Record next optimization target and redesign decision report
- Trigger / reason: User asked to record the proposed optimization program as the next target, proceed by item, redesign the report for a decision-maker view, and decide whether to loosen strategy entry thresholds to collect more optimization data.
- Completed: Pulled live context before acting; Tencent six expected services were active, system alerts had no P0, account unrealized PnL was positive in the pulled snapshot, and attention P0/P1 was clear. Added a new 2026-05-31 next-stage section to `记忆文档/FUTURE_EXECUTION_PLAN.md`: decision report redesign, unified replay/live path, Parquet/DuckDB research warehouse, gray/rollback-aware evolution gate, and open-source framework PoC. Updated `PROJECT_STATE.md` and `记忆文档/MEMORY.md` with the durable decision: do not globally loosen entries again now; A/v11 stays stable, B/v16 observes the already approved full-live candidates, and C/v14 remains in its current controlled sample-expansion window until enough post-change evidence accumulates. Updated `portal_dashboard.py` to add an executive first-screen summary with account/risk/evolution/entry-expansion conclusions, next actions, and a per-strategy action table.
- Not completed / remaining: Unified replay, Parquet/DuckDB warehouse, gray rollback automation, and framework PoC are now recorded as next implementation phases but are not implemented in this step.
- Verification: `py_compile` passed for `portal_dashboard.py`; local portal generation succeeded and the generated `reports/index.html` contains the new executive brief, per-strategy action rows, and controlled-expansion decision. Dry-runs passed for Tencent `portal` and Aliyun `shadow`. Deployed and pulled live context at `2026-05-31T11:11:55+08:00`: all six expected Tencent services active, account unrealized PnL `+32.9317 USDT`, 11 positions, attention `P0=0/P1=0/P2=5`. Remote grep confirmed the live Tencent entry page shows "系统可继续运行", "暂无需要立即批准的进化项", "受控扩样", and "稳态监控".
- Live impact / deployment: Deployed Aliyun `shadow` release `20260531-111111-shadow-4267a37` and Tencent `portal` release `20260531-111122-portal-4267a37`. This changed report/decision surface and durable planning only. No strategy threshold, order execution, or scanner service behavior was changed.
- Files / release / commit: `部署工具/portal_dashboard.py`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-31 11:15 CST - Start research-store export foundation
- Trigger / reason: User asked to proceed by optimization item; after N1 report redesign, the next low-risk foundation is a research warehouse export path for replay/DuckDB work.
- Completed: Added `research_store/` to `.gitignore`; added `部署工具/research_store_export.py`, a read-only SQLite exporter that partitions `events`, `sentinel_scans`, and `account_snapshots` by date into ignored `research_store/` files. It supports Parquet for the target research warehouse and JSONL for dependency-light validation, and writes `manifest_latest.json`. Added `pyarrow` and `duckdb` to `requirements.txt` for the next research-store phase. Updated the future plan and memory to mark the first N3 export step complete.
- Not completed / remaining: DuckDB query layer, replay engine integration, kline/feature export, watermark incremental export, and server timer wiring are not implemented yet. The script is not deployed as a scheduled job in this step.
- Verification: `py_compile` passed for `research_store_export.py` and `portal_dashboard.py`. Local JSONL validation command against `runtime/event_store.sqlite3` completed and wrote a manifest under ignored `research_store/`; local DB did not contain rows in the 1-day test window, so this validated control flow rather than production-scale export volume.
- Live impact / deployment: None. Offline research tooling only; no scanner, order, threshold, or live service behavior changed.
- Files / release / commit: `.gitignore`, `requirements.txt`, `部署工具/research_store_export.py`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-31 11:18 CST - Add DuckDB query and replay model foundations
- Trigger / reason: Continue the active next-stage optimization goal beyond report redesign and export-only research-store work. The next concrete pieces are a read-side research warehouse summary and a unified replay event boundary.
- Completed: Added `部署工具/research_store_query.py`, a DuckDB read layer for exported `research_store/` partitions that produces `runtime/research_store_summary_latest.json` and `reports/research_store_summary_latest.md` with strategy funnel, OPEN_SKIPPED gate, sentinel contribution, and latest account summaries. Added `core/replay.py`, a Python 3.10-compatible replay normalization module with `ReplayEvent`, `ReplayDecision`, observed event-type mapping, and initial gate classification for live/backtest/counterfactual parity. Wired both new files into `release_manager.py` and legacy `deploy_shadow_aliyun.py` deploy lists so analysis nodes receive them. Added research-store export/query steps to both Aliyun analysis scripts. Updated reverse sync to include research-store summary outputs, sync runtime/attention before bulky report files, and added timeout wrappers to the Aliyun analysis scripts so reverse sync cannot stall a whole pipeline. Updated `pull_live_context.py` so fresh local pulls include the research-store summary outputs.
- Not completed / remaining: A/B/C scanner gate logic is not yet refactored to call the replay layer. `counterfactual_open_skips.py` still has its own evaluation path. Tencent live venv still lacks `duckdb/pyarrow`, intentionally left untouched because this is an Aliyun analysis workload.
- Verification: `py_compile` passed for `core/replay.py`, `research_store_query.py`, `research_store_export.py`, `release_manager.py`, `deploy_shadow_aliyun.py`, `sync_aliyun_reports_to_tencent.py`, and `pull_live_context.py`. Local DB inspection showed the current local SQLite mirror has `events` and `account_snapshots` but no `sentinel_scans` table; production Aliyun/Tencent mirrors have the full schema from prior checks. Installed `duckdb-1.5.3` and `pyarrow-22.0.0` on Aliyun analysis Python. Aliyun 3-day Parquet export/query ran successfully: `events=80672`, `sentinel_scans=44762`, `account_snapshots=10959`, and generated `research_store_summary_latest.json/md`. A bounded reverse-sync run uploaded `runtime/research_store_summary_latest.json` and `reports/research_store_summary_latest.md` to Tencent before hitting the bulky market-review section; timeout guards prevent that from stalling future full pipelines. Tencent now has both research-store summary files.
- Live impact / deployment: Deployed Aliyun shadow releases `20260531-112213-shadow-d744194`, `20260531-112541-shadow-d744194`, and `20260531-113016-shadow-d744194`; deployed Tencent research release `20260531-112215-research-d744194`. Offline analysis/replay foundation only; no scanner, order, threshold, or live service behavior changed.
- Files / release / commit: `core/replay.py`, `部署工具/research_store_query.py`, `部署工具/release_manager.py`, `部署工具/deploy_shadow_aliyun.py`, `部署工具/aliyun_analysis_refresh.sh`, `部署工具/aliyun_shadow_review.sh`, `部署工具/sync_aliyun_reports_to_tencent.py`, `部署工具/pull_live_context.py`, `记忆文档/FUTURE_EXECUTION_PLAN.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-31 02:00 CST - B/v16 full-live approval and C/v14 sample expansion
- Trigger / reason: User explicitly approved opening up the strategy constraints because current live sample size is too small to optimize effectively, asked to solve C/v14's low open count, and required A/v11 to remain abnormal-free.
- Completed: Recorded full-live approval for B/v16 candidates `EXP-20260527-v16-atr-stop-bands` and `EXP-20260527-v16-overheat-cap-85`. B/v16 now uses approved ATR stop bands (`2.5x` ATR below 2% ATR/price, `2.0x` normal, `1.5x` above 3%) and score cap `85`. C/v14 now runs `v14_sample_expansion_2026_05_31`: per-timeframe cap `3`, per-sector cap `3`, 1h threshold `50`, 15m confirmation threshold `25`, short penalty `10`, score cap `85`, and high-score 1h candidates can pass weak/no 15m confirmation. Config files were aligned with the hardcoded live constants. A/v11 strategy rules were intentionally not loosened.
- Not completed / remaining: This change intentionally increases sample collection and may lower short-term win rate before enough data accumulates. Continue monitoring B/v16 hard-stop rate, close-confirm failures, true PnL, and C/v14 entry-candidate to open conversion. A/v11 sizing violations must remain zero after deployment.
- Verification: Local `py_compile` passed for `策略文件/scanner_v16.py`, `策略文件/scanner_v14.py`, `部署工具/release_manager.py`, and `部署工具/decision_attention.py`; `git diff --check` passed with only existing CRLF normalization warnings on JSONL/cache files. Dry-runs passed for Tencent `strategy-b`, Tencent `strategy-c`, Tencent `research`, and Aliyun `shadow`. Final live pull at `2026-05-31T02:04:30+08:00` showed all six expected Tencent services active, system alerts `ok/0`, attention `P0=0/P1=0/P2=5`, account unrealized PnL `-8.7217 USDT`, and 5 positions. A/v11 had 4 positions with approximate initial margins `100.82`, `99.89`, `99.63`, and `101.05` USDT, so no current A/v11 100 USDT sizing abnormality was present. Remote file grep confirmed B/v16 `SCORE_MAX=85` / `SL_MULT_LOW_VOL=2.5` and C/v14 `MAX_POSITIONS_PER_TF=3` / `SAMPLE_EXPANSION_POLICY` are on Tencent.
- Live impact / deployment: Deployed Tencent `strategy-b` release `20260531-020144-strategy-b-3a5f427`, Tencent `strategy-c` release `20260531-020214-strategy-c-3a5f427`, Tencent `research` release `20260531-020241-research-3a5f427`, Aliyun `shadow` release `20260531-020337-shadow-3a5f427`, and Tencent `portal` release `20260531-020348-portal-3a5f427`. B/v16 and C/v14 scanner services restarted successfully; A/v11 was not restarted or loosened.
- Files / release / commit: `策略文件/scanner_v16.py`, `策略文件/scanner_v14.py`, `config/v16.toml`, `config/v14.toml`, `research_memory/approvals/*`, `部署工具/release_manager.py`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-31 01:42 CST - Live review, event-store de-duplication, and report refresh
- Trigger / reason: User asked for a full health check, unfinished-task continuation, and a current review after v16 had been manually reset and the global flow had continued running.
- Completed: Pulled live context before drawing conclusions. Tencent A/v11, B/v16, C/v14, market-mover sentinel, account snapshot, and system alerts were all active. Found that SQLite `events` was double-counting strategy records because each scanner wrote the same OPEN/CLOSE/OPEN_FAILED/OPEN_SKIPPED/SIGNAL once through the raw event/signal source and again through the decision-log source; B/v16 also kept sentinel scan mirrors in `events`. Updated A/B/C scanners so JSONL decision logs remain intact while SQLite stores one authoritative event row only, and B/v16 sentinel scans now go to `sentinel_scans`. Added `data_maintenance.py --purge-event-store-mirrors`, tracked the data-maintenance systemd unit/timer in Git, deployed the change, and updated the live hourly maintenance service to purge mirrors persistently. Ran one-time cleanup on Tencent: removed 205,450 decision mirror rows and 24,665 sentinel event mirror rows. Regenerated account snapshot, system alerts, decision attention, and the portal report.
- Not completed / remaining: No strategy-evolution candidate was approved in this step. Current open P0 items are B/v16 strategy-evolution candidates (`EXP-20260527-v16-atr-stop-bands`, `EXP-20260527-v16-overheat-cap-85`), not service failures. Daily market review still shows the scheduled latest full-day file for 2026-05-29 because this review was run at 2026-05-31 01:xx, before the normal 2026-05-30 daily-review window; the live review used SQLite/current snapshots instead.
- Verification: Local `py_compile` passed for A/B/C scanners, `data_maintenance.py`, and `release_manager.py`. Deployed Tencent releases `20260531-013505-strategy-a-dbef97f`, `20260531-013603-strategy-b-dbef97f`, `20260531-013726-strategy-c-dbef97f`, and `20260531-013814-research-dbef97f`; all three scanner services restarted with `Result=success`, `NRestarts=0`, `ActiveState=active`, `SubState=running`. Tencent SQLite `quick_check ok` after cleanup. Final live pull at `2026-05-31T01:41:13+08:00` showed six expected services active, system alerts `ok/0`, account unrealized PnL `-4.7111 USDT`, `7` open positions, and attention `P0=2/P1=0/P2=5`. Regenerated `reports/index.html` no longer contains `no such table` or `无实时账户快照`.
- Live impact / deployment: Live strategy entry/exit thresholds were not loosened. The changed live behavior is observability/data-storage only: future SQLite rows are no longer double-written, B/v16 sentinel scans are stored in the dedicated table, and hourly maintenance now purges mirror rows if they reappear. A/B/C scanners were restarted.
- Files / release / commit: `策略文件/scanner.py`, `策略文件/scanner_v14.py`, `策略文件/scanner_v16.py`, `部署工具/data_maintenance.py`, `部署工具/release_manager.py`, `部署工具/systemd/crypto-data-maintenance.service`, `部署工具/systemd/crypto-data-maintenance.timer`, `CHANGELOG.md`, `记忆文档/MEMORY.md`; Git commit to contain this entry.

## 2026-05-30 09:14 CST - Live health continuation, open-size confirmation, and attention count repair
- Trigger / reason: User asked to check overall project runtime, find unfinished work, and continue completing it. Live inspection showed Tencent services were healthy, but local uncommitted work still needed completion: open-order confirmed quantity handling, A/v11 post-open 100 USDT sizing enforcement, Aliyun shadow sync slimming/path repair, portal realtime snapshot fallback, and attention summary counts that included cleared historical P0 items.
- Completed: `ExecutionEngine.open_position()` now uses confirmed exchange position quantity whenever confirmation is requested, so local tracked size follows the actual exchange position after Binance rounding/fills. A/v11 now confirms post-open size and automatically closes a new position if confirmed initial margin falls outside the allowed 100 USDT target band, logging `OPEN_SIZING_MISMATCH_CLOSED` or `OPEN_SIZING_MISMATCH_FAILED`. B/v16 and C/v14 local `SimPosition.size` now use executed/confirmed quantity; C/v14 also enables open-position confirmation. Cleanup and system alerts preserve/surface new open-sizing mismatch events. `portal_dashboard.py` now prefers the fresh JSON account snapshot and uses the synced Tencent mirror DB when available. `shadow_sync_from_tencent.py` now writes to the project-level `server_logs_tencent`, syncs compact runtime/report files, creates a slim verified SQLite snapshot instead of copying the full DB, skips legacy JSONL unless explicitly requested, streams/scp-fallbacks the archive, and avoids the Python 3.11+ f-string backslash syntax that broke Aliyun's runtime. `decision_attention.py` now counts priorities only for truly `open` items, so `cleared_pending_review`, `archived`, and `resolved` historical P0s no longer inflate the command-center P0 total.
- Not completed / remaining: No B/v16 P1 or P2 strategy-evolution candidate was approved; they remain user-review items. C/v14 now has an open P2 stale-open attention item after more than 48h without a new open, but the service heartbeat is current and no threshold was loosened. Continue monitoring A/v11 post-open sizing mismatch events and exchange close-confirm failures.
- Verification: Local `py_compile` passed for `core/execution_engine.py`, A/B/C scanners, `cleanup_event_store.py`, `decision_attention.py`, `portal_dashboard.py`, `shadow_sync_from_tencent.py`, and `system_alerts.py`; `git diff --check` passed. Local portal generation passed. Aliyun `/root/miniconda3/bin/python3 -m py_compile shadow_sync_from_tencent.py portal_dashboard.py cleanup_event_store.py decision_attention.py` passed. Tencent `decision_attention.py`, `portal_dashboard.py`, and `system_alerts.py --once` ran successfully; final live context pull at `2026-05-30T09:13:50+08:00` showed all six Tencent services active, system alerts `ok/0`, account unrealized PnL `+16.4749 USDT`, `6` open positions, and attention `P0=0/P1=2/P2=3`.
- Live impact / deployment: Deployed Tencent strategy releases `20260529-234335-strategy-a-740bcac`, `20260529-234335-strategy-b-740bcac`, and `20260529-234335-strategy-c-740bcac`; these restarted A/B/C scanner services and changed open confirmation/local sizing behavior but did not loosen entry thresholds. Deployed Tencent portal/alerts releases `20260529-234335-portal-740bcac` and `20260530-091145-portal-740bcac`, plus Tencent research releases `20260529-234623-research-740bcac` and `20260530-091145-research-740bcac`. Deployed Aliyun shadow releases `20260529-234623-shadow-740bcac` and `20260530-091127-shadow-740bcac`. All restarted Tencent services reported `Result=success`, `NRestarts=0`, `ActiveState=active`, `SubState=running`.
- Files / release / commit: `core/execution_engine.py`, `策略文件/scanner.py`, `策略文件/scanner_v14.py`, `策略文件/scanner_v16.py`, `部署工具/cleanup_event_store.py`, `部署工具/decision_attention.py`, `部署工具/portal_dashboard.py`, `部署工具/shadow_sync_from_tencent.py`, `部署工具/system_alerts.py`, `research_memory/attention/open_items.json`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-29 10:00 CST - A/v11 full trailing rollout, close-confirm risk loop, and daily report repair
- Trigger / reason: User approved both A/v11 trailing-pullback P0 candidates for full rollout and requested B/v16/C/v14 forced-close/normal-close loop repair, daily report chain cleanup, unified scanner close direction, and escalation if a forced close does not make the position disappear.
- Completed: A/v11 live trailing pullback now uses `15m=1.0 ATR` and keeps `30m=0.8 ATR`; both P0 candidates (`0.8` and `1.0`) are recorded as full-live approved, with the wider 1.0 selected because only one 15m live parameter can be active. `ExecutionEngine.close_position()` now derives a missing close quantity from the exchange, confirms the target symbol/side position is gone, retries once with remaining quantity, and returns failure if confirmation still shows a position. B/v16 and C/v14 now use `core.position_utils.infer_position_side()` and `leveraged_loss_pct()` for exchange-side counts, restored positions, duplicate-position logging, hard-stop loss calculation, and close direction; normal close and hard-stop failure paths now write `CLOSE_FAILED` / `FORCED_CLOSE_FAILED` and keep local state instead of silently deleting it. B/v16 client fallback was corrected so reduce-only fallback is only used on close, never on open-short. System alerts now surface recent close-confirm failures as bad alerts, de-duplicate same event-store double writes, and read `payload_json.raw.failure_reason` so the portal shows the Binance error code. Event cleanup protects the new failure event types. Aliyun daily shadow-review now regenerates `market_review_latest.md/html` before portal/reverse sync, repairing the stale daily report link in the full report chain. Decision attention now resolves full-live approved evolution candidates, so the two approved A/v11 trailing candidates no longer occupy P0.
- Not completed / remaining: B/v16 still has three legacy exchange positions (`BCHUSDT` long, `ETHUSDT` long, `FHEUSDT` short) that Binance Testnet rejects on close with `-4061: Order's position side does not match user's setting` after the close-confirm repair. C/v14 had no close-confirm failures in the checked window. The residual B/v16 problem is now visible as a system bad alert instead of a silent local-state deletion; continue diagnosing Binance Testnet/account-position corruption or manually flatten those legacy positions if the exchange UI allows it. If A/v11 1.0 ATR increases giveback materially, revert to the also-approved 0.8 ATR candidate.
- Verification: Local `py_compile` passed for `core/execution_engine.py`, all three Binance clients, A/B/C scanners, `decision_attention.py`, `system_alerts.py`, `cleanup_event_store.py`, `daily_market_review.py`, and `release_manager.py`. Tencent daily market review regenerated `market_review_2026-05-28.md/html`; final live pull at `2026-05-29T12:00:57+08:00` showed all six expected Tencent services active, account unrealized PnL `+41.7408 USDT`, `8` open positions, and attention `P0=0/P1=1/P2=9`. System alert status remained `bad/1` because B/v16 close confirmation is still failing on the three legacy positions with visible `-4061` detail.
- Live impact / deployment: Deployed Tencent strategy releases `20260529-111301-strategy-a-f583e34`, `20260529-113648-strategy-b-f583e34`, `20260529-113709-strategy-c-f583e34`, plus B/v16 follow-up `20260529-115110-strategy-b-f583e34`. Deployed Tencent portal/alerts releases `20260529-115333-portal-f583e34`, `20260529-115604-portal-f583e34`, and `20260529-115836-portal-f583e34`. Deployed Aliyun shadow/report releases `20260529-114623-shadow-f583e34`, `20260529-115540-shadow-f583e34`, and `20260529-115825-shadow-f583e34`; ran focused Aliyun `decision_attention.py`, `portal_dashboard.py`, and `sync_aliyun_reports_to_tencent.py` so Tencent report entry immediately reflects P0 cleanup.
- Files / release / commit: `core/execution_engine.py`, `策略文件/scanner.py`, `策略文件/scanner_v16.py`, `策略文件/scanner_v14.py`, `config/v11.toml`, `部署工具/system_alerts.py`, `部署工具/cleanup_event_store.py`, `部署工具/aliyun_shadow_review.sh`, `research_memory/approvals/*`, `CHANGELOG.md`; Git commit to contain this entry.
- Additional files in this change: `交易客户端/binance_client.py`, `交易客户端/binance_client_v2.py`, `交易客户端/binance_client_v3.py`, `部署工具/decision_attention.py`, `部署工具/release_manager.py`, `PROJECT_STATE.md`, and `记忆文档/MEMORY.md`.

## 2026-05-29 02:57 CST - Global live audit, shadow-review completion, and position direction normalization
- Trigger / reason: User requested a whole-project check of strategy runtime, server runtime, and automatic repair of discovered problems. The audit found two real issues: Aliyun daily shadow-review refreshed strategy evolution but did not rebuild decision attention/portal, and Binance testnet position rows sometimes had contradictory `positionSide` versus economic direction, making account PnL/risk direction misleading.
- Completed: Added `部署工具/aliyun_shadow_review.sh` and wired both release paths so the daily shadow-review timer now runs the full chain: sync Tencent SQLite snapshot, truth ledger, sentinel quality review, research memory, signal quality, experiments, counterfactual, evolution gate, decision attention, research dashboard, portal generation, and reverse sync to Tencent. Adjusted `system_alerts.py` attention-staleness threshold to match the Aliyun refresh cadence. Added `core/position_utils.py` and moved account snapshot, shared position models, daily market review, and execution-engine post-open confirmation to a unified effective-position-side inference: match raw unrealized PnL against entry/mark first, then fall back to notional sign, quantity sign, and raw `positionSide`. Fixed account description strings so `runtime/account_snapshot_latest.json` remains valid JSON.
- Not completed / remaining: Continue monitoring Binance testnet for inconsistent `positionSide` rows; the raw side is preserved in account snapshot positions as `raw_side`/`side_source` for audit. Strategy thresholds were not loosened in this change. The current P0 remains a strategy-evolution attention item, not a server outage.
- Verification: Local `py_compile` passed for `core/position_utils.py`, `core/models.py`, `core/execution_engine.py`, `部署工具/account_snapshot_html.py`, `部署工具/daily_market_review.py`, `部署工具/release_manager.py`, `部署工具/deploy_shadow_aliyun.py`, and `部署工具/system_alerts.py`. Synthetic side inference correctly classifies C/v14 `FILUSDT` as SHORT despite raw `positionSide=LONG`, and B/v16 `NEARUSDT` as LONG despite raw `positionSide=SHORT`. Remote account snapshot was regenerated and validated with `python -m json.tool`; `system_alerts.py --once` returned `alert_count=0`. Final `pull_live_context.py` at `2026-05-29T02:56:32+08:00` showed six expected Tencent services active, account unrealized PnL `+28.4635 USDT`, `8` open positions, and attention `P0=1/P1=1/P2=10`.
- Live impact / deployment: Deployed Aliyun shadow releases `20260529-023917-shadow-800da2b` and `20260529-025601-shadow-800da2b`; deployed Tencent portal/system-alert release `20260529-023917-portal-800da2b`; deployed Tencent account release `20260529-024916-account-800da2b`; deployed Tencent strategy releases `20260529-024950-strategy-a-800da2b`, `20260529-025022-strategy-b-800da2b`, and `20260529-025501-strategy-c-800da2b`. Scanner/account/system-alert services are active after deploy.

## 2026-05-29 02:16 CST - Correct SQLite growth follow-up after direct DB inspection
- Trigger / reason: The dual-server pipeline verification initially saw an Aliyun SQLite mirror report around 1.29GB, so the risk was recorded as remaining work.
- Completed: Performed read-only SQLite checks on both Tencent live DB and Aliyun synced mirror after the final backup-based sync. Tencent `/opt/crypto-auto-trader/runtime/event_store.sqlite3` is `212,201,472` bytes, `quick_check ok`, `freelist_count=0`. Aliyun `/opt/crypto-shadow-lab/server_logs_tencent/runtime/event_store.sqlite3` is `215,326,720` bytes, `quick_check ok`, `freelist_count=0`. Updated project memory to remove the stale 1.29GB blocker wording.
- Not completed / remaining: Continue normal disk/DB growth monitoring through system alerts and data maintenance; no immediate VACUUM or destructive cleanup is needed from this check.
- Verification: Read-only PRAGMA checks showed Tencent rows: `events=79032`, `sentinel_scans=29088`, `account_snapshots=32619`; Aliyun rows: `events=78528`, `sentinel_scans=28739`, `account_snapshots=32583`.
- Live impact / deployment: No live mutation; inspection/documentation correction only.

## 2026-05-29 02:12 CST - Complete dual-server report pipeline verification and stale-alert cleanup
- Trigger / reason: User changed code and asked to commit, then verify corresponding server strategy/functions. Live context showed Tencent trading services active, but migrated Tencent portal/report timers still appeared as failures in attention/report surfaces; Aliyun `sentinel_quality_review.py` also needed to be verified against the new `sentinel_scans` table.
- Completed: Updated `pull_live_context.py` so Tencent live-summary service status no longer includes the intentionally inactive `crypto-portal-refresh.service`. Updated `release_manager.py` so Tencent `portal` deploys restart only `crypto-system-alerts.service`, preventing future accidental restarts of migrated portal refresh. Expanded Aliyun `shadow` deployment lists in both `release_manager.py` and `deploy_shadow_aliyun.py` to include strategy truth ledger, sentinel quality review, counterfactual evaluation, attention API, Aliyun analysis refresh, and reverse sync scripts. Added `decision_attention.py` suppression/auto-archive for intentionally migrated or decommissioned components (`crypto-portal-refresh`, migrated analysis timers, Polymarket) so stale historical alerts no longer occupy P0. Fixed `shadow_sync_from_tencent.py` to include a SQLite `backup()` API snapshot of `runtime/event_store.sqlite3` with `pragma quick_check` before packaging, preventing malformed Aliyun DB mirrors. Fixed `aliyun_analysis_refresh.sh` to call the current `counterfactual_open_skips.py` CLI and to regenerate the decision-attention ledger before portal generation. Fixed `sync_aliyun_reports_to_tencent.py` to sync runtime truth/sentinel JSON and the correct attention ledger path back to Tencent. Updated README and PROJECT_STATE to clarify Tencent live services versus Aliyun report/API ownership.
- Not completed / remaining: Continue normal disk/DB growth monitoring through system alerts and data maintenance. A follow-up read-only inspection after this entry found Tencent/Aliyun SQLite sizes around 212-215MB with `quick_check ok` and `freelist_count=0`, so no immediate VACUUM or destructive cleanup is needed.
- Verification: Local `py_compile` passed for changed Python tools. Tencent portal release `20260529-015946-portal-2c55228` deployed and `crypto-system-alerts.service` stayed `active/running`. Aliyun shadow releases `20260529-020117-shadow-2c55228`, `20260529-020616-shadow-2c55228`, and `20260529-020912-shadow-2c55228` deployed. Aliyun sync produced `runtime/event_store.sqlite3` with `quick_check ok`, `events=78030`, `sentinel_scans=28415`, `account_snapshots=32544` in the checked run. `sentinel_quality_review.py` hash matched local and read `sentinel_scans`; latest full refresh: sentinel scanned `28739`, open rate `1.3%`, errors `4`; counterfactual succeeded with `773` events, `763` complete primary samples, simulated PnL `+593.77 USDT`; strategy evolution gate `P0=0/P1=2/P2=9`; decision attention `P0=0/P1=2/P2=10`; reverse sync uploaded 14 report/runtime/attention files to Tencent. Final `pull_live_context.py` at `2026-05-29T02:12:19+08:00` showed all six expected Tencent services active, system alerts `ok/0`, account unrealized PnL `+91.3105 USDT`, `6` open positions, and attention `P0=0`.
- Live impact / deployment: Reporting/analysis/deployment tooling only. No strategy threshold, order sizing, API-key value, or live order execution behavior was changed. Tencent system alerts were restarted; A/B/C scanner services were not restarted by this change.

## 2026-05-28 17:35 CST - Verify user changes and sync WorkBuddy memory
- Trigger / reason: User reported local/server changes including A/v11 API key update, `sentinel_scans`, attention confirmation API and portal buttons, then requested Git submission and server strategy-file/function checks.
- Completed: Verified Git `main`/`origin/main` already contains the recent user commits through `32d58b2`. Checked untracked `.workbuddy` memory files for secret-like strings; no API keys/private keys were found. Added those WorkBuddy memory notes to Git as durable handoff context. Verified local tracked code has no uncommitted changes. Checked Tencent services: A/v11, B/v16, C/v14, market-mover sentinel, account snapshot, and system alerts are active/running; `crypto-portal-refresh.service` is inactive as expected under the newer dual-server/analysis-pipeline architecture. Verified Tencent `sentinel_scans` table exists and has current data. Verified A/v11/B/v16/C/v14 strategy files and shared event-store hashes match local tracked files. Verified A/v11 no longer shows fresh `-1109 Invalid account` errors in the checked journal window and scanner services include `EnvironmentFile=/etc/crypto-auto-trader/trading.env`. Verified Aliyun `crypto-attention-api.service` is active and `/api/health` returns ok; portal HTML and `portal_dashboard.py` include the attention confirmation button and `/api/attention/ack` JavaScript hook.
- Not completed / remaining: Aliyun `sentinel_quality_review.py` appears older than local and still reads sentinel rows from `events`; it should be redeployed so sentinel quality analysis reads `sentinel_scans` first. Tencent does not currently have `cleanup_event_store.py` / `sentinel_quality_review.py` in `/opt/crypto-auto-trader` as checked; if Tencent local analysis is the intended final architecture, deploy those tools there. `crypto-portal-refresh.service` remains inactive on Tencent; leave it inactive if portal generation is intentionally owned by Aliyun or the Tencent-local analysis pipeline, otherwise update `pull_live_context.py` service expectations.
- Verification: Local `py_compile` passed for `core/event_store.py`, A/B/C scanners, `portal_dashboard.py`, `attention_api_server.py`, `sentinel_quality_review.py`, `cleanup_event_store.py`, and `system_alerts.py`. Tencent remote `py_compile` passed for `core/event_store.py`, A/B/C scanners, and `system_alerts.py`. Tencent SQLite counts: `events=42185`, `sentinel_scans=6108`, `account_snapshots=29556`, `attention_items=15`, `attention_acknowledgements=10`; recent `sentinel_scans` rows for 2026-05-28: A/v11 `3365`, C/v14 `2743`.
- Live impact / deployment: Inspection and Git sync only; no live service was restarted and no server file was changed in this step.
- Files / release / commit: `.workbuddy/memory/2026-05-27.md`, `.workbuddy/memory/2026-05-28.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-28 15:30 CST - A/v11 Testnet account recovery
- Trigger / reason: A/v11 Testnet account returned -1109 Invalid account for all write operations (order, leverage, evict) since 2026-05-27 18:02. Read operations (account balance) still worked.
- Root cause: Binance Testnet platform issue — account was marked invalid between 17:50-18:02 on 05-27 with no abnormal client behavior. Earliest -1109 appeared in EVICT_FAILED (weak position eviction), not in new order placement. Testnet periodically resets/inactivates accounts.
- Completed: (1) User registered new Testnet account with 5000 USDT balance. (2) Updated `/etc/crypto-auto-trader/trading.env` with new BINANCE_A_API_KEY and BINANCE_A_API_SECRET. (3) Set dual-side position mode via API. (4) Restarted crypto-scanner service. (5) Verified BTC market buy+sell works. (6) Confirmed no new -1109 errors after restart.
- Not completed / remaining: Scanner needs to complete first full scan cycle with new account to confirm end-to-end flow.
- Verification: API test: Account balance=5000, BTC BUY orderId=13291126667, sell successful. Scanner service active.
- Live impact / deployment: A/v11 now uses new Testnet API key. Existing positions unaffected (positions are on exchange, not local).
- Files / release / commit: `/etc/crypto-auto-trader/trading.env` (API key update), `CHANGELOG.md`.

## 2026-05-28 14:25 CST - Sentinel scans moved to dedicated table
- Trigger / reason: User requested sentinel scan records stored in separate table by date, not mixed into events table.
- Completed: (1) Added `sentinel_scans` table to `core/event_store.py` DDL with date column and per-day indexes. (2) Added `write_sentinel_scan()` method to `EventStoreWriter`. (3) Modified `策略文件/scanner.py` and `策略文件/scanner_v14.py`: added `_log_sentinel_scan_event()` that writes to sentinel_scans table instead of events. (4) Sentinel scans now: still write JSONL (daily shard), still write to SQLite decision log, but NO LONGER write to events table.
- Not completed / remaining: Deploy to Tencent (pending SSH recovery). `sentinel_quality_review.py` needs update to read from `sentinel_scans` table instead of events. Migration script to move existing SENTINEL_SCANNED from events to sentinel_scans.
- Verification: `py_compile` passed for core/event_store.py, 策略文件/scanner.py, 策略文件/scanner_v14.py.
- Live impact / deployment: Not yet deployed. Scanner restart needed after deploy.
- Files / release / commit: `core/event_store.py`, `策略文件/scanner.py`, `策略文件/scanner_v14.py`, `CHANGELOG.md`.

## 2026-05-28 14:30 CST - Sentinel scans separated into dedicated table
- Trigger / reason: event_store.sqlite3 grew to 1.7GB due to SENTINEL_SCANNED events (~97% of rows). User requested sentinel data in separate table with daily partitioning.
- Completed: (1) Added `sentinel_scans` table to `core/event_store.py` DDL with columns: ts, date (YYYY-MM-DD), strategy, symbol, event_type, reason, category, decision_stage, filter_layer, change_pct, velocity_pct, abs_velocity_pct, quote_volume, scan_result, payload_json. Indexes on date, (strategy,date), (symbol,date). (2) Added `EventStoreWriter.write_sentinel_scan()` method. (3) Modified `scanner.py` and `scanner_v14.py` — sentinel scans now write to `sentinel_scans` table via `_log_sentinel_scan_event()`, no longer pollute `events` table. (4) Modified `sentinel_quality_review.py` — reads from `sentinel_scans` first, falls back to `events` for backward compatibility. (5) Updated `cleanup_event_store.py` — also cleans `sentinel_scans` table by date column.
- Not completed / remaining: Deploy to Tencent (SSH still recovering). Need to migrate existing SENTINEL_SCANNED data from events to sentinel_scans (optional, old data will age out). A/v11 -1109 is Testnet account issue.
- Verification: All 5 files compile successfully. No strategy core algorithm changed.
- Live impact / deployment: New table created automatically on first write. Existing events table untouched. No strategy logic changed.
- Files / release / commit: `core/event_store.py`, `策略文件/scanner.py`, `策略文件/scanner_v14.py`, `部署工具/sentinel_quality_review.py`, `部署工具/cleanup_event_store.py`, `CHANGELOG.md`.

## 2026-05-28 14:00 CST - SQLite cleanup script, pipeline fix, portal fix
- Trigger / reason: event_store.sqlite3 on Tencent grew to 1.7GB, causing analysis pipeline timeout and SSH unavailability. Portal dashboard had Python syntax error (`{{}}` in f-string).
- Completed: (1) Added `部署工具/cleanup_event_store.py` — safely deletes SENTINEL_SCANNED/EVENT/SIGNAL older than N days, protects all trading events (OPEN/CLOSE/FORCED_CLOSE/OPEN_FAILED/OPEN_SKIPPED), creates backup before deletion, runs VACUUM. (2) Fixed `portal_dashboard.py` `{{}}` syntax error — replaced with proper Python dict construction. (3) Updated `aliyun_analysis_refresh.sh` — removed `set -euo pipefail`, added `timeout 600` for sync step, each step uses `|| echo WARN` instead of failing the pipeline. (4) Analysis scripts uploaded to Tencent for local execution (data-local-compute pattern).
- Not completed / remaining: Tencent server SSH unreachable (likely OOM from portal_dashboard.py on 1.7GB SQLite). Need to wait for recovery, then run cleanup_event_store.py to shrink DB. A/v11 -1109 is Testnet account issue (keys loaded correctly in process). Analysis pipeline architecture needs to shift: run on Tencent locally, sync only reports to Aliyun.
- Verification: cleanup_event_store.py dry-run passed locally. portal_dashboard.py compiles and generates locally. Aliyun portal updated to 11:15 CST.
- Live impact / deployment: No strategy code changed. Infrastructure fixes only.
- Files / release / commit: `部署工具/cleanup_event_store.py`, `部署工具/portal_dashboard.py`, `部署工具/aliyun_analysis_refresh.sh`, `CHANGELOG.md`.

## 2026-05-28 10:15 CST - Fix A/v11 OPEN_FAILED -1109 and B/v16 CLOSE pnl audit
- Trigger / reason: Daily review found 694 A/v11 OPEN_FAILED with -1109 Invalid account, and B/v16 CLOSE events with pnl=None.
- Completed: (1) Root cause for -1109: all three scanner services missing `EnvironmentFile=/etc/crypto-auto-trader/trading.env`. Added EnvironmentFile to crypto-scanner, crypto-scanner-v14, crypto-scanner-v16 services. Reloaded daemon and restarted all three. (2) Root cause for pnl=None: EventStoreWriter stores pnl in payload_json.raw sub-field, not at top level. Actual data exists (e.g. GRASSUSDT pnl=-1.5034, NEARUSDT pnl=+15.9727 in raw). This is a display/query issue, not data loss.
- Not completed / remaining: EventStoreWriter could be enhanced to extract pnl_usd/pnl_pct from raw to top-level columns for easier querying. B/v16 CLOSE events should populate top-level pnl fields.
- Verification: All three scanner services active/running after restart. EnvironmentFile confirmed in service config.
- Live impact / deployment: A/v11 should now successfully open positions with valid API key. No strategy logic changed.
- Files / release / commit: `CHANGELOG.md`; systemd service configs modified on Tencent server.

## 2026-05-27 21:45 CST - Attention API server and portal confirmation buttons
- Trigger / reason: User requested server-side API for attention item confirmation instead of script-only workflow.
- Completed: (1) Added `部署工具/attention_api_server.py` — lightweight HTTP API using Python built-in http.server, endpoints: GET /api/attention, POST /api/attention/ack, POST /api/attention/resolve, GET /api/health. Supports both SQLite and JSON fallback. (2) Deployed as `crypto-attention-api.service` on Aliyun port 8090. (3) Updated `portal_dashboard.py` — added "确认" button in attention table, JavaScript calls API via AJAX. (4) Synced attention JSON to Aliyun.
- Not completed / remaining: Aliyun security group may need port 8090 opened for external access. Portal needs to be regenerated on Aliyun to show buttons.
- Verification: `py_compile` passed. API service active/running. curl test returns items correctly.
- Live impact / deployment: New API service on Aliyun. Portal UI enhanced with confirmation buttons.
- Files / release / commit: `部署工具/attention_api_server.py`, `部署工具/deploy_attention_api.sh`, `部署工具/portal_dashboard.py`, `CHANGELOG.md`.

## 2026-05-27 20:40 CST - Phase 9 live transition checklist
- Trigger / reason: Execute Phase 9 of FUTURE_EXECUTION_PLAN.md.
- Completed: Added `部署工具/live_transition_checklist.py`. Evaluates readiness for Testnet→live transition by checking: PF≥1.2, win rate≥40%, ≥100 trades in 30d, hard-stop rate≤10%, gate status P0/P1, zero recovery positions. Includes transition rules (fee/slippage 0.15%, expected PF decay 0.7-0.9x on live) and rollback triggers. Outputs `runtime/live_transition_latest.json` and `reports/live_transition_latest.md`. Uploaded to Aliyun.
- Not completed / remaining: Current results: A/v11 NOT READY (2/6), B/v16 NOT READY (4/6), C/v14 NOT READY (2/6). No strategy meets all criteria yet. Live transition requires explicit user approval.
- Verification: `py_compile` passed. Test run: overall_ready=False, B/v16 closest at 4/6 checks passed.
- Live impact / deployment: None. Checklist only, no live transition.
- Files / release / commit: `部署工具/live_transition_checklist.py`, `CHANGELOG.md`.

## 2026-05-27 20:30 CST - Phase 8 promotion gate hardening
- Trigger / reason: Execute Phase 8 of FUTURE_EXECUTION_PLAN.md.
- Completed: Hardened `strategy_evolution_gate.py` `classify_decision()`: (1) Added `adjust_pnl_for_fees()` — deducts 0.15% round-trip cost from shadow PnL; (2) Added `check_rollback_triggers()` — checks sample sufficiency, PF decay, hard-stop increase, account risk; (3) P0 now requires ≥50 samples + fee-adjusted positive PnL; (4) P1 requires ≥30 samples; (5) Rollback triggers added as blockers. Constants: FEE_SLIPPAGE_ADJUSTMENT_PCT=0.15%, MIN_SAMPLE_FOR_P0=50, MIN_SAMPLE_FOR_P1=30, ROLLBACK_PF_DECAY_RATIO=0.8, ROLLBACK_HARD_STOP_RATIO=1.5.
- Not completed / remaining: Regime segmentation (trend/chop/high-vol/low-liquidity) not implemented. Rollback trigger automation not deployed.
- Verification: `py_compile` passed. Aliyun upload OK.
- Live impact / deployment: Gate logic hardened, no live strategy change.
- Files / release / commit: `部署工具/strategy_evolution_gate.py`, `CHANGELOG.md`.

## 2026-05-27 20:20 CST - Phase 7 recovery position management
- Trigger / reason: Execute Phase 7 of FUTURE_EXECUTION_PLAN.md.
- Completed: Enhanced `strategy_truth_ledger.py` with: (1) detailed recovery position audit per strategy (symbol, side, entry/mark price, PnL, margin, leverage); (2) `evaluate_recovery_exit_policies()` shadow-tests 5 candidate exit policies (4h/8h/24h time exit, 2% trailing, opposite signal); (3) recovery exit policies included in JSON output and MD report. Uploaded to Aliyun.
- Not completed / remaining: No automatic recovery exit deployed. Shadow results need validation before any live policy.
- Verification: `py_compile` passed. Test run shows 5 recovery positions, exit policies evaluated.
- Live impact / deployment: None. Analysis only.
- Files / release / commit: `部署工具/strategy_truth_ledger.py`, `CHANGELOG.md`.

## 2026-05-27 19:30 CST - Phase 3/4/5 shadow experiment configs
- Trigger / reason: Execute Phase 3/4/5 of FUTURE_EXECUTION_PLAN.md.
- Completed: Added 10 new experiment specs to `core/experiment.py`: A/v11 entry threshold 115/120, trailing pullback 0.8/1.0; B/v16 ATR stop bands, overheat cap 85; C/v14 strict candidate long65/70, filter ablation (sector, BTC trend). Added `run_threshold_experiment()` and `run_filter_ablation()` to `experiment_runner.py`. Total: 13 experiments. Uploaded to Aliyun.
- Not completed / remaining: Results need 3/7/14/30 day windows. No strategy code changed.
- Verification: `py_compile` passed. 13 specs loaded. Aliyun upload OK.
- Live impact / deployment: Shadow experiments only.
- Files / release / commit: `core/experiment.py`, `部署工具/experiment_runner.py`, `CHANGELOG.md`.

## Recording Rule

- Update this file in the same commit as any material repository change.
- Record a live deploy, rollback, service/config change, or risk decision even when no source code changed.
- Keep generated runtime/log/report data out of Git; record only the conclusion, validation result, release id, and remaining work here.
- Update `PROJECT_STATE.md` when the current architecture, operating procedure, or open priorities change.
- Update `记忆文档/MEMORY.md` when a durable decision, important incident, or milestone should survive daily report rollover.
- Read-only inspection that yields no new decision, incident, or action does not require an empty Git commit.

## Entry Template

```markdown
## YYYY-MM-DD HH:MM CST - Short title
- Trigger / reason:
- Completed:
- Not completed / remaining:
- Verification:
- Live impact / deployment:
- Files / release / commit:
```

## 2026-05-27 19:05 CST - Phase 6 sentinel quality review
- Trigger / reason: Execute Phase 6 of FUTURE_EXECUTION_PLAN.md — measure sentinel signal contribution.
- Completed: Added `部署工具/sentinel_quality_review.py`. Reads SQLite events with sentinel fields, classifies strategy response (opened/skipped/filtered/no_signal/error), computes per-reason and per-strategy stats, lists top 20 movers. Outputs `runtime/sentinel_quality_latest.json` and `reports/sentinel_quality_latest.md`. Uploaded to Aliyun and integrated into 2-hour analysis pipeline.
- Not completed / remaining: Forward-return calculation (15/30/60/120m) not yet implemented (needs K-line data). Coverage audit needs market snapshot comparison. Sentinel score bonus shadow experiment not started.
- Verification: Local `py_compile` passed. Local test: 17,982 sentinel decisions, 16,111 scanned, 14 opened, open rate 0.1%.
- Live impact / deployment: No live strategy change. Analysis on Aliyun only.
- Files / release / commit: `部署工具/sentinel_quality_review.py`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-27 18:50 CST - Phase 2 command-center strategy quality board
- Trigger / reason: Execute Phase 2 of FUTURE_EXECUTION_PLAN.md — integrate truth ledger into portal dashboard so decision-makers see "who is really making money" on the first screen.
- Completed: Extended `部署工具/portal_dashboard.py` with: (1) `strategy_truth_summary()` function reads `runtime/strategy_truth_latest.json`; (2) `build_data()` includes `strategy_truth` data; (3) `build_findings()` generates P1 alerts for negative-expectancy strategies (PF<1) and P2 notes for positive-expectancy strategies (PF>=2) and recovery positions; (4) HTML template includes new "策略质量看板" section showing per-strategy closed trades, win rate, net PnL, PF, payoff ratio, hard-stop count, recovery count, and recovery unrealized PnL. Uploaded to Aliyun.
- Not completed / remaining: Phase 6 (sentinel quality review) not started. Portal generation on Aliyun needs verification after next timer run.
- Verification: Local `py_compile` passed. Local portal generation succeeded. Generated HTML contains "策略质量看板" section with data from truth ledger (19 minutes old).
- Live impact / deployment: No live strategy change. Portal dashboard updated on Aliyun; will be synced to Tencent via reverse sync.
- Files / release / commit: `部署工具/portal_dashboard.py`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-27 18:30 CST - Phase 1 strategy truth ledger implementation
- Trigger / reason: Execute Phase 1 of FUTURE_EXECUTION_PLAN.md — create authoritative truth table separating active strategy PnL from recovery positions.
- Completed: Added `部署工具/strategy_truth_ledger.py`. Reads SQLite `event_store.sqlite3` OPEN/CLOSE/FORCED_CLOSE events and `account_snapshots`. Matches open-close pairs, identifies recovery positions (in snapshots but no matching OPEN event), computes per-strategy stats (win rate, PF, payoff ratio, hard-stop count, recovery count). Outputs `runtime/strategy_truth_latest.json` and `reports/strategy_truth_latest.md`. Uploaded to Aliyun and integrated into both analysis pipelines (daily full + 2-hour refresh).
- Not completed / remaining: Phase 2 (portal integration) not started. Truth ledger reads from synced SQLite on Aliyun; needs verification after first Aliyun timer run with real Tencent data. Fee/slippage estimates are approximate (100USDT × leverage × 0.05% × 2).
- Verification: Local `py_compile` passed. Local test run produced correct output: A/v11 closed=16 win_rate=12.5% net_pnl=-25.98 PF=0.01 recovery=3; B/v16 closed=24 win_rate=50% net_pnl=+127.72 PF=5.9 recovery=2; C/v14 closed=0 (limited local data). Aliyun script uploaded and integrated into both `run_shadow_review.sh` and `aliyun_analysis_refresh.sh`.
- Live impact / deployment: No live strategy change. New analysis script runs on Aliyun only.
- Files / release / commit: `部署工具/strategy_truth_ledger.py`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-27 17:30 CST - Phase 0.5 dual-server architecture migration execution
- Trigger / reason: User approved the dual-server plan and requested immediate execution. Aliyun (39.105.156.210) confirmed: 2 CPU, 1.7GiB RAM (1.4GiB available), 40GB disk (34GB free), Python 3.13.12 with numpy/pandas/paramiko/tomli installed. Only 1 timer running (shadow-review daily 02:20).
- Completed: (1) Slimmed Tencent `system_alerts.py` — removed portal-refresh, counterfactual, evolution-gate, and market-review service/timer checks (migrated to Aliyun). (2) Created `部署工具/sync_aliyun_reports_to_tencent.py` — reverse sync script uploads Aliyun-generated reports to Tencent. (3) Upgraded Aliyun `run_shadow_review.sh` — added Step 9 reverse sync after portal generation. (4) Stopped/disabled on Tencent: `crypto-portal-refresh.service`, `crypto-counterfactual-open-skips.timer`, `crypto-strategy-evolution-gate.timer`, `crypto-market-review.timer`. (5) Deployed Aliyun `crypto-analysis-refresh.timer` — runs every 2 hours: sync data → counterfactual eval → evolution gate → portal refresh → reverse sync to Tencent. (6) Cleaned MEMORY.md from 463 lines to 73 lines — distilled 2026-05-17~2026-05-24 detailed entries into a compact summary section.
- Not completed / remaining: Phase 0.5.1 Aliyun resource check complete. Phase 0.5.2 sync expansion (add event_store.sqlite3, account_snapshot_latest.json, research_memory/ to sync) not yet done. Phase 1 (truth ledger) not started. `deploy_shadow_aliyun.py` needs update to include new scripts.
- Verification: Tencent now shows 7 services (scanner×3 + sentinel + account-snapshot + market-data-cache + system-alerts) + 1 timer (data-maintenance). Aliyun shows 2 timers (shadow-review daily + analysis-refresh every 2h). All scripts `py_compile` passed locally. Aliyun timer deployed and enabled.
- Live impact / deployment: Tencent portal-refresh stopped — portal will be generated on Aliyun and synced back. Tencent counterfactual/evolution-gate/market-review timers stopped — these now run on Aliyun. No strategy code changed.
- Files / release / commit: `部署工具/system_alerts.py`, `部署工具/sync_aliyun_reports_to_tencent.py`, `部署工具/aliyun_analysis_refresh.sh`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-27 16:45 CST - Dual-server architecture optimization and expanded execution plan
- Trigger / reason: User requested optimizing the two-server (Tencent + Aliyun) architecture to reduce Tencent resource pressure, and expanding the execution plan with resource/API constraints, VPB evaluation, sentinel coverage audit, testnet-to-live transition, and rollback triggers.
- Completed: Rewrote `记忆文档/FUTURE_EXECUTION_PLAN.md` as a dual-server architecture plan. Key changes: (1) Tencent retains only 6 API-dependent services; Aliyun takes all analysis/report/experiment/gate tasks as Timer jobs; (2) Added reverse sync from Aliyun to Tencent for generated reports; (3) Added resource constraints: memory <50MB per new process, storage red line at 15GB free, Binance API weight budget <600/min (50% safety line); (4) Added Phase 0.5 for architecture migration, Phase 9 for testnet-to-live transition; (5) Added VPB contribution experiment in Phase 3; (6) Added sentinel coverage audit in Phase 6; (7) Added concrete rollback triggers in Phase 8. Updated `PROJECT_STATE.md` to reference the new plan.
- Not completed / remaining: No code changes made. Phase 0.5 architecture migration, Phase 1 truth ledger, and all subsequent phases remain unimplemented. Aliyun resource check (CPU/memory/disk) pending.
- Verification: Documentation files updated locally. No live service or strategy behavior changed.
- Live impact / deployment: None; planning/documentation only.
- Files / release / commit: `记忆文档/FUTURE_EXECUTION_PLAN.md`, `PROJECT_STATE.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-27 15:35 CST - Persist future strategy-quality execution plan
- Trigger / reason: User asked whether the detailed plan can solve all current problems, requested more concrete technical milestones, and asked to write it into the future execution plan.
- Completed: Added `记忆文档/FUTURE_EXECUTION_PLAN.md` as a durable handoff plan. It clarifies which current system problems the plan can solve and which future market/exchange problems it cannot guarantee. It breaks execution into technical phases: safety baseline, unified strategy truth ledger, command-center quality board, A/v11 evidence program, B/v16 payoff improvement, C/v14 rebuild/retire program, sentinel contribution review, recovery-position management, and promotion-gate hardening. Updated `PROJECT_STATE.md` and `记忆文档/MEMORY.md` so future agents discover and follow the plan.
- Not completed / remaining: This change records the plan only. It does not implement the planned scripts, portal sections, SQLite tables, shadow experiments, or promotion-gate extensions yet.
- Verification: Documentation files were updated locally and are ready for Git guard/commit. No live service or strategy behavior was changed.
- Live impact / deployment: None; planning/documentation only.
- Files / release / commit: `记忆文档/FUTURE_EXECUTION_PLAN.md`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`, `CHANGELOG.md`; Git commit to contain this entry.

## 2026-05-27 15:12 CST - Harden Binance open-order preflight and failure attribution
- Trigger / reason: User requested a comprehensive fix for recent open-order failures. Live SQLite inspection found repeated Binance failures from exchange order rules rather than strategy alpha alone: `-4164 notional_too_small`, `-4005 quantity_over_max`, `-4411 TradFi-Perps agreement required`, `-4131 percent_price_filter`, `-2027 max_position_or_leverage`, and `-1007 status unknown`.
- Completed: Added shared Binance USDM order-rule helpers for exchangeInfo parsing, MARKET_LOT_SIZE/minNotional/maxQty rounding, TradFi-Perps blocking, client order IDs, and percent-price preflight. A/B/C clients now share quantity validation and formatted market quantities. The execution engine performs quantity and market-price preflight before sending orders, preserves nested Binance error codes, and treats `-1007` status-unknown responses as success only after polling account positions. A/B/C scanners now record deterministic execution preflight rejections as `OPEN_SKIPPED` with `decision_stage=execution_preflight`, leaving `OPEN_FAILED` for real exchange/API failures. A/v11 also preserves raw Binance error responses instead of collapsing them to `-1`.
- Not completed / remaining: Exchange outages, backend timeouts with no confirmed position, and true account-level leverage/margin caps can still occur and should remain visible as `OPEN_FAILED`. Continue monitoring fresh failures by exact Binance code before changing strategy thresholds.
- Verification: Local `py_compile` passed for `core/binance_order_rules.py`, `core/execution_engine.py`, all three Binance clients, A/B/C scanners, and `release_manager.py`. `git diff --check` passed with only existing CRLF normalization warnings on two touched files. Remote AST syntax check passed for the deployed eight core strategy/client files without writing `__pycache__`. Deployed file SHA256 hashes match local for the changed strategy/client/execution files. Systemd shows A/B/C scanner services `active/running`, `Result=success`, `NRestarts=0`; journal since the deploy shows only normal stop/start and no Traceback/ImportError/SyntaxError/下单失败 lines. SQLite check since `2026-05-27 15:15 CST` found `OPEN_FAILED=0`. Latest live sync after deployment shows all seven core services active, alerts `ok/0`, five positions, account unrealized PnL about `+443.20 USDT`, and A/v11 sizing violations `0`.
- Live impact / deployment: Deployed Tencent `strategy-a` release `20260527-151519-strategy-a-9ac8976`, `strategy-b` release `20260527-151519-strategy-b-9ac8976`, and `strategy-c` release `20260527-151520-strategy-c-9ac8976`; restarted `crypto-scanner.service`, `crypto-scanner-v16.service`, and `crypto-scanner-v14.service`.
- Files / release / commit: `core/binance_order_rules.py`, `core/execution_engine.py`, `交易客户端/binance_client.py`, `交易客户端/binance_client_v2.py`, `交易客户端/binance_client_v3.py`, `策略文件/scanner.py`, `策略文件/scanner_v14.py`, `策略文件/scanner_v16.py`, `部署工具/release_manager.py`; source commit `9ac8976`, this follow-up ledger commit records live deployment verification.

## 2026-05-27 10:02 CST - Decommission Polymarket and remove command-center integration
- Trigger / reason: User requested deletion of all Polymarket-related files/code and stopping related services, then asked to analyze server memory and whether Account C/v14 filters remain too strict.
- Completed: Removed the tracked `polymarket_lab` code and the Tencent/Aliyun Polymarket deploy scripts. Removed Polymarket from the command-center portal, durable attention detector, live-context pull, and system-alert service list. Stopped, disabled, and removed Tencent `polymarket-monitor.service`; deleted Tencent `/opt/polymarket-lab`. Updated handoff docs so new agents treat Polymarket as decommissioned, not a current live module.
- Not completed / remaining: Historical memory still records the prior Polymarket experiments for audit context. C/v14 was analyzed but not loosened in this change; any threshold/confirmation change should be handled as a separate live strategy risk change after additional evidence.
- Verification: Tencent `polymarket-monitor.service` reports inactive after stop/disable/remove and `/opt/polymarket-lab` is absent. Server memory after shutdown: MemAvailable about 1.4GiB, swap used about 210MiB, no OOM lines in the checked 12-hour kernel journal; largest persistent Python processes are A/v11, B/v16, C/v14 at roughly 40-49MB RSS each. Local `py_compile` and `git diff --check` passed. Latest live sync pulls 20 files and only 7 active core services; `reports/index.html` no longer contains Polymarket text.
- Live impact / deployment: Polymarket is no longer monitored or shown in the command center. Tencent portal release `20260527-100318-portal-ad9d497` deployed and restarted `crypto-portal-refresh.service` and `crypto-system-alerts.service`; trading scanners, sentinel, account snapshot, and strategy evolution were not intentionally changed by this decommission.
- Files / release / commit: `部署工具/portal_dashboard.py`, `部署工具/decision_attention.py`, `部署工具/pull_live_context.py`, `部署工具/system_alerts.py`, `部署工具/git_change_guard.py`, `.gitignore`, `README.md`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`, deleted `polymarket_lab/*`, deleted `部署工具/deploy_polymarket_*.py`; Git commit to contain this entry.

## 2026-05-27 01:20 CST - A/v11 guarded replacement small-live and DB-backed attention acknowledgements
- Trigger / reason: User approved continuing `A/v11 EXP-20260523-v11-replacement-quality` only as an expansion/small-live path, not a full rollout, and requested all other P0 items be archived. User also challenged why P-level attention was being edited through JSON instead of a database.
- Completed: Narrowed A/v11 full-position replacement to the validated guarded scope: `STRONG_SIGNAL_THRESHOLD=112`, `EVICT_SCORE_GAP=25`, profitable positions at `>=2%` are hard-protected, policy version `small_live_v11_replacement_quality_v1`, no total-position increase, no same-symbol stacking change, and no entry-threshold loosening. Added a manual approval record for guarded small-live observation. Attention acknowledgements now write to SQLite tables in `runtime/event_store.sqlite3` (`attention_items`, `attention_acknowledgements`); JSON/Markdown/HTML are now export/cache surfaces for the portal, not the canonical acknowledgement store. Added `acknowledge_attention_items.py` and deployed it to Tencent.
- Not completed / remaining: A/v11 guarded replacement must now be monitored through real `EVICT_CLOSE`, `OPEN_RETRY_AFTER_EVICT`, A/v11 PnL, hard-stop risk, and missed-profit metrics before any further expansion. Attention UI still lacks a browser button for acknowledgements; acknowledgement is script-driven for now.
- Verification: Local `py_compile` passed for scanner and changed tools. Tencent `strategy-a` release `20260527-011050-strategy-a-68bca23` deployed and restarted `crypto-scanner.service` active/running. Tencent portal/research releases `20260527-011150-portal-68bca23` and `20260527-011150-research-68bca23` deployed. Server strategy evolution gate reran successfully: P0=0, top item is A/v11 `small_live_monitoring`, latest A/v11 replacement-quality delta `+844.0191`. Non-A/v11 P0 alerts (`入口页刷新失败`, `总入口页面偏旧`, `近期发生 OOM`) were archived in SQLite acknowledgements. Latest live sync shows 24 files pulled, all core services active, attention counts P0=0/P1=3/P2=4.
- Live impact / deployment: Live A/v11 replacement behavior is changed to a narrower guarded small-live policy and `crypto-scanner.service` was restarted. Portal/system-alert services restarted. B/v16 and C/v14 scanners were not restarted by this change.
- Files / release / commit: `策略文件/scanner.py`, `config/v11.toml`, `部署工具/decision_attention.py`, `部署工具/acknowledge_attention_items.py`, `部署工具/apply_research_approval.py`, `部署工具/experiment_runner.py`, `部署工具/release_manager.py`, `research_memory/approvals/*`, `CHANGELOG.md`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`; Git commit to contain this entry.

## 2026-05-27 00:45 CST - Global health audit and anti-stall live sync hardening
- Trigger / reason: The user requested a full project audit covering bugs, strategy direction, daily review progress, functional coordination, automatic fixes, Git sync, and durable handoff memory. The immediate operational pain was repeated long stalls while pulling live context and checking the server.
- Completed: Identified the 2026-05-26 21:56 CST Tencent SSH stall as an instance OOM event, not a simple network problem. Added a persistent 2G `/swapfile` on the Tencent server with `vm.swappiness=10`. `system_alerts.py` now reports memory, swap, and recent kernel OOM events as command-center alerts. `pull_live_context.py` now prefers OpenSSH with keepalive, compressed grouped file pulls, hard deadlines, and JSON error output; it also syncs daily market review latest Markdown/HTML and market snapshots. `daily_market_review.py` now shows C/v14 `入场候选/原始信号` instead of treating raw analysis noise as executable signals, and writes `market_review_latest.md` for fresh-clone handoff. Historical secrets in `记忆文档/MEMORY.md` were redacted from the current tree.
- Not completed / remaining: The OOM alert remains active until the six-hour kernel-journal window clears or the user explicitly accepts it in the attention ledger. Historical Git commits still contain old credential text; rotate any exposed Binance/testnet/API and SSH password credentials if they are still valid, and consider history rewriting before broader repository sharing. Strategy upgrade is not auto-applied: A/v11 replacement-quality is P0 `verified_upgrade_ready` but still requires expansion review.
- Verification: Local `py_compile` passed for changed scripts. Tencent services `crypto-scanner`, `crypto-scanner-v16`, `crypto-scanner-v14`, sentinel, account snapshot, portal refresh, system alerts, and Polymarket monitor are all `active`; four timers are `enabled`, and failed units are `0`. 2026-05-26 23:56 strategy evolution gate and 2026-05-27 00:00 data maintenance both finished with `Result=success`. Regenerated 2026-05-25 daily review on Tencent: C/v14 shows `57` entry candidates versus `46378` raw signals. Live context pull completed in about 20 seconds with `files_ok=24`.
- Live impact / deployment: Deployed Tencent `portal` release `20260526-234101-portal-84d0ad8`; deployed Tencent `research` releases `20260526-234139-research-84d0ad8` and `20260527-003530-research-84d0ad8`. No strategy scanner was restarted by the research update; portal/system alerts were restarted by the portal release. Server swap change is persistent through `/etc/fstab`.
- Files / release / commit: `部署工具/system_alerts.py`, `部署工具/pull_live_context.py`, `部署工具/daily_market_review.py`, `记忆文档/MEMORY.md`, `CHANGELOG.md`, `PROJECT_STATE.md`; Git commit to contain this entry.

## 2026-05-26 18:00 CST - Enforce complete Git synchronization for future changes
- Trigger / reason: The decision maker requires every future operation, design update, optimization, and change to be synchronized to Git with clear reasons, completed work, and remaining work so a different computer or model can continue without missing context.
- Completed: Added this durable change ledger; extended model and handoff rules; added a staged-change guard that rejects material code/config/tool commits without a staged `CHANGELOG.md` update.
- Not completed / remaining: GitHub CI does not yet enforce the guard remotely; until CI is added, agents must run the local check before every commit and push.
- Verification: `python -m py_compile 部署工具\git_change_guard.py` passed; when the tool file was staged without this ledger it correctly blocked the commit, and after adding this ledger `python 部署工具\git_change_guard.py` passed.
- Live impact / deployment: Documentation and local Git workflow only; no live strategy file, server configuration, or service is changed.
- Files / release / commit: `CHANGELOG.md`, `AGENTS.md`, `README.md`, `PROJECT_STATE.md`, `记忆文档/MEMORY.md`, `部署工具/git_change_guard.py`; no live release; Git commit contains this record.

## 2026-05-26 21:05 CST - Correct C/v14 signal-to-entry observability
- Trigger / reason: C/v14 showed extremely high signal counts but very few opens, making the command center imply a broken conversion funnel. Code inspection showed the metric was counting raw 15m confirmation signals and low-score raw analysis candidates as `SIGNAL`, while the live open gate only allows 1h entry candidates above the true threshold.
- Completed: C/v14 now writes `SIGNAL` only for real 1h entry candidates that pass the live score gate; raw candidate count remains in system heartbeat as `raw_signals_found`. Portal strategy summary now labels the column as entry candidates and, for C/v14, recalculates historical candidate counts from existing SQLite rows using the actual 1h long/short thresholds while showing raw candidate count separately. `config/v14.toml` was corrected to match the currently effective hardcoded thresholds. `release_manager.py` now accepts explicit `--dry-run` to match handoff documentation.
- Not completed / remaining: This does not loosen C/v14 live entry rules or prove a better C/v14 alpha. Next step is to collect post-fix candidate/open ratios and then decide whether C/v14 scoring itself should be redesigned or retired.
- Verification: Local `py_compile` passed for `策略文件/scanner_v14.py`, `部署工具/portal_dashboard.py`, and `部署工具/release_manager.py`. Local portal generation completed. `release_manager.py` dry-run passed for Tencent `strategy-c` and `portal`.
- Live impact / deployment: Intended live impact is observability/reporting only for C/v14 and portal; no trade threshold is loosened. Live deployment was attempted for release `20260526-210539-strategy-c-4c37e4d` but did not upload files because both Paramiko and native SSH timed out during SSH protocol banner exchange.
- Files / release / commit: `策略文件/scanner_v14.py`, `部署工具/portal_dashboard.py`, `config/v14.toml`, `部署工具/release_manager.py`; code commit `4c37e4d` pushed; live deployment remains pending until SSH access is healthy.

## 2026-05-26 21:14 CST - Retry Tencent deployment blocked before SSH login
- Trigger / reason: User requested updating the C/v14 observability fix to the server.
- Completed: Retried Tencent `strategy-c` release `20260526-211114-strategy-c-051f7d5` with a 30 second SSH/banner timeout and separately probed local TCP connectivity to port 22.
- Not completed / remaining: No server files were uploaded and no live services were restarted. Need restore healthy SSH banner/login first, then deploy `strategy-c` and `portal`.
- Verification: Paramiko deployment failed before authentication with `Error reading SSH protocol banner`. Native SSH also timed out during banner exchange. A short TCP probe returned `tcp_22_connected`, so port 22 is reachable but sshd did not return the SSH banner in time.
- Live impact / deployment: No live impact; deployment did not reach upload stage.
- Files / release / commit: This ledger entry records the failed operational attempt; code remains at `051f7d5`.

## 2026-05-26 21:45 CST - Tencent Lighthouse TAT channel also stalled
- Trigger / reason: SSH remains blocked before banner while C/v14 and portal deployment are pending; user attempted Tencent automated command execution as an alternate access path.
- Completed: Identified the target as Lighthouse instance `ap-singapore / lhins-8kx38vad`; evaluated the provided TAT invocation `invt-b43kp606bb`.
- Not completed / remaining: The TAT command has not executed because its status remains `DELIVERING` with empty output. Server access and pending deployment now require VNC login or restoration of an instance-side management channel.
- Verification: External TCP port 22 remains connectable while SSH banner times out; TAT returned `DELIVERING` for `ps aux | grep tat_agent | grep -v grep`, so it cannot currently confirm Agent state.
- Live impact / deployment: No server mutation verified; do not reboot the instance while trading services may still be running.
- Files / release / commit: Operational incident record only; pending deploy remains the C/v14/portal change from `4c37e4d`.

## 2026-05-26 22:08 CST - C/v14 and portal deployed after SSH recovery
- Trigger / reason: User restarted SSH on Tencent; pending C/v14 signal observability and portal summary fixes needed to be applied to live.
- Completed: Verified SSH login restored. Deployed Tencent `strategy-c` release `20260526-220420-strategy-c-631db66`, which uploaded C/v14 scanner/config/core dependencies, syntax-checked without writing `__pycache__`, and restarted `crypto-scanner-v14.service`. Uploaded portal files through release `20260526-220538-portal-631db66`; post command initially failed because it did not `cd` into `/opt/crypto-auto-trader`, then portal generation and `crypto-portal-refresh.service` / `crypto-system-alerts.service` restart were completed manually. Fixed `release_manager.py` so future post commands run from the target root and syntax checks avoid writing remote pycache.
- Not completed / remaining: Monitor the new C/v14 funnel over the next several scan cycles: raw candidates, entry candidates, opens, skips, and PnL. This deployment changes reporting/candidate logging, not C/v14 alpha.
- Verification: Remote portal now contains `入场候选` and shows C/v14 `148` entry candidates versus `84524` raw candidates. `crypto-scanner-v14.service`, `crypto-portal-refresh.service`, and `crypto-system-alerts.service` are all `active/running`, `Result=success`, `NRestarts=0`; v14 journal shows normal stop/start and no startup traceback in the checked window.
- Live impact / deployment: Live C/v14 service and command-center portal are updated. No entry threshold was loosened; the change narrows signal logging to real entry candidates and makes the portal disclose raw versus entry candidates.
- Files / release / commit: `部署工具/release_manager.py` fixed locally; live releases `20260526-220420-strategy-c-631db66` and `20260526-220538-portal-631db66`.
