# AutoTrading Project State

Last updated: 2026-06-10 Asia/Shanghai

This file is the long-lived project memory for multi-location development. Daily reports may roll forward, but this file records the current architecture, what is done, what is not done, and what must not be forgotten.

## Current Architecture

- Main live node: Tencent CVM, `/opt/crypto-auto-trader`.
- Shadow/review node: Aliyun, `/opt/crypto-shadow-lab`.
- Local workspace: `F:\AutoTrading`.
- 2026-06-07 mainline rule: Binance Testnet/simulation is no longer the first-version execution/data mainline. The active landing target is self-owned paper accounting fed by public online market APIs. OKX and Bybit provide high-frequency public market/flow inputs; CoinGecko is reference/sanity input for market-cap/top-universe context. Binance real/Testnet signed/order/account/listen-key paths stay stopped unless a later separate real-exchange recovery gate is approved.
- 2026-06-07 market-data rule: `crypto-market-data-cache.service` is now an external-public-data service, not a Binance queue producer. It builds `runtime/market_data_cache.json` from OKX + Bybit tickers and low-frequency CoinGecko reference data, with no Binance ticker call, no Binance API queue dependency, and no Binance start guard. Current first-version cadence target is OKX/Bybit Top-volume cache every 60 seconds, CoinGecko market-cap/reference refresh every 1800 seconds, A/B/C paper scanners every 60 seconds with 0/20/40-second restart staggering, scanner Kline cache TTL 300 seconds, B/v16 microstructure every 30 seconds, paper ledger every 60 seconds, and the public decision report every 60 seconds. B/v16 order-flow inputs are supported by `crypto-market-microstructure.service`, which stores compact OFI/CVD/funding features in `runtime/market_microstructure_latest.json` and bounded daily JSONL partitions instead of raw full tick history.
- 2026-06-08 early-mover API-budget rule: early pump/dump capture must reuse the existing 60-second OKX/Bybit ticker snapshots from `crypto-market-data-cache.service`; it must not add Binance calls, new ticker providers, extra Kline pulls, extra depth pulls, or scanner-side REST fanout. `market_data_service.py` calculates 60-second last-price tick, 24h change delta, and volume delta from the cached snapshots, then promotes `起涨捕捉` / `起跌捕捉` candidates into the watchlist ahead of ordinary sudden movers. This remains scan-priority evidence only: A/B/C strategy gates, sizing, stops, thresholds, paper fill logic, automatic rollback, automatic tuning, and automatic upgrade are unchanged.
- 2026-06-08 paper-report blocker rule: when `runtime/paper_exchange_latest.json` has `mode=paper_exchange`, report readiness must not treat stale/queued Binance restoration artifacts as current blockers. Binance queue/account-state/cooldown evidence may remain visible as historical/future-real-exchange risk, but it must not produce `blocked_by_queue`, `blocked_by_account_state`, or false "account state blocks opening" wording for the current external-data/self-paper mainline. Strategy open-staleness alerts must use paper positions/recent paper fills; open paper positions suppress false `48h 无新开仓` rows even if the older event DB lacks scanner OPEN rows.
- 2026-06-08 automatic-upgrade readiness rule: automatic strategy upgrade remains report-only and disabled. `auto_upgrade_readiness.py` separates non-sample blockers from sample blockers and always writes `automatic_upgrade_allowed=false` plus `apply_enabled=false`. It is wired into Aliyun refresh scripts, Tencent/Aliyun release bundles, reverse sync, live-context pull, first-screen report, full portal, and long-term skeleton checks. Non-sample blockers must be cleared by real evidence or explicit policy only: approved auto-upgrade policy, fresh strategy-evolution JSON, replay readiness, verified P0/P1 upgrade candidate, no active P0/P1 rollback pressure, and paper-vs-small-real calibration. Sample blockers remain fresh contextual paired samples for A/v11 and B/v16. Even if all preconditions reach `preconditions_met_report_only`, a separate human-approved procedure is required before any upgrade.
- 2026-06-08 strategy candidate governance rule: automatic strategy evolution now has a report-only governance registry layer. `strategy_candidate_governance.py` merges strategy-evolution decisions, readiness gates, rollback guard, replay readiness, and A/B rollout reviews into a candidate registry, parameter registry, sample acceptance contract, lifecycle summary, and pruning/promotion hints. This mirrors common public-framework governance patterns (parameter spaces/registry/lifecycle/progressive analysis) but keeps our system conservative: B/v16 rollback-watch rows are pause-expansion/quality-review items, A/v11 trailing rows are maturing/continue-sampling items, and no candidate is auto-promoted. The tool always writes `automatic_upgrade_allowed=false` and `apply_enabled=false`; it must never mutate strategy config, deploy, restart services, submit orders, or enable automatic rollback/tuning/upgrade.
- 2026-06-08 waiting-period progress rule: while fresh contextual samples mature, `waiting_period_progress.py` is the canonical report-only ledger for work that can advance now. It tracks eight items: sample-quality contract, B/v16 context gap, disabled auto-upgrade policy template, rollback dry-run preview, paper-vs-small-real calibration plan, report main-screen focus, API/disk guard, and accidental-apply guard. It writes `runtime/waiting_period_progress_latest.json`, `reports/waiting_period_progress_latest.md`, `runtime/paper_real_calibration_plan_latest.json`, and `reports/paper_real_calibration_plan_latest.md`; first-screen report, full portal, live-context pull, Aliyun refresh, reverse sync, and release bundles must keep these visible. The policy template at `research_memory/approvals/auto_upgrade_policy.json` deliberately has `approved=false` and `automatic_upgrade_enabled=false`: it removes missing-file ambiguity but does not approve automation. This chain must always show `automatic_upgrade_allowed=false`, `automatic_rollback_allowed=false`, `automatic_tuning_allowed=false`, `apply_enabled=false`, and `binance_requests_enabled=false` unless a later separate human-approved procedure explicitly changes the rule.
- 2026-06-08 paper tradability wording rule: `合约不存在` in A/B/C strategy rows means the external-market candidate failed the strategy tradable/simulatable contract-universe gate before paper entry. It is not a self-owned paper-ledger order failure. The first-screen report should explain this as "outside the current strategy可交易/可模拟合约清单" while keeping the raw reason as audit text.
- 2026-06-08 paper funding/accounting rule: self-owned paper bills do not calculate funding rates. `core.paper_exchange` and `paper_exchange_runner.py` maintain fills, positions, mark-to-market PnL, realized PnL, and fees only; legacy `funding_paid` / funding source fields are stripped from loaded paper state and prior funding cash impact is reversed. The first-screen report must not show funding-rate columns, funding caveats, or funding as a PnL component. This only removes funding from bill/accounting/report口径; B/v16 microstructure/scanner funding-rate features may remain as strategy inputs unless explicitly removed by a separate strategy-change request.
- 2026-06-08 operator-decision persistence rule: report-side operator decisions for strategy rollback/evolution items (`继续收样`, `收窄 B/v16`, `准备回滚`) are durable by `item_id`, not by volatile evidence fingerprint. If PnL/PF wording changes after `继续收样`, the same item must not reopen as an active blocking P1/P0. Plain system-alert acknowledgements still require matching fingerprints so new/different evidence can reappear.
- 2026-06-07 central Kline prefetch rule: `crypto-market-data-cache.service` now owns a bounded Top100/Top180 Kline prefetch loop. Top100 is the hot tier and Top180 is the wider warm tier; default prefetch specs cover A/B/C scanner needs (`15m`, `30m`, `1h`, `4h`) through OKX first and Bybit fallback, with no Binance Kline request. It writes Binance-shaped rows into `runtime/kline_cache` and publishes `kline_prefetch` status in `runtime/market_data_cache.json`. Scanner reads can reuse larger fresh cache files for smaller limits, so a centrally prefetched `1h_200` can satisfy `1h_100` and `1h_2` requests.
- 2026-06-08 replay-data ingest rule: 30-day Kline/depth readiness for automatic rollback/tuning must be filled through public external data first, not Binance queue. `external_replay_data_ingest.py` is the bounded operator tool for this path: Bybit first, OKX fallback public Klines to `research_store/klines`/`features`, and Bybit first, OKX fallback orderbook snapshots to `research_store/depth_snapshots`; no Binance request, no queue submit, no service restart, no live behavior change. Aliyun may be unable to reach Bybit directly, so OKX fallback is required for the first replay-data fill. Replay readiness remains blocked until `research_store_query.py` and downstream replay reviews confirm target coverage.
- 2026-06-08 replay-data fill status: first 30-day public seed is landed in Aliyun `research_store` and priority-synced to Tencent reports. `15m/30m/1h` Kline coverage is now `31` days with target_met=true, and depth has `10` symbols / `50` snapshots. This removes the Kline/depth coverage blocker for the first target set, but automatic rollback/tuning still must remain disabled because `replay_readiness_review.py` still reports A/v11 sample gap (`72h paired trades 5/10`) and B/v16 replay completion gap (`72h completion 0%`, missing-open pairing). The next data/replay work is event pairing/completion, not more blind Kline/depth fetch.
- 2026-06-10 one-year historical backtest data rule: Top30 one-year historical Kline collection is a Tencent-only historical warehouse path, not part of the live scanner/report refresh loops and not a two-server data sync workload. `historical_kline_backfill.py` is bundled only to Tencent research, defaults to plan-only, writes Tencent `runtime/historical_kline_backfill_latest.json` / `reports/historical_kline_backfill_latest.md`, and only calls public Bybit/OKX when `--apply` is passed. It must never call Binance, submit the Binance queue, restart services, change A/B/C scan frequency, mutate strategy config, or auto-trigger from `crypto-decision-portal-refresh.timer`. The bulky `research_store/historical_klines` warehouse and future historical backtests stay on Tencent; Aliyun only mirrors the small baseline/incremental progress JSON/MD and renders report visibility. Aliyun reverse sync must not upload `historical_kline_backfill_latest.json`, `historical_kline_backfill_latest.md`, `historical_kline_incremental_latest.json`, or `historical_kline_incremental_latest.md` back to Tencent; Tencent owns those artifacts. Operational status as of `2026-06-10 14:00 CST`: the frozen one-year task queue is complete (`pending_tasks=0`, `percent=100.0`, `written_rows=1602051`) with quality `complete_with_provider_gaps`, usable coverage `26/30` symbols and `104/120` symbol-interval pairs. This means queue complete, not perfect provider/listing coverage. The one-time full-backfill supervisor/operator was removed on Tencent after verification. Daily maintenance now uses `crypto-historical-kline-incremental.timer`, scheduled around `03:20 CST` with randomized delay, low public Bybit/OKX RPS, and the same no-live-scanner-impact rule.
- 2026-06-10 historical backtest module usable research-adapter rule: once the historical baseline is complete, the first-screen report should stop presenting `历史数据拉取进度` as an active progress block and instead show `历史回测模块`. `backtest_module.py` now accepts frontend/API job specs for A/v11, B/v16, or C/v14 with symbols, interval, period, direction, cost/slippage, and parameter JSON; validates registered parameter ranges; enforces anti-overfitting limits; and runs a read-only historical Kline research adapter through `backtest_engine.py`. Aliyun does not own the large historical warehouse; `/opt/crypto-shadow-lab` must always delegate backtest jobs to Tencent `/opt/crypto-auto-trader` unless `BACKTEST_DISABLE_REMOTE_DELEGATE=1` is explicitly set. Any residual Aliyun local historical partitions are ignored for frontend backtests so partial shadow data cannot return false `data_unavailable`. Aliyun mirrors only small job/status/report artifacts. Backtest job files are retained under `runtime/backtest_jobs/*.json`; reports/API expose a bounded recent list and fall back to `latest_job` when a mirror lacks the full job directory, so the UI must not imply only one history exists. The first-screen history list must be collapsed by default and may delete individual retained job JSON records through `DELETE /api/backtest/job?id=...`; deletion only removes the job record under `runtime/backtest_jobs`, never the Tencent historical Kline warehouse. Returned and rendered metrics include net PnL, return, max drawdown, profit factor, win rate, trades, fee/slippage, equity curve/PnL chart, drawdown/monthly returns, detailed open/close trade records, benchmark, coverage, summary conclusion, research recommendation, and parameter-sweep/OOS review. Frontend parameter controls must render the registered per-strategy tunable parameters with min/max/step and keep manual JSON as advanced input only; at most 3 tuned parameters and 24 variants remain enforced to avoid fitting. Job creation is currently synchronous research-adapter execution; the UI should show elapsed seconds and an estimated runtime band, not a fake percentage. True staged progress needs a later async Tencent job queue. This is `engine_parity=research_adapter`, not live scanner byte-for-byte replay, so results are useful for screening and parameter comparison but not sufficient for automatic promotion. Any parameter retest must keep train/validation/test, walk-forward/OOS, cross-symbol/timeframe/regime validation, minimum trade count, neighbor-parameter stability, and complexity penalty. Automatic tuning, automatic rollback, automatic upgrade, live strategy config mutation, scanner restarts, Binance calls, and order paths remain disabled.
- 2026-06-10 A/v11 full-universe research report rule: operator-driven A/v11 one-year full-symbol/full-timeframe parameter research should run through `v11_historical_research_report.py` on Tencent only. The script reads the completed Tencent `research_store/historical_klines` warehouse once, runs all requested intervals over available Top30 symbols, compares a small pre-registered parameter set, writes `runtime/v11_historical_research_latest.json`, and generates `reports/v11_historical_research_latest.html`. It intentionally separates "full-window best-looking PnL" from "anti-fit/OOS research candidate"; it does not mutate `config/v11.toml`, does not call Binance or any exchange API, does not restart scanners, does not place paper/real orders, and must keep `auto_apply_allowed=false`, `automatic_tuning_allowed=false`, `automatic_rollback_allowed=false`, and `automatic_upgrade_allowed=false`. In this research adapter, only `entry_threshold` and `trailing_pullback_atr` are compared because they are modeled by the current historical signal/exit path; registered `strong_signal_threshold` and `evict_score_gap` are documented but not treated as optimized until the replacement-position path is modeled.
- 2026-06-10 Tencent disk pressure rule: if `/opt`/root disk alerts show high usage, first distinguish Tencent vs Aliyun and root filesystem vs `/opt/crypto-auto-trader`. The 14:30 CST incident was not caused by the one-year historical Kline warehouse (`research_store` was about `502M`) and not by Aliyun (`24%` used); it was Tencent `/tmp` containing `2018` stale `/tmp/autotrading_shadow_sync_*` directories totaling about `31G`, left by interrupted shadow mirror sync. After deleting only those temp artifacts and vacuuming systemd journal to `256M`, Tencent root moved from `100%` used / `0.3G` free to `34%` used / `32G` free. `shadow_sync_from_tencent.py` must keep cleaning old `/tmp/autotrading_shadow_sync_*` dirs/files older than 15 minutes and remove the current temp dir immediately after bundle creation; do not solve this by deleting historical data, paper ledger, runtime DBs, release backups, durable memory, or research artifacts.
- 2026-06-08 disk/sync optimization status: Aliyun `/opt/easyforge` has been deleted after confirming EasyForge/Nginx services were inactive/disabled; do not recreate it without explicit approval and a security review. Aliyun runtime replay seed artifacts were removed after import. Key Aliyun-to-Tencent report sync should prefer `sync_aliyun_reports_to_tencent.py --priority-tar-only` when bounded refresh windows matter, because full per-file sync can stall on long-tail files. The lightweight Aliyun decision-portal refresh path uses priority tar sync for its reverse-sync step. Data maintenance now has release retention and Tencent-log-mirror retention knobs; use them before manual deletes so receipts stay auditable.
- 2026-06-08 retention status: Tencent releases are trimmed to the latest `120`; Aliyun shadow releases are trimmed to the latest `60`; Aliyun `server_logs_tencent` mirror is on a 7-day retention path. Reverse sync must not push Aliyun copies of Tencent-owned live-state runtime (`paper_exchange_latest.json`, `market_data_cache.json`, `market_microstructure_latest.json`) back to Tencent. Replay diagnostics now show the remaining rollout completion blocker is paper event pairing/context (`timeframe=paper_exchange` and missing open/entry timeframe), not disk, Kline, or depth coverage.
- 2026-06-08 paper replay context rule: new paper-exchange OPEN/CLOSE events must preserve the source strategy candidate context needed for replay: source event type/timestamp/payload, source timeframe, ATR, stop-loss/take-profit, and entry time on close. Rollout reviews should prefer the real source timeframe over the generic `paper_exchange` marker and should label unresolved old samples as paper context gaps instead of Kline/depth blockers. Historical paper events that never saved source context remain non-reconstructable; automatic rollback/tuning stays disabled until fresh contextual paired samples satisfy replay readiness.
- 2026-06-08 paper fill fidelity rule: `paper_fill_model_v2` is the default paper-exchange fill model. It uses fresh local depth snapshots when available, consumes asks for buy/long execution and bids for sell/short execution, records requested/executed quantity and price, partial-fill state, fill ratio, slippage, depth age/source, and fallback reason, and falls back to side-aware synthetic spread/slippage when fresh depth is missing. Scanner local positions and events should use the executed paper fill price/quantity while retaining requested price/exit-price audit fields. This is closer to live fills, but still not exchange-identical: no hidden-liquidity, real queue-priority, latency, Binance mark/index basis, or real commission calibration is proven. Funding is intentionally excluded from the paper bill口径. Automatic rollback/tuning remains gated by fresh contextual paired samples and later paper-vs-small-real calibration.
- 2026-06-08 paper ledger precision/dust rule: homepage paper-ledger fields must not round tiny non-zero quantities/prices to `0`; use enough precision or scientific notation for micro-price assets such as SATS/SHIB/PEPE. Active paper position counts should exclude near-zero notional residuals, because dust left after partial/close rounding is not economically open exposure. This is accounting/report hygiene, not a strategy behavior change.
- 2026-06-08 live-state homepage source rule: Tencent is the source of truth for current paper ledger, market cache, microstructure, and reset boundary. Aliyun first-screen report generation must prefer `server_logs_tencent/runtime/{paper_exchange_latest.json,market_data_cache.json,market_microstructure_latest.json,testnet_data_reset_latest.json}` over Aliyun local `runtime/` copies, even if local shadow files have newer mtimes. Aliyun local runtime is only a fallback when the Tencent mirror is absent.
- 2026-06-08 paper_fill_model_v2 deployment status: Tencent live node has `paper_fill_model_v2` plus the tiny-residual hotfix deployed through `58297fd`: research `20260608-032040-research-58297fd`, A/v11 `20260608-032339-strategy-a-58297fd`, B/v16 `20260608-032629-strategy-b-58297fd`, C/v14 `20260608-032730-strategy-c-58297fd`; A/B/C were restarted and verified active. Aliyun shadow has `20260608-032859-shadow-58297fd`. Final smoke confirms v2 preserves dust close quantities (`1e-11`) and live paper refresh/journals are clean. Report consistency commit `8304613` is deployed to Aliyun shadow `20260608-034239-shadow-8304613`, Tencent research `20260608-034239-research-8304613`, and Tencent portal `20260608-034239-portal-8304613`. Final live context shows paper v2 fidelity, `P0=0/P1=0`, B/v16 continue-sampling decisions preserved, and automatic rollback/tuning still gated.
- 2026-06-07 simulation-code cleanup rule: the old Binance Testnet/simulation landing artifacts are removed from source and release bundles: `market_mover_sentinel.py`, `crypto-market-mover-sentinel.service`, `deploy_market_mover_sentinel.py`, `paper_sample_executor.py`, and the staged stale-empty Testnet account-state assumption. Sudden-move watchlist now belongs to `market_data_service.py`; paper positions/PnL belong to `paper_exchange_runner.py` / `core.paper_exchange`. Dormant Binance client/API-queue/guard modules remain only for future explicit public-market or real-exchange recovery gates, not for the current mainline.
- 2026-06-07 sudden-move rule: sudden pump/dump capture now belongs to the external market-data mainline, not the old Binance sentinel service. `market_data_service.py` writes scanner-compatible `runtime/market_mover_watchlist.json` from OKX/Bybit/CoinGecko merged tickers every cache cycle, ranking 24h gainers, losers, change-velocity movers, and quote-volume spikes. A/B/C scanners already merge this watchlist into their scan universe through `core.market_watchlist`; it is scan-priority input only and does not directly open positions or loosen strategy gates. The old Binance sentinel service/source has been removed and must not be restored as a separate Binance producer.
- 2026-06-07 disk/replay rule: keep durable replay/evolution features, not bulky raw streams. First-version retention for microstructure partitions is 14 days on Tencent; long-history Klines/research artifacts remain on bounded research-store retention/compaction paths. Old Binance queue/runtime/log/archive data may be cleaned after write services are stopped, but durable memory, approvals, source, attention acknowledgements, reset receipts, and current paper ledger must be preserved.
- Primary decision entry: online Aliyun `http://39.105.156.210:8090/`, served by `crypto-attention-api.service` from generated `reports/index.html`.
- 2026-06-07 Aliyun security-containment rule: after Alibaba Cloud Security Center reported `DDOS木马`, confirmed root malware processes `t.me/flylegit` running deleted `/tmp/iran.x86_64` were killed and `/tmp/iran.*` payloads removed. Aliyun now keeps the AutoTrading decision/report API on port `8090`; exposed non-AutoTrading EasyForge services, Nginx port `80`, and stale Polymarket monitor were stopped/disabled as attack-surface reduction. Do not re-enable EasyForge, Nginx, or Polymarket on this node without a fresh security review and explicit user approval.
- 2026-06-07 Telnet hardening rule: after the incident was matched to a Telnet-bruteforce/DDoS toolchain using the `t.me/flylegit` marker, both cloud nodes were rechecked for Telnet. Ports `23` and `2323` are closed externally on Aliyun and Tencent. Tencent's dormant `inetutils-telnetd` package was removed/purged and telnet/xinetd units are masked. Do not install or enable Telnet services on either node.
- Local decision entry: `reports/index.html` generated by `部署工具/portal_dashboard.py`; this is a backup/mirror and is fresh only after pull/sync.
- Detailed drilldown entry: `reports/portal_latest.html` keeps the older full portal; the simplified first screen is generated by `部署工具/decision_portal.py`.
- 2026-06-04 decision portal rule: daily operator report should be online-first and same-origin with `/api/attention/ack`, so user-confirmation items can be acknowledged in the report UI without asking Codex each time. Aliyun `crypto-decision-portal-refresh.timer` is the lightweight 60-second refresh path: bounded Tencent mirror sync, decision attention, long-term skeleton review, waiting-period review, rollback execution dry-run plan, rollback automation guard, portal generation, reverse sync. It does not submit Binance queue work or call Binance REST. Deep analysis still runs through the longer Aliyun analysis/shadow timers.
- 2026-06-05 decision confirmation rule: the first-screen `你需要确认的事项` section must show only current open P0/P1 items that need operator action. Historical `cleared_pending_review` rows and open P2 observation items stay in the attention API/full drilldown, not in the first-screen confirmation table. Strategy-evolution/rollback items must be rendered in plain Chinese wording instead of raw `EXP-*` IDs/action codes, with separate "为什么出现" and "你现在要做什么" explanations plus explicit button meaning. For B/v16 rollback-watch rows, name the concrete change and surface recent after-cost PnL/PF evidence when present. The action cell should list the data the operator needs, not ask them to go ask Codex. Strategy decision buttons are `继续收样`, `收窄 B/v16`, and `准备回滚`; they write to `/api/attention/decision` and persist in the attention acknowledgement ledger. `继续收样` clears the row from homepage confirmation. `收窄`/`准备回滚` records an execution request for the guarded execution chain; it must not silently mutate strategy parameters from a browser misclick. The report `刷新报表` button calls `/api/report/refresh`, which starts the same lightweight report-only Aliyun refresh path; it must not submit Binance queue work, run scanners, run account baselines, or call Binance REST.
- 2026-06-07 manual-intervention execution rule: report-side `收窄 B/v16` / `准备回滚` decisions now feed `rollback_execution_plan.py`. Matching rollback-watch candidates are promoted to operator-requested dry-run statuses (`operator_requested_narrow_dry_run` / `operator_requested_rollback_dry_run`) and shown as actionable dry-run review items. The 60-second report refresh path must regenerate both rollback execution plan and automation guard before portal generation, so operator clicks become visible without waiting for the two-hour analysis refresh. This closes the manual intervention loop up to plan generation only; `apply_enabled=false`, release ids remain reviewed/operator-provided, and no config mutation, rollback, deployment, scanner restart, or order path may run automatically.
- 2026-06-07 mover participation report rule: the first-screen report must answer whether the daily gainer/loser/sudden-move board was scanned and whether the system entered. `今日涨跌榜跟踪` joins external `market_mover_preview` with current paper positions and shows scan-pool status, entry status, A/B/C position side, whether the side is trend-following or counter-trend relative to the move, current paper PnL, quote volume, and data source. It must also show the signal phase (起涨/起跌初段, 中段, 末段), a compact strategy-filter summary such as `挡：A/v11、C/v14；未扫：B/v16`, and the plain no-entry reason from recent `events` / `sentinel_scans`. Detailed strategy stage/layer and raw reason belong in the no-entry/audit text, not in the compact strategy-filter column. This is read-only report evidence; it does not force entries or loosen strategy gates.
- 2026-06-05 decision portal strategy table rule: the first-screen `三策略现在是否正常` table must not use one ambiguous `失败` column. It should separate `开仓执行失败`, `平仓/强平失败`, and `候选被挡住`, with a plain-Chinese `主要原因`. Strategy-confirmation rejections such as B/v16 `15m无确认信号` are normal gate blocks, not service failures; service red/green should follow systemd/service health.
- 2026-06-05 decision portal operator-language rule: the first-screen report should explain operator-facing fields in plain Chinese first, with raw technical causes only as secondary audit text. `主要原因` must translate common raw strategy reasons such as `15m无确认信号`, account-state freshness failures, cooldown/rate-limit rows, Kline gaps, threshold/score gates, duplicate-position gates, and min-notional rules into human-readable decision language. `今日重点` should avoid unexplained English/status-code phrasing when a short Chinese decision sentence is possible. Layout should favor wide desktop scanning: the report max width is wider, `今日重点` is a full-width cockpit panel, and strategy reasons get enough table width.
- 2026-06-06 decision portal visual rule: the first-screen report should follow a dark financial-operations dashboard style inspired by the user's Nord Analytics reference: left navigation rail, dark panels, dense KPI tiles, table-first operations layout, cyan/blue actions, green/yellow/red status accents, and Chinese operator wording. This is report-only; it must not add Binance calls, queue submissions, scanner starts, or strategy behavior changes.
- 2026-06-07 decision portal paper-ledger rule: the first-screen report now uses only the current external-data/self paper-ledger vocabulary. Do not show old Testnet/simulation, from-zero cleanup, small-release gate, or server-cleanup plan sections on the operator homepage. The A/B/C simulated ledger is tabbed by strategy; each position row expands to entry-context Kline SVG using existing local/mirrored Kline cache, with a visible auto-refresh countdown. Cleanup receipts and reset history remain in CHANGELOG/MEMORY, not the first-screen report.
- 2026-06-07 paper-mainline alert rule: when `runtime/paper_exchange_latest.json` shows `mode=paper_exchange`, the stopped real account-snapshot path is not an actionable homepage/attention warning. `crypto-account-snapshot.service` inactivity, missing `account_snapshot_latest.json`, stale real account snapshot, and empty `account_snapshots` should not be surfaced as P1/P0 blockers in this mode. The homepage should lead with `自建模拟账本` positions/PnL/refresh age; real account snapshots are only a secondary future real-exchange recovery panel until a separate recovery gate is approved.
- 2026-06-08 paper-mainline construction timer wording rule: `crypto-data-maintenance.timer` may remain inactive as a warning-level staged-validation maintenance task. In the current paper-ledger mainline it is not an A/B/C trading blocker, but automatic rollback/tuning still stay gated by replay/data readiness and fresh paired samples. The Aliyun first-screen homepage must prefer Tencent live mirrored alerts over Aliyun shadow-local alerts, because Tencent is the live node and local shadow alert mtimes can otherwise preserve stale wording.
- 2026-06-08 report wording deployment status: report-only commits `29f2a38` and `7067837` are deployed with no scanner restart. Releases: Aliyun `20260608-093145-shadow-29f2a38`, Tencent `20260608-093145-portal-29f2a38`, Aliyun `20260608-093829-shadow-7067837`, Tencent `20260608-093829-portal-7067837`. Aliyun/Tencent homepages both show the corrected `合约不存在` and data-maintenance timer explanations; final live context is `P0=0/P1=0/P2=1`.
- 2026-06-05 account-state confirmation rule: stale central account-state must not indefinitely block post-submit open/close confirmation. `ExecutionEngine` defaults `CENTRAL_ACCOUNT_STATE_CONFIRM_REST_FALLBACK_ENABLED=1`, so if central account-state is too old after an order submit, it invalidates the client snapshot and performs one controlled exchange position check for confirmation instead of immediately returning `*_confirm_account_state_unavailable`. Pre-entry risk gates require a real cached account-state row within the recovery TTL; the old staged stale-empty Testnet assumption has been removed. This fallback is only for post-submit proof. B/C close orders must keep hedge-mode `positionSide`, avoid `reduceOnly` fallback, and floor close quantity to step size so close requests do not exceed the actual position.
- 2026-06-05 open-success proof rule: a Binance/Testnet `orderId` is only submission proof, not proof that a position exists. `ExecutionEngine.open_position()` may mark local open success only when the order response has positive fill quantity (`fills`, `executedQty`, or `cumQty`) or post-submit account confirmation shows the matching position. If an order id exists but no fill/position proof is available, report `open_submitted_unconfirmed` and do not create a local position. This rule prevents `账户状态不够新` recovery work from hiding a worse problem: fake local opens from unfilled `NEW` orders.
- 2026-06-06 close reduce-only recovery rule: Binance/Testnet `HTTP 400/-2022 ReduceOnly Order is rejected` on a close is not an account-freshness failure when central account-state is fresh and still shows the position. Treat it as an exchange-side closeable-quantity/open-order conflict. Scanner close paths must not pre-cancel on every close and must not retry forever. `ExecutionEngine.close_position()` may recover only after the first close submit returns `-2022`: cancel open orders for that symbol only, require a clear cancel success result, then retry the same close once. If cancel fails or cancel support is absent, fail closed as `close_reduce_only_cancel_failed`.
- 2026-06-06 B/v16 residual exchange-position rule: if fresh central account-state still shows a B/v16 symbol open but the latest B/v16 event for that symbol is `CLOSE` or `FORCED_CLOSE`, treat it as a local-close/exchange-position mismatch, not as stale account-state. B/v16 may attempt one residual cleanup close through `ExecutionEngine.close_position()`, then remember the symbol in-process so a stale account snapshot cannot cause repeated close requests before account-state changes. Latest `CLOSE_FAILED` / `FORCED_CLOSE_FAILED` must not trigger automatic residual retry or normal Kline stop/TP fallback. Live ARBUSDT proof showed symbol open-order cancel succeeded but retry close still returned `-2022`; a minimal read-only signed diagnostic then hit `HTTP 418/-1003`, so all Binance-facing services are stopped until the global cooldown ends at `2026-06-06 03:12:55 CST` and a clean post-cooldown check passes.
- 2026-06-05 account-state recovery TTL rule: during Testnet post-cooldown recovery, pre-entry risk gates and close-target lookup may use a longer verified-account-state window through `BINANCE_ACCOUNT_STATE_CACHE_MAX_AGE_SEC=14400` and `CENTRAL_ACCOUNT_STATE_TARGET_MAX_AGE_SEC=14400`. This prevents a known real account-state snapshot from being discarded only because services waited through a Binance cooldown. Post-submit confirmation remains strict (`CENTRAL_ACCOUNT_STATE_CONFIRM_MAX_AGE_SEC=15`) and must use state observed after submission or the controlled one-shot REST confirmation fallback. Report wording must distinguish "pre-entry account-risk data too old" from "order submitted but post-trade confirmation proof not back yet".
- 2026-06-06 pre-entry account-state source rule: strategy pre-entry account-risk gates and reports must choose the newest valid account row across `runtime/account_state_latest.json` and legacy `runtime/account_snapshot_latest.json`; an older central-state file must not hide a newer account snapshot and falsely block opens as "账户状态不够新". `core.account_state_cache` now defaults pre-entry account-risk TTL to `14400s` even if the recovery drop-in is absent. A/B/C strategy releases install `crypto-scanner-account-state-recovery.conf` into each scanner systemd drop-in so the deployed service env matches the code/report rule. This rule does not weaken post-submit confirmation: order success still needs executed quantity or account-state/REST proof observed after submission, using the strict confirmation TTL.
- 2026-06-06 post-cooldown safe-observation restoration: after the `09:49:57 CST` global queue cooldown expired, Tencent was restored only to a no-order observation state. Active services are API queue, A/B/C user-stream (`crypto-binance-user-stream.service`, `crypto-binance-user-stream-v16.service`, `crypto-binance-user-stream-v14.service`), market-data-cache, sentinel, central-only account-snapshot, A/B/C scanners, and system-alerts. A/B/C scanners have a temporary high-priority `/etc/systemd/system/*scanner*.service.d/50-safe-observe.conf` drop-in forcing `SCANNER_ORDER_ENABLED=0`, `SCANNER_KLINE_NETWORK_ENABLED=0`, `SCANNER_MARKET_DATA_NETWORK_ENABLED=0`, account-state TTL `14400/14400/15`, and scanner interval `300s`. This keeps scan/report heartbeat alive but prevents opens, closes, cancels, leverage/margin calls, and post-submit signed confirmation probes. Do not remove this drop-in or enable orders until queue backlog is stable, account evidence is fresh enough, and B/C residual close blockers are handled without another signed diagnostic cooldown.
- 2026-06-06 12:20 restoration pause: the next gate tried a single A-only account-state baseline because A/v11 remained stale while B/C were fresh. Even that minimal signed `/fapi/v2/balance` request hit Binance/Testnet `HTTP 418/-1003`; the queue global cooldown now lasts until `2026-06-06 13:49:57.537 CST`. A/B/C scanners, market-data-cache, sentinel, account-snapshot, API queue, and A/B/C user-stream services are stopped again. Do not restart Binance-facing services, enable orders, or run signed diagnostics before cooldown expiry plus a clean read-only queue/service/journal gate. Waiting-period reports now treat account-state blocking as a restoration blocker, not as "ready".
- 2026-06-06 scanner market-data routing rule: scanner-side Binance public market fallback is now opt-in only through `SCANNER_BINANCE_PUBLIC_FALLBACK_ENABLED=1`; default is off. B/v16 OFI/depth, funding, and CVD/trades use OKX first and then neutral/empty fail-closed values instead of Binance depth/funding/aggTrades when fallback is off. A/v11 and C/v14 current-price checks use local Kline cache latest close first and skip/fail closed if no cached price exists; they do not call Binance ticker-price by default. Central `market-data-cache` and sentinel may still use their paced public ticker producer, but per-strategy direct public fallbacks should not be reintroduced during safe observation.
- 2026-06-06 16:03 safe-observation status: after the `13:49:57.537 CST` cooldown cleared and the queue/journal gate was clean, Tencent is again restored to full no-order observation. Active: A/B/C scanners, market-data-cache, sentinel, central-only account-snapshot, API queue, A/B/C user-streams, and system-alerts. `50-safe-observe.conf` still forces `SCANNER_ORDER_ENABLED=0`, Binance Kline/scanner market-data network off, and `300s` scanner intervals. A/B/C Binance clients now also obey `SCANNER_BINANCE_PUBLIC_FALLBACK_ENABLED=0` for `exchangeInfo`; scanner-client metadata lookup no longer queues Binance public `exchangeInfo` during safe observation. Post-fix queue since `15:55 CST` contains only paced cache/sentinel ticker `200` rows and no new `exchangeInfo`, `418`, `429`, or `-1003`. This is not order-enabled restoration; do not remove the safe-observe drop-in or retry signed baselines/residual closes without a new gated plan.
- 2026-06-06 paper full-run rule: because Binance/Testnet signed/order recovery keeps triggering IP wind-control even at very low request counts, the first-version "today full online" target is A/B/C full-universe paper execution, not real Binance order-enabled restoration. `SCANNER_EXECUTION_MODE=paper` makes the shared execution layer simulate filled opens/closes before any real order, close, cancel, leverage, margin, exchangeInfo, quantity-rule, or post-submit account-confirmation call. A/B/C scanners in paper mode skip startup real-account import and exchange-position sync; pre-entry risk uses local paper positions plus `SCANNER_PAPER_BALANCE_USDT` instead of central account freshness. Real Binance order paths must stay locked by `SCANNER_ORDER_ENABLED=0`; Binance Kline/scanner market-data fallback stays off. Reports may use these paper `OPEN` rows as simulated execution evidence for strategy iteration, but they are not live exchange fills.
- 2026-06-06 paper isolation correction: paper mode must not run exchange-level hard-stop sweeps against old central account-state positions. A/B/C `_enforce_hard_stop_on_exchange()` now returns immediately in `SCANNER_EXECUTION_MODE=paper`, so old MITOUSDT/SQDUSDT/ARBUSDT residual exchange rows cannot create paper `FORCED_CLOSE` / `CLOSE_FAILED` pollution. Paper full-run evidence must start after a runtime reset and should contain only fresh scanner signals, paper opens/closes, paper PnL, and service/report rows.
- 2026-06-06 paper exchange ledger rule: the daily operator report should prefer `runtime/paper_exchange_latest.json` as the first-version simulation trading ledger. `paper_exchange_runner.py` maintains A/B/C paper positions, mark-to-market PnL, fee debits, and explicit OPEN/CLOSE events with `mode=paper_exchange`; paper bills do not calculate funding rates. This is stronger than old `paper_sample_open` plumbing because it has an account ledger and live mark prices, but it is still not real Binance fills. Do not mix old exchange residual positions, stale account snapshots, and paper exchange positions in the same primary PnL table. Real exchange account data may remain a secondary risk panel only.
- 2026-06-06 paper exchange freshness/fidelity rule: Tencent is the paper exchange source-of-truth. `crypto-paper-exchange-refresh.timer` refreshes `paper_exchange_runner.py` about every 60 seconds using OKX public Klines and fresh local cache, never Binance signed/order paths. Aliyun must only sync and display Tencent `runtime/paper_exchange_latest.json`; it must not run its own paper ledger refresh or create a second source of truth. The report must show paper ledger age and a plain caveat: paper prices/time/fees are simulation inputs, while slippage, matching-engine latency, partial fills, Binance mark/index basis, and exact exchange commission can differ from real live trading. Funding is not part of the paper bill. Strategy promotion still needs measured paper-vs-real calibration before treating paper PnL as exchange-exact.
- 2026-06-06 fresh-start report/reset rule: when the user asks to clear historical dirty data and restart from zero, use `runtime_data_reset.py --include-generated-runtime --apply` only after stopping write services. The reset archives old runtime/event/log files and preserves durable memory, approvals, source code, attention acknowledgement tables, and baseline metadata. `paper_exchange_state/latest`, queue summary, waiting-period report, and generated skeleton runtime files are included in the reset so the first-screen report cannot show old paper positions as new. After reset, `paper_exchange_runner.py` can bootstrap from a default OKX-compatible symbol universe even if old market cache and event-store candidates were cleared. Replay/evolution requires complete trade loops (open, holding market context, close, fees, skipped candidates, and strategy context), not just open/holding count.
- 2026-06-06 fresh-start reset receipt: Tencent reset archive `/opt/crypto-auto-trader/archive/testnet_data_reset/20260606-210624` is the current from-zero boundary. Post-reset durable DB counts are `events=15`, `sentinel_scans=0`, `account_snapshots=0`, with A/B/C paper exchange opens `5/5/5`; attention acknowledgements and baseline metadata are preserved. `runtime/testnet_data_reset_latest.json` must be synced Tencent -> Aliyun -> Tencent report paths together with `runtime/paper_exchange_latest.json`, otherwise the public report can incorrectly show old mirrored data as "等待清理".
- 2026-06-05 cooldown protection status: a pre-fix deferred C/v14 `DELETE /fapi/v1/allOpenOrders` row hit Binance `HTTP 418/-1003`, and the queue recorded a global cooldown through `2026-06-06 00:13:59.648 +08`. A/B/C scanners, market-data-cache, sentinel, account-snapshot, API queue, and A/B/C user-streams were stopped; stale queued/deferred rows were failed so they cannot replay after cooldown. Until a read-only post-cooldown check is clean, do not restart Binance-facing services. New close behavior keeps `SCANNER_CLOSE_CANCEL_OPEN_ORDERS_ENABLED=0` by default, so scanner-managed exits do not first send extra signed `allOpenOrders` deletes. Order-rule market-price preflight uses public market data from `BINANCE_MARKET_BASE_URL` (default Binance mainnet public) and the normal queue client timeout (`180s` by default), not Testnet public with a short `timeout+5` wait.
- 2026-06-05 public market-data source safety rule: A/B/C scanners must not direct-fetch Binance Testnet Kline as a recovery shortcut. Direct Kline networking defaults off and now requires both `SCANNER_KLINE_NETWORK_ENABLED=1` and `SCANNER_DIRECT_KLINE_NETWORK_ALLOWED=1`; if explicitly enabled, scanner Kline URLs default to Binance mainnet public `https://fapi.binance.com`, not Testnet. Scanner public ticker/depth/funding/premiumIndex helpers and `market_data_service.py` also default to Binance mainnet public through `BINANCE_MARKET_BASE_URL`, not `testnet.binancefuture.com`; order/account Testnet endpoints remain unchanged. The preferred first-version path remains cache-only scanner plus controlled Kline cache/backfill. `research_kline_backfill.py --submit` writes explicit mainnet public Kline URLs into the central queue. During the active `2026-06-05 10:06:50 CST` global cooldown, scanners/cache/sentinel/api-queue stay inactive; only read-only checks and no-restart file deploys are allowed.
- 2026-06-05 staged expansion status: after the `10:06:50 CST` cooldown cleared and staged restoration stayed clean, Tencent B/v16 and C/v14 scanner universe limits were raised one layer from `1+1` to `3+3` (`SCANNER_B_TOP_SYMBOLS=3`, `SCANNER_B_SENTINEL_LIMIT=3`, `SCANNER_C_TOP_SYMBOLS=3`, `SCANNER_C_SENTINEL_LIMIT=3`). Kline networking remains off, scanner market-data fallback remains off, and B/C order-enabled staged mode remains active. Post-change queue check showed no active requests, no active cooldown, latest public rows `200`, and no fresh `418/429/-1003`. This is not normal-universe restoration; next expansion must be gated by another clean observation window.
- 2026-06-05 staged expansion layer 2: after roughly 35 minutes of clean `3+3` operation, B/v16 and C/v14 were raised to `5+5` (`SCANNER_B_TOP_SYMBOLS=5`, `SCANNER_B_SENTINEL_LIMIT=5`, `SCANNER_C_TOP_SYMBOLS=5`, `SCANNER_C_SENTINEL_LIMIT=5`). Scan frequency was intentionally not increased because frequency is direct API pressure. Kline networking and scanner market-data fallback remain off. Post-change first observation still had no active cooldown and no `418/429/-1003`; one queued public row is normal central-executor pacing.
- 2026-06-05 Top100 universe target: after the user clarified the target is top 100 by trading volume or market capitalization, live scanner drop-ins were changed to trading-volume Top100 coverage for all three strategies: A/v11 `100 top + 100 spike + 40 sentinel`, B/v16 `100 top + 40 sentinel`, and C/v14 `100 top + 40 sentinel`. This uses existing Binance volume-ranking inputs; Binance does not provide market-cap ranking, so market-cap Top100 requires a later external data-source integration. Frequency remains unchanged, Kline network remains off, and scanner market-data fallback remains off.
- 2026-06-05 Top100 effective-coverage small release: after a clean gate check with no active queue requests, no active cooldowns, no recent `418/429/-1003`, and no open records, Tencent A/B/C scanner effective coverage was widened from the earlier Top100 target capped by `sentinel_limit=40` to `sentinel_limit=100`: A/v11 `SCANNER_A_TOP_SYMBOLS=100`, `SCANNER_A_SPIKE_SYMBOLS=100`, `SCANNER_A_SENTINEL_LIMIT=100`; B/v16 `SCANNER_B_TOP_SYMBOLS=100`, `SCANNER_B_SENTINEL_LIMIT=100`; C/v14 `SCANNER_C_TOP_SYMBOLS=100`, `SCANNER_C_SENTINEL_LIMIT=100`. Scan frequency remains `120s`, market-data-cache/sentinel remain `300s`, direct Kline networking remains off, scanner market-data fallback remains off, and strategy thresholds/sizing/leverage/stops are unchanged. Immediate observation showed B/C scanning about `106` symbols and A/v11 real scans resumed; no opens yet.
- 2026-06-05 external data-source credential staging: local ignored `.env.server-secrets` was used to install non-empty Binance mainnet and OKX variables into `/etc/crypto-auto-trader/trading.env` on Tencent and Aliyun with mode `0600`. This is credential staging only: no code consumes those credentials yet, Binance mainnet trading is not enabled, Bybit/CoinGecko remain unset, and no services were restarted. Future multi-source work should add read-only adapters first and keep Binance mainnet keys out of order/execution paths until explicitly approved.
- 2026-06-05 B/v16 OKX read-only data path: B/v16 now uses OKX public market data for Kline/volume, OFI/depth, funding-rate, and CVD/trades on Tencent through `OKX_MARKET_DATA_ENABLED=1`. OKX Klines are converted into the existing Binance-shaped cache rows and saved in `runtime/kline_cache`, so repeated scanner passes do not repeatedly hit external APIs. Binance/Testnet Kline networking remains off, Binance mainnet trading is not enabled, and strategy thresholds/sizing/leverage/stops are unchanged. Immediate post-enable observation showed B/v16 active, no Binance rate-limit/cooldown, `171` scanned symbols in the 10-minute window, and `83` fresh Kline cache files. This is the first multi-source data path; A/v11, C/v14, Bybit, and CoinGecko are still future work.
- 2026-06-05 A/C OKX read-only Kline split: A/v11 and C/v14 now share the OKX read-only Kline path. A/C `fetch_klines()` uses cache first, then OKX when `OKX_MARKET_DATA_ENABLED=1`, and only then the old Binance path; live drop-ins keep `SCANNER_KLINE_NETWORK_ENABLED=0` and `SCANNER_MARKET_DATA_NETWORK_ENABLED=0`, so scanners do not re-enable Binance Kline fallback. Post-enable observation showed no active Binance queue/cooldown, no `418/429/-1003`, fresh Kline cache growth to `215` files in 10 minutes, and A/B/C scanning about `169/183/185` symbols. Effective range is already Top100+; frequency remains `120s`. Next cadence expansion should be a separate clean-window, one-strategy test, preferably B/v16 first, not all strategies at once.
- 2026-06-05 all-strategy frequency release rule: A/B/C scanners now support env-controlled interval variables (`SCANNER_A_INTERVAL_SEC`, `SCANNER_B_INTERVAL_SEC`, `SCANNER_C_INTERVAL_SEC`) with prior `120s` defaults. Per user direction, the live release target is all three at `90s`, while lowering `OKX_MARKET_DATA_MAX_PER_MIN` to `60` per scanner process. Direct Binance Kline and scanner market-data fallback stay disabled; Top100+ universe and strategy risk parameters stay unchanged. Roll back interval drop-ins to `120s` first if queue/cooldown/journal checks deteriorate.
- 2026-06-05 external market-data hot-path rule: external data helpers used by A/B/C scanner loops must not sleep inside the scanner hot path when a provider-side per-process budget is exhausted. They should fail fast, let scanner cache/direct-source gating decide the next step, and avoid turning budget exhaustion into symbol-level negative cache. Unsupported external symbols, especially non-ASCII Binance-only/test symbols, should be rejected before network calls; provider failures for a concrete symbol/source/interval should be negative-cached briefly to avoid repeated retries every scan cycle. This protects A/v11 freshness after OKX was enabled for Top100+ scans.
- 2026-06-05 A/v11 scan-heartbeat rule: `sentinel_scans` is only a sentinel-context table and must not be the sole proof that A/v11 is alive. A/v11 now writes `SCAN_STATS` through `log_system()` at scan-cycle start (`phase=start`) and at normal cycle end (`phase=complete`), with `phase=error` for symbol-list failures. Report/health code should treat fresh A `SCAN_STATS` rows as the primary A/v11 liveness source when sentinel context is absent, cache-only Kline gaps prevent sentinel rows, or a long scan is still in progress. A/v11 VPB funding-rate lookup should prefer OKX read-only funding; when OKX market data is enabled, Binance public `premiumIndex` fallback is disabled by default and only returns if `VPB_BINANCE_FUNDING_FALLBACK_ENABLED=1`.
- 2026-06-05 waiting-period optimization rule: while Binance/API stability is being observed, allowed optimization work is report-only/offline. `waiting_period_optimization.py` reads local/mirrored runtime data only and writes JSON/Markdown/HTML for queue/cooldown state, Top100 volume coverage, OPEN_SKIPPED/no-open reasons, Kline/depth gap plans, report freshness, and safe waiting actions. It is wired into Aliyun lightweight/deep refresh, reverse sync, live-context pull, release bundles, decision portal, and full portal. It must not submit queue work, request Binance, restart services, raise scan frequency, enable direct Kline networking, run signed baselines, or change strategy parameters.
- 2026-06-05 waiting-period next-stage report rule: `waiting_period_optimization.py` now explains no-open/no-trade reasons in plain Chinese, derives Top100 symbols from current `market_data_cache.available_symbols`, shows per-strategy live heartbeat from `pull_live_context.py`, and reports Top100 real-scan coverage only when the local/mirrored event DB is fresh enough for the selected window. If the mirrored SQLite is stale, coverage is `stale_mirror_unknown` instead of a misleading `0%`. The readiness gate remains report-only: no frequency increase, no Kline/depth `--submit`, no service restart, no strategy parameter change.
- 2026-06-06 waiting-period cooldown/account-state rule: the waiting-period report must not declare the system safe when queue DB details are missing but fresh alerts show Binance `418/429/-1003` or cooldown, or when recent bad Binance/API rows exist even without an active cooldown row. It now reads live `api_cooldowns` as well as legacy `cooldowns`, and falls back to alert evidence by marking `blocked_by_cooldown`. It also exposes account-state freshness as a separate read-only section: pre-entry account-risk data is judged against the recovery TTL (`14400s`), while post-submit fill/position proof remains strict (`15s`). Account-state source selection must choose the newest valid account row timestamp across `account_state_latest.json` and `account_snapshot_latest.json`, so stale mirror files cannot falsely claim "账户状态不够新". If the report shows `account_state_blocking=false`, do not explain current no-open/no-run state as "账户状态不够新"; inspect cooldown, service stop, close failures, or strategy gates instead.
- 2026-06-06 post-cooldown waiting report status rule: once active queue cooldowns are expired and active queued/deferred/leased rows are empty, historical `recent_bad` rows must not keep the report in `blocked_by_cooldown`. Waiting-period status now distinguishes `blocked_by_cooldown` for current cooldown, `blocked_by_queue` for uncleared active rows, and `cooldown_clear_recent_bad_history` for clean queue with historical bad-request evidence. The last state is a risk warning and staged-restoration caution, not proof that cooldown is still active.
- 2026-06-06 recovery TTL deployment status: commits `60e2084` / `1220f48` are no-restart deployed to Tencent A/B/C strategy bundles and Tencent/Aliyun report bundles. Tencent installed A/B/C scanner drop-ins with pre-entry/close-target account-state TTL `14400s` and post-submit confirmation TTL `15s`. Tencent Binance-facing services remained inactive after deploy; only `crypto-system-alerts.service` active. Tencent/Aliyun waiting reports both show `account_state_blocking=false`, `pre_entry_ttl_sec=14400`, `active_cooldowns=0`, and `active_requests=0`.
- 2026-06-06 full-restore cooldown status: a user-requested full restoration attempt at `09:14-09:22 CST` passed the initial read-only queue/journal gate, but the required A/v11 single-account signed baseline immediately hit `HTTP 418/-1003` on `/fapi/v2/balance`. API queue was stopped immediately. New queue global cooldown is active until about `2026-06-06 09:49:57.944 CST`. Do not start Binance-facing services, retry signed baselines, start user-stream listen-key flows, or run scanners until a post-cooldown read-only gate is clean.
- 2026-06-05 report cockpit rule: the first-screen `decision_portal.py` report is now a cockpit-style operator view with Top100 real-scan metric and a visible small-release gate. `waiting_period_optimization.py` HTML is also a cockpit/detail page with cards, progress bars, service heartbeat, Top100 coverage matrix, no-open reason table, Kline/depth plan-only status, and safety gate panels. These report pages remain read-only and must not call Binance, submit queue work, or restart services.
- 2026-06-04 attention API source rule: `crypto-attention-api.service` must read/write Aliyun local `runtime/event_store.sqlite3` and `research_memory/attention/open_items.json`, not the Tencent mirror DB under `server_logs_tencent`. The Tencent mirror is analysis input only; using it for `/api/attention` can resurrect stale cleared P0 items and make the page disagree with the confirmation API. Public verification after the unit fix showed `/api/attention` open counts `P0=0/P1=0/P2=2` and no stale `账户快照采集失败` / `HTTP 418`.
- 2026-06-03 construction-mode note: Tencent Testnet trading/scanning/reporting loops are intentionally paused while long-term P0/P1 engineering is prioritized. Earlier staged construction briefly ran `crypto-binance-api-queue.service` and A/B/C `crypto-binance-user-stream*.service`, but after the offline-first redirect all Binance-facing/test services are intentionally inactive until a final staged fresh-run is ready. This is not an outage until the project is explicitly resumed.
- 2026-06-03 offline-first redirect: user requested stopping slow staged server testing and pushing optimization goals offline first. As of `10:57 CST`, all Binance-facing/test services are intentionally inactive: API queue, A/B/C user-stream, market-data-cache, sentinel, A/B/C scanners, account-state, account-snapshot, system-alerts, and data-maintenance. Do not restart services for incremental tests until offline P0/P1 implementation is substantially complete and a final staged fresh-run is ready.
- 2026-06-04 long-term skeleton status: `部署工具/long_term_skeleton_review.py` is now the first-version landing gate for the canonical long-term queue. It checks only bones (input, main processing, output, portal, sync/deploy, tests) and does not start services or call Binance. Current local result after FINAL foundation zero-run acceptance is `143/143` bones ready, `missing_skeleton=0`, `skeleton_ready=12`, `blocked_by_staged_validation=0`, `validation_blockers=0`, and `post_launch_backlog=19`. First-version skeleton is landed; remaining work is staged service restoration and post-launch evidence collection, not first-version blockers.
- 2026-06-04 FINAL reset/zero-run status: Tencent dirty testnet runtime reset was applied after safe pre-checks. Archive: `/opt/crypto-auto-trader/archive/testnet_data_reset/20260604-124114`; receipt: `/opt/crypto-auto-trader/runtime/testnet_data_reset_latest.json`; reset tables after apply: `events=0`, `sentinel_scans=0`, `account_snapshots=0`, with attention/baseline tables preserved. A first zero-run attempt hit A/v11 signed `/fapi/v2/positionRisk` `HTTP 418/-1003`, so all Binance-facing services were stopped until the signed cooldown cleared. The accepted retry deployed the bootstrap fix, ran account-state as stale placeholders without signed REST, then left API queue plus A/B/C user-stream active with queue `pending=[]`, `cooldowns=[]`, `max_rowid=470`, reset tables still `0/0/0`, and no journal `418/429/-1003` through `13:16:40 CST`.
- 2026-06-04 post-cooldown staged restoration checkpoint: the B/v16 signed cooldown expired at `2026-06-04 15:15:54.959 +08`, and the read-only post-cooldown check was clean (`active_cooldowns=[]`, `active_status=[]`, no fresh `418/429/-1003`). The first restoration layer brought API queue, market-data-cache, sentinel, and A/B/C user-streams back with rows `486`-`508` clean (`200`), including A listen-key row `494`, B listen-key row `501`, and C listen-key row `506`. Central account-state was still partial at that checkpoint: A fresh, B/C stale (`fresh_accounts=1`, `stale_accounts=["B","C"]`). Do not retry B/C signed baselines; scanner restoration must not treat user-stream idle as account freshness.
- 2026-06-04 staged scanner observation rule: `SCANNER_ORDER_ENABLED=0` is now the intended first scanner-observation switch after post-cooldown user-stream restoration. Default behavior remains order-enabled, but disabled staging mode makes `ExecutionEngine` reject open/close as `scanner_order_disabled` preflight before order, close, cancel, or confirmation calls; C/v14 also skips its pre-open leverage/margin calls in this mode. Use it only with the existing cache-only/small-universe scanner drop-ins and queue/cooldown/journal prechecks. It helps validate scanner input/main/output/portal evidence without creating orders or adding signed/order pressure; it does not make stale B/C account-state fresh.
- 2026-06-04 staged scanner observation result: commit `962147a` was deployed no-restart to Tencent A/B/C strategy releases `20260604-173137-strategy-a-962147a`, `20260604-173229-strategy-b-962147a`, and `20260604-173328-strategy-c-962147a`. A/B/C scanner drop-ins now include `SCANNER_ORDER_ENABLED=0` plus cache-only staged controls. Bounded observations ran A/v11, B/v16, and C/v14 one at a time, then left all scanners inactive. Queue after the run: `max_rowid=549`, `active_status=[]`, `active_cooldowns=[]`; A added one public ticker row `545 done/200`, B/C added no Binance queue rows, and journals had no `418/429/-1003`. Cache, sentinel, API queue, and A/B/C user-streams are active; account-snapshot remains inactive. Next faster landing step is order-enabled staged testing, starting with the smallest scope and stopping on any cooldown.
- 2026-06-04 A/v11 order-enabled staged result and B/C acceleration rule: A/v11 ran order-disabled from `18:17` to `18:18 CST`, then order-enabled from `18:22` to `18:24 CST`, both with small-universe/cache-only controls and A stopped afterward. Queue stayed clean through row `565`, A ticker rows `561`/`564` were `done/200`, pending/cooldowns stayed empty, and journals had no `418/429/-1003`. To avoid retrying B/C signed baselines, `core.account_state_cache` now has a default-off Testnet-only staging switch `BINANCE_ACCOUNT_STATE_ALLOW_STALE_EMPTY_TESTNET=1`: it can let pre-entry risk checks treat a stale empty placeholder as empty account state with assumed testnet balance, but only when the stale row has zero positions. Execution confirmation still requires real fresh central account-state after order submission and cannot use this assumption. Use this only for B/C short staged windows; it is not production/live policy and not account-snapshot restoration.
- 2026-06-04 B/C order-enabled staged result and account-snapshot restoration rule: B/v16 ran order-enabled from `18:55:18` to `18:56:48 CST`, then C/v14 from `18:57:27` to `18:58:58 CST`, each one at a time with cache/sentinel/account-snapshot stopped. Queue stayed at row `572`, with `active_status=[]`, `active_cooldowns=[]`, no new Binance requests, and no journal `418/429/-1003`. To restore account-snapshot without reintroducing B/C signed baseline pressure, `ACCOUNT_SNAPSHOT_SOURCE=central` is the intended staged mode: `account_snapshot_service.py` reads `runtime/account_state_latest.json` and writes snapshot JSON/HTML without calling Binance signed balance/position endpoints. Full signed account-snapshot polling remains post-launch expansion, not first-version restoration.
- 2026-06-04 scanner staged market-data fallback rule: in staged cache-only restoration, scanner market-data universe selection must not fall back to `/fapi/v1/ticker/24hr` when `runtime/market_data_cache.json` is missing/stale/incomplete. `market_data_network_enabled()` now follows `SCANNER_KLINE_NETWORK_ENABLED` by default, so existing staged drop-ins with `SCANNER_KLINE_NETWORK_ENABLED=0` also disable A/B/C market-data ticker fallback unless `SCANNER_MARKET_DATA_NETWORK_ENABLED=1` is explicitly set. Normal default behavior remains network-enabled outside staged cache-only mode.
- 2026-06-04 current Testnet staged-online status: commit `79b68fe` is deployed to Tencent strategy releases `20260604-194506-strategy-a-79b68fe`, `20260604-194637-strategy-b-79b68fe`, and `20260604-194506-strategy-c-79b68fe`. At `20:07 CST`, API queue, market-data-cache, sentinel, A/B/C scanners, account-snapshot, system-alerts, and A/B/C user-streams were all active. `pull_live_context.py` showed positions `0`, upnl `0.0`, attention `P0=0/P1=11/P2=0`. Queue `max_rowid=588`, active requests `[]`, cooldowns `[]`; journals since `19:50 CST` had no `HTTP 418`, `HTTP 429`, `-1003`, `Traceback`, `ImportError`, or `SyntaxError`. This is staged Testnet online with cache-only/small-universe controls, central-only account-snapshot, and B/C Testnet stale-empty risk assumption; normal universe, full signed snapshot polling, and longer natural open/close evidence remain post-launch expansion.
- 2026-06-04 local start-guard rule: Binance-facing systemd units should run `binance_start_guard.py` as `ExecStartPre` before starting queue/user-stream/cache/sentinel/account-snapshot paths. The guard reads only local queue cooldowns and blocks startup with exit `75` during matching `global`, `public`, `signed`, or account-specific cooldowns; it makes no Binance request. This is a safety belt for mistaken starts and does not replace the ordered post-cooldown read-only checks before restoration. Tencent no-restart releases `20260604-144216-api-queue-060a7c4`, `20260604-144311-user-stream-060a7c4`, `20260604-144538-sentinel-060a7c4`, and `20260604-145449-account-060a7c4` installed the guard/unit updates; verification showed all Binance-facing services still inactive and queue `active_status=[]`.
- 2026-06-04 deployed skeleton path rule: Tencent/Aliyun release roots flatten repo folders, so the long-term skeleton review resolves deployed aliases such as `部署工具/x.py -> x.py`, `策略文件/x.py -> x.py`, `交易客户端/x.py -> x.py`, and `部署工具/systemd/x -> systemd/x`. A root with flattened entry scripts is treated as deployed-flat even if residual repo-named directories still exist. In flat-deploy mode, flattened root files win over stale residual repo-shaped copies, and the original repo path is only a fallback. Local repo checks remain strict; remote source/test bones not deployed to the runtime node are treated as repo-only bones already verified by local CI/smoke before deploy. Portal deploys run skeleton review before system alerts so construction-mode inference sees the fresh status.
- 2026-06-04 Tencent research deploy rule: Tencent `research` and aggregate `all` release bundles must include the report sources used by the long-term skeleton matrix, including `strategy_truth_ledger.py` and `sentinel_quality_review.py`. Missing these files causes remote skeleton false positives even when local strict skeleton is complete.
- 2026-06-04 final reset rule: do not clear old dirty runtime/signal data again. FINAL foundation zero-run is accepted only for the first-version skeleton. P0-A/P0-B first-version gates are accepted; longer public-producer expansion, normal scanner-universe observation, natural open/close exact-case rows, long-window Kline/depth coverage, mature rollout/recovery samples, automatic rollback/recovery governance, deeper sentinel attribution, visual polish, and CI enforcement are post-launch backlog, not first-version blockers.
- 2026-06-04 post-reset bootstrap rule: in construction zero-run, `crypto-account-state.service` must not run signed balance/position baselines. It should execute `account_state_service.py --bootstrap-empty`, producing stale A/B/C rows only. User-stream websocket idle may refresh timestamps only for already verified non-stale rows; a real `ACCOUNT_UPDATE` is required to turn a stale placeholder fresh. This avoids fake account freshness and avoids signed REST at zero-run bootstrap.
- 2026-06-04 guarded account-state restoration rule: after the foundation zero-run, scanner restoration must not start from stale placeholder account state. Use `account_state_service.py --once --account A|B|C` to refresh one account at a time, with queue/cooldown checks after each account, instead of running a full A/B/C signed baseline in one action. Filtered baselines merge into existing `account_state_latest.json`, preserving other stale placeholders.
- 2026-06-04 fresh parity audit rule: `replay_live_parity_audit.py` supports `--since` / `--until` and records those boundaries in JSON/Markdown/portal output. Use this for P0-B staged acceptance so old dirty rows without exact cases remain historical gaps but cannot contaminate a post-instrumentation fresh-window verdict. This does not loosen exact coverage or pass-rate thresholds.
- 2026-06-04 construction alerts rule: while `runtime/construction_mode.json` is enabled or long-term skeleton status is `blocked_by_staged_validation`, intentionally paused scanner/cache/sentinel/account-snapshot/data-maintenance units are warning-level "施工暂停", not P0 failures. This prevents false P0 attention during offline/staged-validation work; outside construction mode real inactive units still alert.
- 2026-06-04 remote skeleton validation: Tencent no-restart sync `20260604-064134-all-47e03a4` and live pull at `06:44 CST` confirm deployed skeleton `143/143`, `missing_skeleton=0`, status `blocked_by_staged_validation`, alerts `warn` with planned `施工暂停`, and attention `P0=0/P1=10/P2=2`. This clears false remote P0 skeleton/paused-service blockers, but staged validation and final zero-run remain open.
- 2026-06-04 staged foundation validation: `crypto-binance-api-queue.service` is active, A/B/C account-state baseline completed through the queue with all six signed balance/positionRisk calls returning `200`, A/B/C user-stream listenKey starts returned `200`, A/B/C user-stream services are active/running, and central account state is fresh for all three accounts (`fresh_accounts=3`, `partial_error_count=0`). No 418/429/-1003 cooldown occurred in this foundation window. Market-data-cache, sentinel, scanners, Kline/depth ingest, replay readiness, final reset, and zero-run remain open.
- 2026-06-04 staged market-data-cache validation: `crypto-market-data-cache.service` was started as the first public producer and completed two 300s cache cycles cleanly (`07:12` and `07:17 CST`). Both queued `/fapi/v1/ticker/24hr` calls returned `200`, queue cooldown stayed empty, and no 418/429/-1003 appeared. Sentinel, scanners, staged Kline/depth ingest, replay readiness, final reset, and zero-run remain open.
- 2026-06-04 staged sentinel validation: `crypto-market-mover-sentinel.service` was started after cache passed. It produced two queued public `/fapi/v1/ticker/24hr` calls with `200`, cache continued one additional clean cycle, queue cooldown stayed empty, and no 418/429/-1003 appeared. A/B/C scanners, staged Kline/depth ingest, replay readiness, final reset, and zero-run remain open.
- 2026-06-04 A/v11 scanner staged finding: A/v11 small-universe run was stopped after exposing `close_confirm_account_state_unavailable` on restored-position close handling. Queue stayed clean and no 418/429/-1003 appeared. The blocker was `ExecutionEngine` using the strict post-submit confirmation freshness window for the pre-submit close-target lookup. Local fix separates close-target freshness (`CENTRAL_ACCOUNT_STATE_TARGET_MAX_AGE_SEC`, default `60s`) from strict post-submit confirmation; deployment and A/v11 recheck remain next.
- 2026-06-04 A/v11 scanner staged validation: Tencent no-restart release `20260604-074130-all-a9d0b13` deployed the close-target fix. A/v11 small-universe scanner then ran from `07:44 CST` to about `07:51 CST` while B/C stayed inactive. Restored-position closes for `XNYUSDT` short and `VVVUSDT` long succeeded, the previous close-target blocker did not recur, queue active cooldowns stayed empty, and no new 418/429/-1003 appeared. One concurrent `market-data-cache` request timed out while still queued; this is a queue-wait warning, not an API-limit response. B/v16 and C/v14 staged scanner gates remain next.
- 2026-06-04 B/v16 scanner staged blocker: B/v16 small-universe scanner started at `07:55 CST` while A/C stayed inactive, but cache and sentinel were still active from prior gates. B/v16 public queue requests returned `200` at `07:56` and `07:58`, then hit Binance Testnet `HTTP 418/-1003` at `08:00:27`; public cooldown is active until `2026-06-04 08:34:51 CST`. B, cache, and sentinel were stopped. Next staged retry should run a single public producer only: keep cache/sentinel stopped and retry B/v16 alone after cooldown clears, then C/v14 alone.
- 2026-06-04 attention API staged validation: Aliyun `crypto-attention-api.service` was active/listening but timed out on local API requests while running single-threaded `HTTPServer`. Release `20260604-080630-shadow-c84fefd` deployed `ThreadingHTTPServer` with daemon request threads and backlog `64`, then restarted the service. `/api/health` and `/api/attention` returned `200`; synthetic `/api/attention/ack` wrote SQLite acknowledgement rows and was cleaned up. Browser public-click check remains UI-level, but API read/write path is staged-verified.
- 2026-06-04 P2-D first-version validation: while waiting for Binance public cooldown, Aliyun attention acknowledgement was verified through the public portal API path. Public `GET /api/health`, `GET /api/attention`, CORS preflight `OPTIONS /api/attention/ack`, and synthetic public `POST /api/attention/ack` all passed; the synthetic attention row was then deleted from `attention_items` and `attention_acknowledgements` and JSON was re-exported. `long_term_skeleton_review.py` now treats P2-D as first-version ready; optional public visual click-through polish and remote CI guard enforcement are post-launch backlog, not first-version blockers.
- 2026-06-04 first-version blocker narrowing: per user instruction to avoid expanding detail work before the skeleton lands, P1/P2 items that require long-horizon sampling or mature governance are now post-launch backlog in the long-term matrix. P0-B scan-flow exact parity is first-version ready after the staged A/B/C scanner rows passed strict audit; natural open/close exact rows are post-launch evidence. P0-A is also first-version ready after queue/user-stream/cache-only staged evidence plus a 30+ minute quiet read-only window with unchanged queue, no pending requests, no active cooldowns, fresh central account state, and no 418/429/-1003 journal errors. First-version hard blocker is now only FINAL zero-run waiting for reset pre-check safety. Latest matrix: `skeleton_ready=11`, `blocked_by_staged_validation=1`, `validation_blockers=1`, `post_launch_backlog=17`.
- 2026-06-04 narrowed skeleton remote sync: commit `c4ba2fe` was deployed without restart to Tencent release `20260604-092436-all-c4ba2fe` and Aliyun release `20260604-092150-shadow-c4ba2fe`. Remote skeleton refresh on both nodes reports `143/143`, `missing_skeleton=0`, `skeleton_ready=9`, `blocked_by_staged_validation=3`, `validation_blockers=5`, `post_launch_backlog=13`; Tencent alerts are warning-level construction state. A/B/C scanners, cache, and sentinel remain inactive; queue and A/B/C user-stream services remain active; public cooldown remains until `2026-06-04 10:08:56 CST`.
- 2026-06-04 queue hygiene during B cooldown: stale B/v16 public request `c1390484e6544d7eb4715b93fb1c0666` was changed from `deferred` to `failed` so it cannot auto-replay when public cooldown clears. Queue summary became `done=371`, `failed=86`; no Binance request was made by this cleanup. Public cooldown still ends `2026-06-04 08:34:51 CST`.
- 2026-06-04 B/v16 staged retry config: Tencent B/v16 scanner drop-in was tightened to `SCANNER_B_TOP_SYMBOLS=1` and `SCANNER_B_SENTINEL_LIMIT=1` for the next post-cooldown retry. This is staged-validation config, not live strategy policy. Cache and sentinel remain stopped; retry B alone after `08:34:51 CST`.
- 2026-06-04 construction-alert refresh: Tencent and Aliyun long-term skeleton artifacts were re-run with their intended Python runtimes after a stale pulled artifact briefly made construction pauses look like bad service failures. Tencent now shows skeleton `143/143`, `missing_skeleton=0`, status `blocked_by_staged_validation`, and paused construction services as warning-level `施工暂停`, not P0/bad outages.
- 2026-06-04 C/v14 staged retry config: Tencent C/v14 scanner drop-in was tightened to `SCANNER_C_TOP_SYMBOLS=1` and `SCANNER_C_SENTINEL_LIMIT=1` for the C-alone staged run after B/v16 passes. This is staged-validation config only; C/v14 remains inactive and must not start before B/v16 has a clean B-alone retry.
- 2026-06-04 B/v16 B-alone staged blocker update: after public cooldown cleared at `08:34:51 CST`, B/v16 alone still hit `HTTP 418/-1003` on the first `/fapi/v1/klines?symbol=BTCUSDT&interval=1h&limit=200` after two clean ticker `200` responses. B/v16 was stopped; the stale deferred Kline request was marked failed; queue cooldown is conservatively active until `2026-06-04 10:08:56 CST`. The next retry should deploy/use `SCANNER_KLINE_NETWORK_ENABLED=0` so staged scanners only consume local Kline cache and never call public Kline REST during scanner skeleton validation. True Kline coverage remains the separate staged Kline backfill/ingest gate.
- 2026-06-04 staged cache-only deployment: commit `d124eeb` was deployed to Tencent as no-restart release `20260604-085014-all-d124eeb`. A/B/C scanner drop-ins now set `SCANNER_KLINE_NETWORK_ENABLED=0` and `SCANNER_KLINE_CACHE_MAX_AGE_SEC=86400`; B/C remain at `SCANNER_*_TOP_SYMBOLS=1` and `SCANNER_*_SENTINEL_LIMIT=1`. A/B/C scanners, cache, and sentinel remain inactive; API queue and A/B/C user-stream services remain active; queue has no pending requests. Do not retry B/v16 until the public cooldown clears at `2026-06-04 10:08:56 CST`.
- 2026-06-04 B/C cache-only staged retry: after the `10:08:56 CST` public cooldown cleared, B/v16 ran alone with cache/sentinel/A/C stopped and produced two `/fapi/v1/ticker/24hr` `200` rows, no Kline REST, no pending backlog, and no cooldown; then C/v14 ran alone with the same cache-only Kline mode and produced two `/fapi/v1/ticker/24hr` `200` rows, no Kline REST, no pending backlog, and no cooldown. Both scanners were stopped after the short evidence window. This advances P0-A staged scanner safety, but P0-B remains partial because no fresh open-flow or scan-level exact gate rows were produced; remote parity still reports open rows `0`, scan rows `0`, close rows `9`, close exact coverage `11.11%`. Commit `c60f115` added `SCANNER_MARKET_CACHE_MAX_AGE_SEC` and was deployed without restart to Tencent as `20260604-103308-strategy-b-c60f115`, `20260604-103308-strategy-c-c60f115`, and `20260604-103432-strategy-a-c60f115`; A/B/C staged drop-ins now set `SCANNER_MARKET_CACHE_MAX_AGE_SEC=86400`, and all three scanners remain inactive.
- 2026-06-04 P0-B fresh-window audit: local `replay_live_parity_audit.py --since "2026-06-04 10:40:00"` correctly reports `no_historical_rows`, proving old dirty rows are excluded from the fresh window rather than reused or blocking the new verdict. P0-B still needs post-window exact case rows before it can be accepted.
- 2026-06-04 fresh-window audit deployment: source commit `adb39e6` was deployed without restart to Tencent research release `20260604-110127-research-adb39e6`, Tencent portal release `20260604-110237-portal-adb39e6`, and Aliyun shadow release `20260604-110305-shadow-adb39e6`. Post-deploy Tencent/Aliyun `--since "2026-06-04 10:40:00"` audits both report `no_historical_rows`; Tencent queue remains `cooldowns=[]`, `pending=[]`, and A/B/C/cache/sentinel remain inactive.
- 2026-06-04 fresh-window synthetic smoke: Tencent row id `247` was temporarily inserted into `sentinel_scans` with marker `P0B_FRESH_WINDOW_SYNTHETIC_SMOKE_20260604_1108` and an exact `score_max` case, then deleted immediately after the audit. With the row present, the fresh-window audit accepted one scan exact case at `100%`; after cleanup the same window returned `no_historical_rows`. This proves audit plumbing only and must not be counted as real P0-B market/scanner acceptance.
- 2026-06-03 offline P0-A hardening: central API queue client now has pending-backlog fail-closed protection. Before inserting a request it checks active cooldown, then pending queued/deferred/leased count by scope/account. Defaults: public max pending `3`, signed max pending `20`, other scope max pending `10`; overflow returns `queue_status=backlog` without enqueue. This is meant to prevent multiple public producers from creating deterministic queue buildup during the final staged fresh-run.
- 2026-06-03 offline P0-A scanner pacing: A/B/C scanner universes now have env-controlled limits for final fresh-run staging, with defaults unchanged. Use `SCANNER_A_TOP_SYMBOLS` / `SCANNER_A_SPIKE_SYMBOLS` / `SCANNER_A_SENTINEL_LIMIT`, `SCANNER_B_TOP_SYMBOLS` / `SCANNER_B_SENTINEL_LIMIT`, and `SCANNER_C_TOP_SYMBOLS` / `SCANNER_C_SENTINEL_LIMIT` to start from a small universe before expanding back to normal.
- 2026-06-03 offline P0-B parity: `core.strategy_gate_cases` now provides a serializable case runner for shared gates. Replay/tests can feed JSON-like cases into the real `core.strategy_gates` functions and compare expected allowed/reason outputs, giving the first reusable same-input parity harness before full historical replay.
- 2026-06-03 offline P1-C replay/fill: `core.replay_fill` now provides the first deterministic fill kernel for counterfactual opens: long/short entry, SL/TP, fee, slippage, conservative intrabar stop/take handling, and end-of-window exits. It is not yet integrated into reports or strategy-specific trailing/recovery rules.
- 2026-06-03 offline P0-B/P1-C/P1-D update: the strategy-gate case runner now covers many more existing pure gates, so serialized same-input checks can exercise account-state, sizing, confirmation, position/sector, stale-price, tail, market-microstructure, and execution cases. `counterfactual_open_skips.py` now uses `core.replay_fill` for hypothetical OPEN_SKIPPED fills and stores the fill payload. `strategy_evolution_gate.py` now emits per-decision `decision_packet` fields, and `rollback_watch_review.py` renders them for P0/P1 rollback-watch items. These are offline/reporting changes only; automatic upgrade/rollback remains disabled.
- 2026-06-03 offline P1-C trailing primitive: `core.replay_fill` now also supports optional percent trailing-stop activation/exit for long and short fills under the same conservative intrabar policy. This is a generic fill primitive; A/v11 ATR pullback and recovery-position replay are still not fully wired.
- 2026-06-03 offline P1-C ATR trailing integration: `core.replay_fill` now supports ATR-based trailing activation and pullback (`atr`, `trailing_activation_atr`, `trailing_stop_atr`) for long and short fills, and `counterfactual_open_skips.py` now applies the approved A/v11 15m/30m ATR trailing parameters when skipped A/v11 rows include positive ATR. Non-A/v11 rows and A/v11 rows without usable ATR remain on the fixed percent barrier model. A/v11 rollout-review full historical fill comparison and recovery-position strategy-specific exits remain open.
- 2026-06-03 offline P1-A/P1-D update: `a_v11_rollout_review.py` now emits and renders a dedicated `decision_packet` for the A/v11 trailing-pullback rollout, including selected live parameter, approval rationale, risk evidence, maturity, rollback path, and report-only automation status. It still does not auto-rollback or change live parameters.
- 2026-06-03 offline P1-A/P1-C/P1-D update: A/v11 rollout review now also classifies realized closes by exit model (`atr_trailing_stop`, `take_profit`, `max_loss_guard`, `profit_retrace_guard`, `range_exit`, `exchange_auto_close`, `other`) and adds 72h cost sensitivity at `0.10%`, `0.15%`, and `0.25%`. The decision packet carries these fields so rollback-watch can distinguish trailing giveback, hard loss guard, exchange auto-close, and fee/slippage sensitivity. This is report-only; no live parameter, rollback, order, or service behavior changed.
- 2026-06-03 offline P1-C update: A/v11 rollout review now has a first local-cache replay/fill comparison. It pairs A/v11 `OPEN` with `CLOSE/FORCED_CLOSE`, reads only local synced `runtime/kline_cache` / `server_logs_tencent/runtime/kline_cache`, and replays the approved ATR trailing rule through `core.replay_fill`. Missing Kline cache is reported as `missing_kline_cache` / `missing_bars`; no Binance API call is made. Long-history Kline coverage and recovery-specific exits remain open.
- 2026-06-03 offline P1-B/P1-D update: `b_v16_rollout_review.py` now mirrors the A/v11 rollout-review pattern for the approved B/v16 ATR stop bands + score cap 85 full-live rollout. It emits 24h/72h/168h report-only windows, cost-adjusted PnL, failure/forced-close pressure, a decision packet, and portal visibility. It does not auto-rollback or change B/v16 parameters.
- 2026-06-04 offline P1-B/P1-D update: B/v16 rollout review now adds realized profit/loss, profit factor, exit-model attribution, open-failure reason attribution, and `0.10%/0.15%/0.25%/0.35%` cost sensitivity. The portal B/v16 card/detail table now show PF, the main exit model, the main open-fail reason, and 0.25% cost-stress PnL. This is report-only and uses local SQLite/report files; no Binance API call, service start, live parameter change, or rollback action occurred.
- 2026-06-04 offline P1-B/P1-C/P1-D update: B/v16 rollout review now has a local replay/fill comparison. It pairs B/v16 `OPEN` with `CLOSE/FORCED_CLOSE`, reads local/mirrored `research_store/klines` then local `runtime/kline_cache`, optionally consumes local/mirrored depth snapshots, and replays B/v16 SL/TP, `1.0 ATR` activation/pullback, hard-bottom, and profit-retrace exits through `core.replay_fill`. The report and portal show paired/completed counts, actual-vs-replay PnL delta, replay exit reasons, depth entry-fill counts, and depth slippage. No Binance API call, service start, live parameter change, or rollback action occurred.
- 2026-06-03 offline P1-C/P2-B update: recovery-position review now includes report-only snapshot path metrics. `strategy_truth_ledger.py` enriches recovery rows with first-seen timestamp, sample count, MFE/MAE on margin, drawdown from MFE, and price-direction MFE/MAE; the truth ledger Markdown and portal recovery table show MFE/MAE/MFE回撤. This does not auto-close recovery positions or alter exits.
- 2026-06-03 offline P1-C/P2-B update: recovery-position review now also reads existing `SIGNAL` / `SIGNAL_ONLY` / `OPEN_SKIPPED` rows as same-strategy signal evidence. Recovery rows show same-side re-open support, opposite-side open-like evidence, latest compact signal facts, and `opposite_signal_manual_review` when the same strategy now produces an opposite open-like signal. This is report-only evidence; it does not auto-close recovery positions, change scanner behavior, or deploy services.
- 2026-06-04 offline P1-C/P2-B update: recovery-position review now has a report-only strategy-specific exit evidence layer. `strategy_truth_ledger.py` attaches per-position `strategy_exit_evidence` / `strategy_exit_action` using A/B/C MFE and MFE-drawdown profiles, so recovery rows can distinguish opposite-signal manual review, MFE drawdown manual review, trailing watch, same-side hold bias, and keep-monitoring. The portal recovery table shows the new action and summary counts. This does not auto-close recovery positions or alter scanner/order/exit behavior.
- 2026-06-04 offline P1-C/P2-B update: recovery-position review now has a first local-Kline bar replay evidence layer. `strategy_truth_ledger.py` attaches `recovery_replay_evidence` by reading only local or mirrored `runtime/kline_cache`, replaying from first-seen snapshot to latest snapshot through `core.replay_fill`, and marking `bar_replay_exit_manual_review`, `bar_replay_hold_bias`, or `replay_data_gap`. The truth-ledger Markdown and portal recovery table show replay action/status/exit reason. This does not fetch Binance Klines, close recovery positions, or alter scanner/order/exit behavior.
- 2026-06-04 offline P1-C update: OPEN_SKIPPED counterfactual review now surfaces replay/fill summaries instead of burying fill details in each row payload. `counterfactual_open_skips.py` aggregates exit-model counts, exit-reason counts, gross PnL, fee, slippage, net PnL, average bars held, and per-exit-model rows; the Markdown report and portal counterfactual section show these fields. This is report-only; no Binance Kline fetch, scanner/order/exit behavior, or service state changed.
- 2026-06-04 offline P1-C/P1-D update: `core.replay_fill` now has report-only partial-fill caps (`max_fill_quantity`, `max_fill_notional_usdt`, `allow_partial_fill`). `counterfactual_open_skips.py` can aggregate requested/filled/unfilled quantity, partial-fill count, and average fill ratio, and the portal counterfactual section shows the liquidity assumption. This is still a simple cap model, not order-book depth or queue-priority simulation; no live order behavior changed.
- 2026-06-04 offline P1-C update: `core.replay_fill` now has a first depth-aware entry fill primitive. Optional `entry_order_book` levels let long replay consume asks and short replay consume bids, producing average entry price, depth slippage, levels used, and partial-fill facts; explicit empty books fail closed. This is a kernel primitive only and is not yet wired to live depth snapshots or reports with real depth data.
- 2026-06-04 offline P1-C update: OPEN_SKIPPED counterfactual replay now has a local depth-cache input path. `core.replay_depth_cache` reads only local/mirrored `runtime/depth_cache` snapshots near the simulated entry time; `counterfactual_open_skips.py` passes matching bids/asks into `core.replay_fill` and reports entry fill source, order-book fill count, depth snapshot age, and depth slippage. The portal shows those metrics. This still does not fetch or persist depth snapshots and has no live service/order impact.
- 2026-06-04 offline P1-C/P1-D update: OPEN_SKIPPED counterfactual replay now has a report-only fill stress grid. `counterfactual_open_skips.py` derives `base/conservative/stress` scenarios from completed `replay_fill` payloads, capping effective fill ratio and adding extra entry impact to show net PnL sensitivity; the Markdown report and portal show the grid. This does not rerun Klines/depth, call Binance, or change live behavior.
- 2026-06-04 offline P1-E update: research-store query now has explicit 30-day Kline acceptance. `research_store_query.py` computes per-interval Kline coverage days and emits `kline_acceptance` for key intervals `15m/30m/1h`; `portal_dashboard.py` shows whether long-window Kline coverage is `ok`, `coverage_gap`, or `no_klines`. This prevents replay/fill and evolution work from mistaking short local cache partitions for long-history evidence. It does not fetch historical Klines or start services.
- 2026-06-04 offline P1-E update: long-history Kline backfill now has a queue-safe planner. `research_kline_backfill.py` defaults to plan-only, can seed symbols from `events` / `sentinel_scans` when Kline partitions are empty, can optionally submit idempotent public Kline intents to `BinanceApiQueue`, and can ingest DONE queue results into `research_store/klines` / `features`. Aliyun refresh/shadow, release/deploy lists, reverse sync, live-context pull, and portal visibility are wired. No real submit, queue executor run, or Binance request happened in this offline pass.
- 2026-06-04 offline P1-C/P1-E update: current depth snapshot sampling now has a queue-safe planner/warehouse path. `research_depth_backfill.py` defaults to plan-only, can seed symbols from `events` / `sentinel_scans`, can optionally submit idempotent public `/fapi/v1/depth` intents to `BinanceApiQueue`, and can ingest DONE queue responses into `research_store/depth_snapshots` plus optional `runtime/depth_cache`. `research_store_query.py`, the portal, Aliyun refresh/shadow, release/deploy, reverse sync, and live-context pull are wired. No real submit, queue executor run, or Binance request happened in this offline pass.
- 2026-06-04 offline P1-C update: replay/fill now has report-only queue-ahead and market-impact assumptions. `core.replay_fill` can consume visible depth ahead of our simulated order via `entry_order_book_queue_ahead_quantity`, apply side-aware `entry_market_impact_bps`, and report `order_book_queue_ahead_quantity` / `market_impact_usdt`. `counterfactual_open_skips.py` and the portal expose these fields. This is not a live queue-priority simulator and does not change order behavior.
- 2026-06-04 offline P1-C/P1-E update: rollout and recovery bar replay now prefer accumulated local `research_store/klines` before falling back to short `runtime/kline_cache`. `core.replay_kline_source` loads JSONL/parquet Kline partitions from local or mirrored research-store, filters by symbol/timeframe/window, dedupes bars, and never calls Binance. `a_v11_rollout_review.py` and `strategy_truth_ledger.py` use it for A/v11 rollout replay and recovery-position replay, so repeated offline Kline exports/backfills can expand replay coverage without changing live behavior.
- 2026-06-04 offline P1-C update: rollout and recovery bar replay now also consume optional local/mirrored `runtime/depth_cache` snapshots for entry fill through `core.replay_depth_cache`. A/v11 rollout replay, recovery replay, Markdown reports, and portal summaries expose order-book entry-fill count, fill source, depth snapshot age/source, and depth slippage when cache data exists. Missing depth keeps previous synthetic entry behavior. This is report-only and does not fetch depth or change live orders.
- 2026-06-04 offline P1-C/P1-E update: replay depth lookup now also consumes local/mirrored `research_store/depth_snapshots` partitions directly. Staged depth ingest output can feed OPEN_SKIPPED counterfactuals, A/v11 rollout replay, and recovery-position replay without first being rewritten into `runtime/depth_cache`. The lookup remains read-only, local/mirrored only, and report-only; no Binance request, service start, live order, recovery-exit, upgrade, or rollback behavior changed.
- 2026-06-04 offline P1-C/P1-E update: replay/fill readiness now has a unified report-only acceptance layer. `replay_readiness_review.py` reads only existing local/mirrored research-store, A/v11 rollout, B/v16 rollout, and truth-ledger JSON artifacts, then emits `runtime/replay_readiness_latest.json` and `reports/replay_readiness_latest.md` with statuses `ready_for_operator_review`, `data_gap`, `waiting_for_samples`, or `report_gap`. The portal, Aliyun refresh/shadow scripts, release/deploy lists, reverse sync, and live-context pull include this report. It does not fetch Klines/depth, start services, recompute trades, approve rollout, or change live behavior.
- 2026-06-04 offline P1-E update: research-store retention now has a safe planner. `research_store_retention.py` scans partitioned `research_store` tables, classifies `hot/warm/archive_candidate/invalid_date`, and writes JSON/Markdown reports plus portal visibility. Default mode is plan-only; explicit `--apply` moves old partitions to ignored `research_store_archive/` and does not delete data. Aliyun refresh/shadow, release/deploy, reverse sync, and live-context pull are wired. This does not compact Parquet files, submit queue jobs, fetch Binance data, or change live behavior.
- 2026-06-04 offline P1-E update: research-store compaction now has a safe planner. `research_store_compaction.py` scans JSONL/Parquet partitions, reports size/rows/row-groups and `compact_candidate` reasons, and writes JSON/Markdown reports plus portal visibility. Default mode is plan-only; explicit `--apply` backs up the full partition to ignored `research_store_compaction_backup/` before atomically rewriting the data file. It does not merge dates, delete data, fetch Binance data, or change live behavior.
- 2026-06-03 offline P1-D update: rollback-watch now includes close-failure attribution. Post-approval windows split raw/resolved/unresolved close failures and surface compact reasons (`position_still_open_after_close`, `confirmation_state_unavailable`, Binance codes, timeouts) through rollback-watch JSON/Markdown and the portal card. This is report-only; automatic rollback remains disabled.
- 2026-06-03 offline P1-D update: `strategy_evolution_gate.py` now adds post-approval paper cost/slippage sensitivity at `0.10%`, `0.15%`, `0.25%`, and `0.35%` using the existing 400 USDT notional assumption. Mature windows can be marked bad when the conservative `0.25%` row crosses the rollback-review loss threshold, and decision packets carry the sensitivity grid plus conservative-cost risk strings. This is report-only; no automatic rollback or live behavior changed.
- 2026-06-04 offline P1-D update: `strategy_evolution_gate.py` now uses report-only gate profiles by strategy/change family. A/v11 replacement, A/v11 trailing, B/v16 exit-risk, C/v14 sample expansion, confirmation-policy, and default profiles have distinct P0/P1 sample thresholds and 24h/72h/168h closed-sample requirements. Decisions, window quality, decision packets, and the Phase 8 audit now expose `gate_profile`; full-live review also carries `regime_robustness` (`missing` / `single_regime` / `weak` / `ok`). This strengthens operator review only; automatic upgrade/rollback remains disabled.
- 2026-06-04 offline P1-D update: `strategy_evolution_gate.py` now also assigns report-only post-approval minimum profit-factor thresholds by gate profile. Full-live windows accumulate realized profit/loss from closes, compute `profit_factor`, mark mature low-PF windows bad, and carry PF evidence into decision packets. This is still operator evidence only; no automatic rollback, service action, or live behavior changed.
- 2026-06-04 offline P1-D update: `strategy_evolution_gate.py` now adds report-only post-approval fill-liquidity sensitivity at fill ratios `1.00/0.75/0.50/0.25`. Window quality and decision packets show filled/unfilled notional, scaled after-cost PnL, and a conservative `0.50` partial-fill row. This is still assumption-based paper evidence, not order-book depth/queue-priority simulation, and it does not change rollback automation.
- 2026-06-04 offline P1-D update: rollback governance readiness is now explicit. `rollback_watch_review.py` checks each P0/P1 rollback-watch item for decision-packet completeness, rollback path, risk items, evidence maturity, automation disabled status, close-failure pressure, and current replay readiness. It reports `operator_ready`, readiness status counts, and governance gaps in JSON/Markdown and the portal card. This only prepares human continue/narrow/rollback review; automatic upgrade/rollback remains disabled.
- 2026-06-04 offline P1-D update: rollback execution dry-run planning is now explicit. `rollback_execution_plan.py` turns rollback-watch items into report-only execution preparation packets with dry-run release-manager command hints, disabled apply commands, freeze/evidence/rollback-path/post-check/abort checklist, and portal visibility. `apply_enabled=false` remains hard-coded; no rollback, deploy, service restart, or live behavior change occurs.
- 2026-06-04 offline P1-D update: rollback automation is now explicitly guarded. `rollback_automation_guard.py` reads rollback-watch, rollback execution plan, replay readiness, and optional `research_memory/approvals/rollback_automation_policy.json`, then writes JSON/Markdown plus portal visibility. It keeps `automatic_rollback_allowed=false` and `apply_enabled=false`, blocks on missing policy, replay/data gaps, non-actionable dry-run plans, placeholder release ids, or non-actionable candidates, and can only reach `preconditions_met_report_only`. It never runs rollback, deploy, service restart, order, config mutation, or Binance calls.
- 2026-06-03 offline P0-B exact parity audit: `replay_live_parity_audit.py` now measures true same-input serialized `strategy_gate_case(s)` through the shared pure gate runner and reports exact coverage/pass/mismatch/errors separately from observed replay-gate classification. The portal, Aliyun refresh/shadow scripts, reverse sync, and live-context pull include the new `replay_live_parity_latest` artifacts. This is report-only; no service restart or strategy behavior change occurred.
- 2026-06-03 offline P0-B scanner exact cases: A/v11, B/v16, and C/v14 now persist exact `strategy_gate_case` payloads on important `OPEN_SKIPPED` paths: central account-state availability/read failure, duplicate same-symbol position, execution quantity/preflight result, plus A/v11 market/tradability/margin, B/v16 active-position limit/small-live stage/confirmation/threshold, and C/v14 stale-price/ATR market-data/confirmation/tail gates. The strict audit now also reads `sentinel_scans` scan-level gate rows separately from open-flow rows, and scanner pre-filter/cooldown/score/risk rows now carry selected exact cases for A/v11, B/v16, and C/v14. This improves parity-audit coverage after the next staged fresh-run; deeper orchestration coverage is still open.
- 2026-06-03 offline P0-B orchestration parity: B/v16 threshold rejects now persist the preceding confirmation gate plus the failing threshold gate as a `strategy_gate_cases` chain, and C/v14 tail-guard rejects now persist the preceding confirmation gate plus the failing tail gate. The strict audit already counts multiple exact cases per row, so future fresh-run data can distinguish orchestration-chain parity from isolated final-gate parity. This is still offline/report-only; broader A/v11 replacement and B/C risk/execution orchestration remain open.
- 2026-06-03 offline P0-B A/v11 replacement chain: A/v11 pool-full replacement rows now persist replayable orchestration cases for strong-signal eligibility and pool-capacity decisions, and forced replacement skips append a release-result gate when no weak position can be freed. This closes the first A/v11 replacement orchestration audit gap without changing replacement thresholds, close/open behavior, or service state.
- 2026-06-03 offline P0-B entry-risk parity: A/B/C shared entry-risk rejects now persist exact `entry_risk` cases for total-position cap, side-position cap, and available-balance reserve protection. This strengthens strict replay/live parity for risk-gate skips without changing thresholds or opening behavior.
- 2026-06-03 offline P0-B execution-failure parity: A/B/C non-preflight `OPEN_FAILED` rows now persist exact `execution_result` cases, so strict replay/live parity can audit real execution failures separately from deterministic preflight skips. This is event instrumentation only; execution behavior is unchanged.
- 2026-06-03 offline P0-B close-flow parity: `replay_live_parity_audit.py` now reports close-flow rows separately from open-flow and scan-level rows, including normal close, forced close, A/v11 replacement release, and A/v11 post-open sizing cleanup events. A/B/C close and forced-close failure paths now persist exact `execution_result` cases; B/C hard-stop exceptions also become visible `FORCED_CLOSE_FAILED` events. This is observability/parity instrumentation only; no close/open behavior or service state changed.
- 2026-06-03 offline P0-B A/v11 replacement candidate parity: A/v11 replacement release selection now has replayable exact cases for every evaluated weak-position candidate when detail collection is requested, and `EVICT_CLOSE` / `EVICT_FAILED` release flows now carry candidate, release-result, and close `execution_result` cases as one chain. This improves future strict parity coverage for replacement orchestration without changing release thresholds, candidate ranking, close/open behavior, or service state.
- 2026-06-03 offline P0-B open-success parity: A/v11, B/v16, and C/v14 successful `OPEN` rows now persist exact `execution_result` cases, so strict same-input parity can audit execution success/confirmation proof instead of counting successful opens as missing-case coverage gaps. This is event instrumentation only; no order path, confirmation rule, threshold, or service state changed.
- 2026-06-03 offline P0-B B/C open-success chain parity: B/v16 successful `OPEN` rows now carry `[confirmation, entry_threshold, execution_result]` exact cases, and C/v14 successful `OPEN` rows carry `[confirmation, tail_guard, execution_result]`. This upgrades open-success parity from isolated execution proof to first orchestration-chain proof for B/C, without changing live decisions, orders, or service state.
- 2026-06-03 offline P0-B A/v11 open-success chain parity: A/v11 successful `OPEN` rows now carry a replayable chain for normal Hanmuxia opens: entry threshold, optional resonance requirement, pool capacity, market microstructure, tradability, duplicate-position, sizing, account-state, entry-risk, and execution success. VPB opens keep VPB's own threshold semantics and only carry shared pool/safety/risk/sizing/execution context. Replacement success chains append release cases into the final successful-open event. The successful-open sizing-confirm path also now defines the min-notional adjustment and margin bounds before use.
- 2026-06-03 offline P0-B B/C runtime open-success parity: B/v16 successful `OPEN` rows now include duplicate-position, central account-state, entry-risk, and positive-quantity pass cases between the confirmation/threshold chain and execution success. C/v14 successful `OPEN` rows now include duplicate-position, central account-state, entry-risk, stale-price, market-microstructure, and positive-quantity pass cases between confirmation/tail and execution success. This is event instrumentation only; no threshold, risk, order, confirmation, or service state changed.
- 2026-06-03 offline P0-B A/v11 post-open cleanup parity: A/v11 `OPEN_SIZING_MISMATCH_CLOSED` and `OPEN_SIZING_MISMATCH_FAILED` rows now carry a two-case exact chain: confirmed `a_v11_margin_sizing` failure plus close `execution_result`. This makes post-open sizing cleanup visible to strict close-flow parity even when the cleanup close succeeds; no sizing target, tolerance, close behavior, or service state changed.
- 2026-06-03 offline P0-B acceptance conclusions: `replay_live_parity_audit.py` now emits an explicit same-input acceptance layer for open-flow, scan-flow, and close-flow. It reports accepted/partial/blocked/missing/no-historical status, readiness score, blocking flows, and whether a fresh-run is still required. This keeps old missing-case rows as coverage gaps instead of overstating parity.
- 2026-06-03 10:15 construction blocker update: latest staged fresh-run failed again on public REST. A/v11 Kline traffic triggered Binance Testnet public `HTTP 418/-1003`, queue cooldown was active until `2026-06-03T10:31:59.888+08:00`, and public producers were stopped again at `10:06 CST`. After cooldown cleared, `crypto-market-data-cache.service` alone ran clean for more than five minutes with new `200` ticker responses and no cooldown. The next construction pacing change is to load tracked 300s systemd intervals for market-data-cache and sentinel; do not start scanners until cache + sentinel prove clean under the slower cadence.
- 2026-06-03 fresh-run correction: P0-A central chain first fresh-run succeeded (`fresh_accounts=3`, `partial_error_count=0`, A/B/C user-stream idle/ACCOUNT_UPDATE handling ok), but the later scanner/market-data fresh-run attempt exposed two blockers. First, `crypto-account-state.service` periodic polling could still trigger signed `A/v11 GET /fapi/v2/balance`; this was fixed by making account-state a manual `Type=oneshot` baseline and removing it from user-stream dependencies. Second, after market-data-cache and sentinel were clean, starting scanners caused public `HTTP 418/-1003` from Kline pressure. A/v11 scanner, B/v16 scanner, sentinel, and market-data-cache were stopped after each blocker, stale public pending queue rows were failed/no-retry, `core.binance_api_queue_client` now blocks new requests before enqueue when queue cooldown is active, cancels unleased timed-out requests to failed, and the construction API queue executor now runs at `--interval 60` instead of `15`. Public producer queue waits are at least `180s`. User-stream units no longer start account-state, account-state is a manual baseline unit, all active construction units run as `ubuntu`, and central account-state remains fresh through websocket idle touches.
- Next optimization target as of 2026-05-31: upgrade from threshold tuning to a strategy-evolution system. The durable execution plan is in `记忆文档/FUTURE_EXECUTION_PLAN.md` under "2026-05-31 下一阶段目标".
- Current canonical long-term execution queue is in `记忆文档/FUTURE_EXECUTION_PLAN.md` under "2026-06-02 当前长期任务 P 级执行队列（canonical）". This P-level is engineering priority, not portal attention priority. Execute in order: P0-A Binance API root fix, P0-B replay/live same path, P1 rollback-quality/replay/fill/gate/history work, then P2 sentinel/recovery/governance/PoC.

## Strategy Stack

- `A/v11`: original high-frequency/scanner strategy, now has full-position replacement/release logic and same-symbol no-stacking protection. As of 2026-05-27, replacement-quality remains guarded small-live only: strong signal >=112, score gap >=25, profitable positions >=2% are protected, no total-position expansion. As of 2026-05-29, user approved both trailing-pullback P0 candidates for full rollout; live uses 15m pullback `1.0 ATR` and 30m pullback `0.8 ATR`.
- `B/v16`: CVD/OFI order-flow strategy. Small-stage guard was temporarily removed per user instruction; latest server event checks show active scanning and new opens, not a stall. As of 2026-05-29, close/forced-close confirms the exchange position disappears before local state can be removed; the legacy Testnet `-4061` close-confirm alert cleared by 2026-05-30, but future close failures must remain visible if they recur. As of 2026-05-31, user approved `EXP-20260527-v16-atr-stop-bands` and `EXP-20260527-v16-overheat-cap-85` for full-live rollout: live uses score cap 85 and ATR stop bands 2.5/2.0/1.5 by volatility.
- `C/v14`: four-factor scoring strategy with same-symbol no-stacking protection. Signal reporting distinguishes raw analysis candidates from real 1h entry candidates. As of 2026-05-31, C/v14 is in sample-expansion mode to fix too-few opens: 1h threshold 50, 15m confirm threshold 25, short penalty 10, max 3 positions per timeframe/sector, and high-score 1h candidates can pass weak/no 15m confirmation. Hard risk gates remain unchanged: same-symbol no-stacking, account/side caps, exchange-rule preflight, 100 USDT normal risk size, and close confirmation.
- Current entry-threshold decision: do not keep globally loosening entries after the 2026-05-31 B/C changes. A/v11 stays stable, B/v16 observes the approved full-live candidates, and C/v14 stays in its current controlled sample-expansion window until enough post-change evidence accumulates. More samples should come from controlled expansion plus rollback watches, not from unlimited threshold cuts.
- Sentinel: market mover detector feeds unusual movers into strategies; strategy scans now record which layer opened, filtered, rejected, or had no signal.
- Shared execution layer: A/B/C use `core/binance_order_rules.py` and `core/execution_engine.py` for Binance exchange-rule preflight, MARKET_LOT_SIZE/minNotional/maxQty rounding, TradFi-Perps blocking, percent-price checks, `-1007` status-unknown position confirmation, open-position quantity confirmation, and close confirmation. A close is not considered successful until the matching exchange position disappears; remaining positions after retry must be logged as `CLOSE_FAILED` or `FORCED_CLOSE_FAILED` and surfaced by system alerts. Deterministic preflight rejections should be logged as `OPEN_SKIPPED`, not `OPEN_FAILED`.
- 2026-06-01 update: shared execution now treats Binance USDM min-notional as at least `5.05` USDT even if exchangeInfo is stale/zero, and residual exchange `-4164` min-notional rejects are reclassified as execution preflight rejects. Future `-4164` should appear as `OPEN_SKIPPED/execution_preflight`, not strategy-quality `OPEN_FAILED`.

## Done

- Real-time account snapshots are persisted into SQLite and surfaced on the command-center page.
- Command-center page now prioritizes account PnL, strategy PnL, service health, sentinel health, signal quality, OPEN_SKIPPED counterfactuals, strategy evolution, and persistent attention items.
- All strategies block opening a symbol when the same symbol is already held in the account or local state.
- Strategy evolution gate exists and combines candidates, shadow experiments, counterfactuals, reviews, and account risk into P0/P1/P2/reject decisions.
- Gate safety rule: no P0/P1 promotion when evidence is only confirmation-policy shadow output without real or paper PnL.
- Polymarket was decommissioned per user request on 2026-05-27: repo code was removed, Tencent `/opt/polymarket-lab` was deleted, and `polymarket-monitor.service` was stopped/disabled/removed. It is no longer a command-center module or live service.
- Operational alerts watch systemd services, timers, disk pressure, event freshness, account snapshots, strategy evolution, and the persistent attention ledger.
- P-level attention state is SQLite-backed in `runtime/event_store.sqlite3` via `attention_items` and `attention_acknowledgements`; `research_memory/attention/open_items.json` is an exported portal cache, not the canonical acknowledgement store.
- Log/data growth has guardrails: generated logs, reports, SQLite DBs, mirrors, and bulky backtest outputs are excluded from Git.
- Cross-machine handoff is now explicit: `AGENTS.md`, `README.md`, `PROJECT_STATE.md`, and `记忆文档/MEMORY.md` tell a new model what to read and how to avoid stale conclusions.
- Local dependencies are declared in `requirements.txt`; Python 3.10 compatibility for TOML parsing is handled through `tomli`.
- The required scanner helper package `cloud/analyzer/auxiliary.py` is in Git, so fresh clones no longer miss the live indicator dependency.
- `部署工具/pull_live_context.py` provides one-command compact live-state sync from Tencent into ignored local files before making current PnL/position/service conclusions.
- `部署工具/pull_live_context.py` now uses OpenSSH compressed grouped pulls with hard deadlines, so live sync should fail loudly instead of hanging silently. It pulls portal, alerts, account snapshot, strategy evolution, counterfactuals, daily market review latest MD/HTML, market snapshot, and attention ledger.
- `部署工具/release_manager.py` provides component-based dry-run deploys, remote backups, release manifests, service restarts, release listing, and rollback.
- `部署工具/release_manager.py` treats Tencent portal deploys as alert/report-file deploys only; it must not restart the intentionally inactive Tencent `crypto-portal-refresh.service`. Aliyun `shadow` deploys include truth ledger, sentinel quality review, counterfactual report, attention API, analysis refresh, reverse-sync scripts, and the SQLite snapshot sync path.
- `部署工具/shadow_sync_from_tencent.py` syncs Tencent SQLite through the SQLite `backup()` API and verifies the copied DB with `pragma quick_check` before Aliyun analysis reads it.
- `部署工具/shadow_sync_from_tencent.py` now writes the mirror to project-level `server_logs_tencent`, syncs compact runtime/report context, builds a slim verified SQLite snapshot for Aliyun analysis, and keeps legacy JSONL pulls optional.
- 2026-05-31 update: Tencent→Aliyun shadow sync now supports a bounded SQLite mirror: recent `events`, newest `sentinel_scans`, newest `account_snapshots`, and attention tables, followed by `pragma quick_check` and freshness validation. The every-2-hour Aliyun command-center refresh uses a 1-day bounded mirror to avoid stalls; the daily shadow-review path tries 3 days first, then falls back to the same 1-day mirror if the deeper sync times out. Aliyun→Tencent report sync sends key runtime/report outputs first and keeps bulky market-review files optional.
- 2026-05-31 update: research-store export now uses manifest watermarks to skip unchanged partitions, and Tencent data maintenance retention also prunes old `sentinel_scans` rows so the highest-volume scan table does not grow without bound.
- 2026-06-01 update: the research warehouse now has a first `klines/features` layer sourced from Tencent `runtime/kline_cache`; it exports recent cached Klines and simple derived features into partitioned research_store tables for DuckDB/replay work. As of 2026-06-04, `research_kline_features.py` merges existing Kline partitions with new synced cache rows, dedupes by `(symbol, interval, open_time_ms)`, and rebuilds features from the merged series, so repeated offline refreshes can accumulate longer Kline history without growing SQLite or calling Binance. This is still not a true historical backfill source.
- 2026-06-01 update: the replay/research bridge now has a first feature-alignment dataset. `replay_feature_dataset.py` aligns A/v11, B/v16, and C/v14 `OPEN` / `OPEN_SKIPPED` / `OPEN_FAILED` / `SIGNAL` events to the nearest prior same-symbol feature row and surfaces coverage in the command center. This is still an analysis dataset, not the final shared live/replay execution engine.
- 2026-06-01 update: the replay/live bridge now also has a gate audit layer. `replay_gate_audit.py` classifies recent live `SIGNAL` / `OPEN` / `OPEN_SKIPPED` / `OPEN_FAILED` rows through `core.replay` and surfaces gate coverage, unknown gate count, and per-strategy top gates in the command center. This is the next N2 checkpoint before extracting A/B/C pure strategy gate functions.
- 2026-06-01 update: A/v11 legacy `OPEN_SKIPPED` rows without `stage/layer` are now inferred from reason text in `core.replay`, bringing Tencent live replay gate audit to `100%` coverage / `unknown_gate=0`. This is still an audit bridge, not full replay/live shared gate execution.
- 2026-06-01 update: strategy evolution post-approval close-failure windows now cross-check the latest fresh account snapshot. A raw `CLOSE_FAILED` only triggers rollback-required P0 if the matching strategy/symbol/side position still exists; already-disappeared positions are counted as `resolved_close_failed`.
- 2026-06-01 update: system alerts now attribute recent Binance API 418/429/-1003 pressure by systemd service over a bounded 30-minute journal window and the command center surfaces the count/source in the automatic-alert card. This helps distinguish account-snapshot cooldown from scanner-side API pressure.
- 2026-06-01 update: all three Binance clients now use `core/binance_api_guard.py`, a file-backed cross-process signed REST guard. A/v11, B/v16, C/v14, and the account snapshot service share a minimum request interval and a persisted ban/cooldown window from `418/429/-1003` responses. `system_alerts.py` surfaces the guard state and top signed REST paths.
- 2026-06-01 update: the same API guard now also enforces `BINANCE_API_GUARD_MAX_REQUESTS_PER_MIN` (default `120/min`) across all live processes and `BINANCE_API_GUARD_MAX_ACCOUNT_REQUESTS_PER_MIN` (default `80/min`) per account/strategy label. It reports `rolling_count_60s/top_paths_60s` to system alerts.
- 2026-06-01 update: all three Binance clients now use `BINANCE_ACCOUNT_CACHE_TTL_SEC` for balance/position cache TTL, default `5s`. Order paths still invalidate the cache after submit, so this reduces repeated scan-cycle account reads without weakening open/close confirmation.
- 2026-06-02 update: A/v11, B/v16, and C/v14 open-risk gates now reuse one exchange-position snapshot and one balance snapshot per gate decision through `core/exchange_state.py`. This cuts duplicate signed REST reads in the entry path without changing thresholds, sizing, leverage, stops, or risk limits. The remaining API-pressure target is still user-data-stream or a centralized account-state service plus confirmation-loop snapshot reuse.
- 2026-06-02 update: A/v11 now uses `/fapi/v2/balance` for balance reads, matching B/v16/C/v14, and its close submit path skips a duplicate position lookup when the shared execution layer already supplied close quantity/order side. `account_snapshot_service.py` also refuses to overwrite latest snapshots when all failures are only missing local `BINANCE_*` env vars from a naked manual run.
- 2026-06-02 update: `core/binance_api_guard.py` now has a first trade-priority reserve. Normal signed reads pause before the hard rolling budget is exhausted, preserving `20/min` default headroom for trade-control paths such as order, cancel, leverage, and margin type. System alerts expose priority counts, normal limit, trade reserve, and the latest real error fields separately from the latest request path.
- 2026-06-02 update: after a fresh Binance `418/-1003` IP ban, signed REST guard defaults were tightened to `90/min` total, `45/min` per account, `650ms` minimum interval, and `10min` ban grace while keeping `20/min` trade-priority reserve. Account snapshots now read shared guard cooldown before sending balance/position requests, and A/B/C clients preserve longer error bodies so `banned until` timestamps remain parseable. A first public REST guard (`120/min` default) now also coordinates scanner Kline/ticker/depth/funding/aggTrades, sentinel ticker, market-data-cache ticker, and order-rule public price checks through the same cooldown state. For 418/429 without explicit `banned until`, the fallback cooldown is now `30min`. This is a mitigation, not the final user-data-stream/central account-state migration.
- 2026-06-02 update: the remaining known Binance public REST callers are also routed through the shared API guard: A/v11 client `exchangeInfo`, VPB funding lookup, counterfactual Kline fetch, and daily review/market-review Binance fetchers. This closes another cooperative pacing gap, but user-data-stream/central account-state remains the durable fix for signed account pressure.
- 2026-06-02 update: repeated Binance 418/429/-1003 responses without an explicit `banned until` timestamp now use exponential fallback cooldown (`30m -> 60m -> 120m`, capped at `4h` by default). This is because Binance/Testnet can keep returning IP-level `429` after very low local request counts, so short fixed probes can extend the ban.
- 2026-06-02 update: B/v16 and C/v14 client `exchangeInfo` now also uses the shared public REST guard and 60-second public cache instead of signed REST. This reduces account API pressure without changing strategy thresholds, sizing, leverage, stops, cadence, or order behavior.
- 2026-06-02 update: P0-A API root-fix has a first central account-state cache bridge. `core/account_state_cache.py` reads fresh `runtime/account_snapshot_latest.json` rows for non-confirmation entry risk gates, and A/v11, B/v16, C/v14 `_can_open_new_position()` require that fresh central state; if it is missing/stale, they pause new opens rather than falling back to signed REST.
- 2026-06-02 update: P0-A public REST attribution is now visible in system alerts. `system_alerts.py` aggregates `recent_public_requests` into top public 60-second callers, and the public guard now defaults to a smoother `60/min` with `1000ms` minimum interval. The public cooldown observed at `17:34 CST` was from `public:C/v14 /fapi/v1/klines`; top recent public callers were C/v14 Klines and A/v11 Klines. This is mitigation/observability, not the final API queue.
- 2026-06-02 update: P0-A signed REST fallback has been tightened further. Non-confirmation entry risk gates now require fresh central account state; if the account snapshot is missing/stale, A/B/C log `OPEN_SKIPPED/account_state_unavailable` and pause new opens instead of falling back to signed balance/position reads. Runtime same-symbol checks, position counts, exchange-position sync, and hard-stop exchange sweeps also use fresh central state and skip non-order signed sweeps while stale. Actual order/open/close confirmations still use exchange REST.
- 2026-06-02 update: account snapshots now pace multi-account collection. The account snapshot service waits `ACCOUNT_SNAPSHOT_ACCOUNT_GAP_SEC` (default `65s`) between successful account collections, checks shared guard cooldown before each account, and stops the cycle if a rate-limit error appears. This allowed the natural `19:29 CST` snapshot to recover with `fresh_accounts=3`, `partial_error_count=0`, and guard cooldown cleared. This is still REST pacing, not user-data-stream.
- 2026-06-02 update: A/v11 trailing-pullback rollout now has a read-only review chain. `a_v11_rollout_review.py` produces 24h/72h/168h live windows, forced-close contribution, estimated cost, and a P-level recommendation; Aliyun refresh/review runs it, reverse sync and live-context pull include it, and the portal surfaces the result on the first screen plus a detail table. Latest local review showed `P1/manual_review_required`, 72h after-cost PnL about `-320.76` USDT. This is evidence for review only; no automatic rollback or parameter change is performed.
- 2026-06-02 update: P1 rollback-watch now also has a cross-strategy action matrix. `rollback_watch_review.py` summarizes active P0/P1 rollback decisions from strategy evolution into closed samples, after-cost PnL, forced-close/open-fail rates, regime, and action (`pause_expansion_review_quality` / `prepare_rollback_review` / `prepare_manual_rollback`). It is report-only and appears in the command-center function cards.
- 2026-06-02 update: `core.replay` now exposes `ReplayGateResult` / `evaluate_observed_gate()` as the first pure gate result surface. `replay_gate_audit.py` uses that API, while the older `classify_replay_decision()` wrapper remains compatible. This advances N2 toward shared live/replay gate functions, but scanners have not yet been refactored to call pure A/B/C strategy gates.
- 2026-06-02 update: P0-B has live scanner pure-gate call paths for A/v11, B/v16, and C/v14 entry-threshold gates, B/v16 15m confirmation, C/v14 15m confirmation, C/v14 low-score tail guard, C/v14 stale entry-price market-data guard, C/v14 ATR market microstructure gate, A/v11 resonance-adjusted replacement-signal eligibility, A/v11 releasable-position selection, A/v11 market microstructure gate, A/B/C no-same-symbol position gating, C/v14 same-side position/sector/stop-loss gates, B/v16 stop-loss gate, B/C score-max gates, B/v16 active-position limit gate, and B/v16 small-live stage guard. `tests/test_strategy_gates.py` now provides a first shared gate parity smoke. A/v11 replacement close/open orchestration/confirmation/risk/execution gates, C/v14 remaining risk/execution gates, B/v16 remaining position/execution gates, and historical event-driven same-input replay/live parity tests still need completion.
- 2026-06-02 update: P0-A public REST guard was tightened again after `public:C/v14 GET /fapi/v1/klines` returned a fresh `429` and set cooldown to `2026-06-02T21:30:59+08:00`. Shared public defaults are now `45/min` and `1400ms`; A/B/C, sentinel, and market-data-cache were restarted with the new core. Verification still needs a natural post-cooldown observation window.
- 2026-06-02 update: P0-A signed REST startup fallback was tightened after natural post-cooldown verification immediately hit `A/v11 GET /fapi/v2/balance` `429/-1003` at `21:30:59 CST`. A/v11 and C/v14 startup exchange-state sync now require fresh central account state and skip startup sync when unavailable instead of probing account config, positions, or balance. A/B/C scanner balance-summary helpers also no longer call signed balance when central state is missing.
- 2026-06-02 update: P0-A now has a post-ban recovery ramp in `core/binance_api_guard.py`. After a ban timestamp expires, signed and public REST callers share a default 5-minute quiet period and then a 30-minute combined recovery ramp at `4/min` with `15000ms` minimum interval. This addresses the observed pattern where very low local request counts still trigger fresh Testnet/IP bans when multiple services wake at the same time.
- 2026-06-02 update: `core.execution_engine` now reuses a fresh operation-local position snapshot for close target and immediate close-retry confirmation steps, reducing duplicate `positionRisk` calls in one close loop without weakening delayed confirmation freshness. Confirmation attempts after sleeps still refresh and still require exchange position disappearance.
- 2026-06-03 construction update: P0-A now has a central API-queue foundation. `core/binance_api_queue.py` persists Binance REST request intent in `runtime/binance_api_queue.sqlite3` with priority, cooldown deferral, leases, idempotency keys, and result/error records; `binance_api_queue_service.py` and `crypto-binance-api-queue.service` are draft construction artifacts. Scanners are not switched to this queue yet, and paused Testnet services remain intentionally stopped.
- 2026-06-03 construction update: P0-A user-data-stream groundwork now includes `core/account_state_stream.py`, a pure reducer for Binance Futures `ACCOUNT_UPDATE` payloads. It updates central account rows, balances, position add/remove, side counts, notional, and upnl without REST. Listen-key/websocket transport is still not active.
- 2026-06-03 construction update: `account_state_service.py` can now apply newline-delimited user-data-stream event logs through `--stream-events` / `--stream-strategy` into `runtime/account_state_latest.json`. This gives the future websocket/listen-key service a no-REST write path, but offset/dedup persistence and live websocket transport remain open.
- 2026-06-03 construction update: `core/binance_user_stream.py` now stores listen-key state in `runtime/binance_user_stream_listen_keys.json`, detects keepalive/restart due records, and builds start/keepalive/close request specs for the central API queue. It does not call Binance or open websockets yet.
- 2026-06-03 construction update: `core/binance_api_executor.py` now provides the first central API-queue executor. It signs queued requests, executes public/signed REST through an injectable transport, records success/failure, and turns `418/429/-1003` into queue cooldown deferral. `binance_api_queue_service.py --execute` enables this path, but no service is started in construction mode and scanner clients are not switched to the queue yet.
- 2026-06-03 construction update: `account_state_service.py --stream-events` now maintains `runtime/account_state_stream_offsets.json` with seen event ids and per-strategy last event metadata, so repeated user-stream JSONL after reconnect does not reapply duplicate account updates.
- 2026-06-03 construction update: P0-A now has a user-data-stream runtime/service foundation. `core/binance_user_stream_runtime.py` parses websocket messages, writes durable user-stream JSONL, and creates latest-event files. `binance_user_stream_service.py` can replay local message files into central account state and has optional websocket mode via `websocket-client`; the draft service is deployed only as disk code and is not started.
- 2026-06-03 construction update: listen-key startup is now connected to the queue executor. `refresh_listen_key_via_queue()` can start or keepalive `/fapi/v1/listenKey`, update listen-key state, and the user-stream service can call it when no listen-key is supplied. This is still not fresh-run verified because services remain stopped.
- 2026-06-03 construction update: A/v11, B/v16, and C/v14 Binance clients now have a queue-client bridge. With `BINANCE_API_QUEUE_CLIENT_ENABLED=1` (default), signed REST and client public exchangeInfo REST are submitted to the central API queue and fail closed if the executor is absent instead of falling back to direct REST. This is deployed only as construction code until the fresh-run start sequence explicitly starts the API queue executor first.
- 2026-06-03 construction update: remaining known public Binance REST producers now also default to the central API queue: scanner Kline/ticker/depth/funding fetchers, B/v16 live aggTrades, sentinel ticker, market-data-cache ticker, VPB funding/premium fetches, order-rule public price checks, counterfactual Kline fetches, and daily review market fetches. Direct guarded urlopen remains only when `BINANCE_API_QUEUE_CLIENT_ENABLED=0`.
- 2026-06-03 construction update: execution confirmation now requires fresh central account-state by default. If open/close confirmation cannot read central account-state that is fresh enough and newer than the order submission timestamp, `ExecutionEngine` fails closed with explicit `*_confirm_account_state_unavailable` codes instead of falling back to client position REST or treating missing confirmation state as an already-closed position. The old fallback is only available by explicitly setting `CENTRAL_ACCOUNT_STATE_CONFIRM_REQUIRE=0`.
- 2026-06-03 construction update: user-stream service orchestration now has A/v11, B/v16, and C/v14 draft systemd units, all depending on the API queue and account-state services. They are deployed only as disk code until the controlled fresh-run starts.
- 2026-06-03 construction update: release-manager post hooks for `api-queue`, `account-state`, and `user-stream` now install their construction unit files into `/etc/systemd/system/` and run `daemon-reload` without enabling or starting them.
- 2026-06-03 fresh-run update: `binance_user_stream_service.py` treats websocket receive timeout as an idle heartbeat instead of a process failure, because user-data-stream can be quiet when no account event occurs. When `--apply-state` is enabled, idle websocket loops refresh the matching central account-state row timestamp. Tencent runtime ownership for queue/guard/listen-key state must remain `ubuntu:ubuntu`; root-owned runtime queue/listen-key files will block construction services. Do not re-enable periodic account-state polling; run it only as a manual baseline after cooldown if needed.
- 2026-06-02 update: sentinel quality review now includes N6 forward-return and coverage audit. It estimates 15m/30m/60m/120m raw and directional after-fee returns from later same-symbol sentinel scan prices, compares large `SENTINEL_SIGNAL` events against strategy-scan coverage, and surfaces coverage/forward-return tables in the command center. This is report-only analytics and does not add API calls or alter strategy behavior.
- 2026-06-02 update: sentinel quality review now also includes a first missed-big-move attribution layer. Large `SENTINEL_SIGNAL` rows are matched to nearby strategy `sentinel_scans` and bucketed into not scanned, scanned/no signal, strategy rejected, risk/cooldown/position rejected, confirmation rejected, execution rejected, market-data rejected, pre-filter rejected, or analysis/data error. This is still coarse report-only attribution; the next gap is splitting `not_scanned` into watchlist/cadence/mirror-truncation and joining outcomes to full replay/fill.
- 2026-06-02 update: the `not_scanned` N6 bucket now has a first timing/universe split. `sentinel_quality_review.py` reports whether unscanned big movers were never present in the mirrored `sentinel_scans`, scanned only outside the matching window, or near-window misses. This helps separate scanner cadence/window issues from scanner-universe or bounded-mirror gaps, but it still needs durable watchlist snapshots and full replay/fill before changing cadence.
- 2026-06-02 update: market-mover sentinel now persists durable watchlist snapshots to `runtime/market_mover_watchlist_history.jsonl` and daily shards. Shadow sync carries that history to Aliyun, and sentinel quality review/portal show whether watchlist history is available. This is observability-only and starts collecting evidence for the next N6 split of watchlist exclusion versus scanner/mirror gaps.
- 2026-06-02 update: `strategy_truth_ledger.py` now reads realized close PnL from nested `payload.raw` as well as top-level payload fields, fixing the false zero-PnL truth-ledger view. It also exposes a read-only `recovery_review` block with per-position age, PnL on margin, risk label, and shadow action; the portal shows this under the strategy quality board. This advances Phase 7 without deploying any automatic recovery-exit rule.
- 2026-06-02 update: `strategy_evolution_gate.py` now exposes a Phase 8 `promotion_gate_hardening` audit. It summarizes minimum sample rules, 3/7/14/30-day window coverage, fee/slippage adjustment, regime labels, account-risk checks, rollback trigger coverage, post-approval window readiness, and whether any gate evidence gaps remain. The portal shows this status on the first screen and in the strategy-evolution section. This is report-only; automatic upgrade/rollback remains disabled.
- 2026-06-01 update: strategy evolution output now includes `expansion_readiness` for controlled expansion. It turns full-live post-approval windows into mature/still-sampling/pause-review counts, missing 24h closed samples, per-candidate action, and a first-screen command-center line so expansion decisions are no longer buried in raw gate tables. Full-live approval files also create gate decision records even if the matching experiment/candidate row is absent on a node, preventing approved live changes from disappearing due to stale research context.
- 2026-06-01 update: strategy evolution post-approval PnL now reads close PnL from both top-level payload fields and nested `payload.raw`, and historical deterministic `-4164` min-notional `OPEN_FAILED` rows are recorded as `resolved_open_failed` rather than strategy-quality `open_failed`. This cleared the B/v16 false rollback-watch while preserving raw counts for audit.
- `部署工具/git_change_guard.py` enforces that material staged code/config/tool changes include a `CHANGELOG.md` entry and rejects staged runtime/secret-like artifacts.
- Tencent server has persistent 2G swap configured through `/swapfile` and `/etc/fstab`; system alerts watch memory, swap, and recent OOM kernel events.

## Persistent Memory Files

- `AGENTS.md`: model-facing operating rules. A new coding model should follow this first.
- `README.md`: fresh-clone, data policy, live sync, and deployment handoff.
- `CHANGELOG.md`: mandatory reason/outcome/verification/remaining-work ledger for every material Git or live operational change.
- `PROJECT_STATE.md`: current state and migration rules. Update this when architecture, deployment, or unresolved priorities change.
- `记忆文档/MEMORY.md`: chronological memory of decisions and major changes.
- `记忆文档/FUTURE_EXECUTION_PLAN.md`: detailed future execution plan for strategy truth ledger, command-center quality board, A/B/C experiments, sentinel contribution, recovery-position management, and promotion gate hardening.
- `runtime/event_store.sqlite3`: canonical event store. Tables: `events` (trading decisions), `sentinel_scans` (sentinel data, date-partitioned), `account_snapshots`, `attention_items`, `attention_acknowledgements`. DB was cleaned from 1.7GB to 85MB on 2026-05-28.
- `research_memory/attention/open_items.json`: exported attention ledger for portal/live sync. Items stay visible until explicitly confirmed or resolved; a daily report rolling over must not remove them.
- `research_memory/hypotheses`, `research_memory/approvals`, `research_memory/lessons`, and `research_memory/promotions`: compact research memory worth carrying between machines.

## Current Open Attention

- A/v11 sizing/risk must remain watched from real-time account snapshots and automatic alerts.
- 2026-06-01 live note: Binance testnet returned `HTTP 418 / -1003 Way too many requests` during account snapshot collection. Treat any sudden zero-wallet/zero-position account snapshot during that window as an API-failure artifact unless a later clean snapshot confirms it. Snapshot collection now preserves the last valid snapshot on API error, parses Binance `banned until` timestamps, sleeps through the retry window, and surfaces cooldown through system alerts. Because the ban recurred after a 120-second interval, Tencent account snapshots are being throttled to 900 seconds and paused through the active ban window; alerts now also show recent API-limit source services. Next architecture work should reduce signed REST calls and move toward websocket/user-data-stream account state.
- 2026-06-02 live note: another Binance `418/-1003` ban occurred at `13:35 CST`, and final verification at `13:48 CST` still had attention `P0=1` until cooldown expiry. The account snapshot service was restarted with the new guard pre-check and logged `status=cooldown`, so do not force manual account snapshot retries during the ban. Wait for cooldown or use non-Binance live evidence only.
- 2026-06-02 later live note: a code audit found and fixed remaining unguarded public REST callers. After cooldown expiry, verify that account snapshots become fresh and the attention ledger clears the Binance P0/P1; if not, inspect guard state before any manual Binance call.
- 2026-06-02 latest live note: cooldown was extended again at `15:44:57 CST` by a no-timestamp `429/-1003` from `C/v14 GET /fapi/v2/balance` after only two signed requests in the local 60-second guard window. Treat this as residual/shared-IP Binance pressure; do not force account snapshot retries. The guard cooldown was manually extended to `2026-06-02T16:44:57+08:00` with `rate_limit_error_streak=2`; wait for that expiry before verification.
- 2026-06-02 later live note: after the `16:44:57 CST` cooldown expired, A/v11 `GET /fapi/v2/positionRisk` received `418/-1003` after only four recent signed requests, and the guard extended cooldown to `2026-06-02T17:26:54+08:00`. Do not force account snapshot retries; continue non-Binance work and verify after that expiry.
- 2026-06-02 latest live note: after P0-A cache bridge deploy, Binance public REST returned `429` from `public:C/v14 GET /fapi/v1/klines` and the shared cooldown moved to `2026-06-02T18:00:59+08:00`. Do not force Binance calls before that expiry. Verify after expiry that public rolling count stays under the new `60/min` default and no fresh `418/429/-1003` appears.
- 2026-06-02 latest live note: after the public cooldown expired, a pre-fix B/v16 runtime `GET /fapi/v2/positionRisk` still hit `418/-1003` after only three local signed requests and extended cooldown to `2026-06-02T19:14:56+08:00`. A/B/C were then patched/deployed so entry gates and non-order runtime sweeps no longer fall back to signed REST when central account state is stale. Next verification should wait until `19:14:56 CST` plus a natural service cycle.
- 2026-06-02 latest live note: P0 API blocker cleared after account-snapshot pacing. At `2026-06-02T19:38+08:00`, live context showed six core services active, account upnl `+69.0386`, `7` positions, attention `P0=0/P1=5/P2=3`, guard `cooldown_seconds=0`, and account snapshot `fresh_accounts=3/partial_error_count=0`. Continue watching public REST at `60/min` and signed snapshot cadence, but do not treat the earlier 19:14 line as active cooldown after the guard has cleared.
- 2026-06-02 latest live note: after C/v14 confirmation/tail pure-gate deploy, `2026-06-02T19:53+08:00` live context still showed six core services active, account upnl `+89.5569`, `6` positions, attention `P0=0/P1=5/P2=3`, guard `cooldown_seconds=0`, and v14 journal had no import/syntax/API-limit errors. Public rolling remained `60/60`; keep watching public REST bursts while continuing P0-B/P1 work.
- 2026-06-02 latest live note: after A/v11 replacement-signal pure-gate deploy, `2026-06-02T20:02+08:00` live context still showed six core services active, account upnl `+89.5569`, `6` positions, attention `P0=0/P1=5/P2=3`, guard `cooldown_seconds=0`, and A/v11 journal had no import/syntax/API-limit errors.
- 2026-06-02 latest live note: after A/B/C same-symbol position-gate deploy, `2026-06-02T20:18+08:00` live context showed six core services active, account upnl `+35.9180`, `4` positions, attention `P0=0/P1=4/P2=2`, guard `cooldown_seconds=0`, and A/B/C journals had no import/syntax/API-limit errors.
- 2026-06-02 latest live note: after C/v14 stale entry-price pure-gate deploy, `2026-06-02T20:35+08:00` live context showed six core services active, account upnl `+33.4479`, `4` positions, attention `P0=0/P1=4/P2=2`, guard `cooldown_seconds=0`, and v14 journal had no import/syntax/API-limit errors.
- 2026-06-02 latest live note: after A/v11 releasable-position pure-gate deploy, `2026-06-02T20:43+08:00` live context showed six core services active, account upnl `+36.5515`, `4` positions, attention `P0=0/P1=4/P2=2`, guard `cooldown_seconds=0`, and A/v11 journal had no import/syntax/API-limit errors.
- 2026-06-02 latest live note: after B/C risk-position pure-gate deploy, `2026-06-02T20:56+08:00` live context showed six core services active, account upnl `+36.5515`, `4` positions, attention `P0=0/P1=4/P2=2`, guard `cooldown_seconds=0`, and B/C journals had no import/syntax/API-limit errors.
- 2026-06-02 latest live note: at `2026-06-02T21:06+08:00`, a new public cooldown appeared from `public:C/v14 /fapi/v1/klines` (`429`, cooldown until `21:30:59`). Public defaults were tightened to `45/min + 1400ms` and deployed/restarted by `21:21`; live context still showed six services active and attention `P0=0/P1=4/P2=2`, but post-cooldown verification remains required.
- 2026-06-02 latest live note: at `2026-06-02T21:30:59+08:00`, natural post-cooldown verification found a fresh signed `429/-1003` from `A/v11 GET /fapi/v2/balance`, extending cooldown to `22:30:59`. This shows P0-A is still not root-fixed; do not force Binance probes before expiry.
- 2026-06-02 latest live note: after removing scanner startup signed REST fallbacks and deploying A/B/C by `21:43 CST`, six core services were active and A/B/C journals since `21:38 CST` had no new `418/429/-1003` or import/syntax errors. Guard cooldown remains from the pre-fix `21:30:59` A/v11 balance error until `22:30:59`; wait for natural expiry before verification.
- 2026-06-02 latest live note: while waiting for P0-A cooldown expiry, B/v16 small-live stage guard was extracted and deployed as `20260602-215704-strategy-b-d9607f8`. At `21:59 CST`, six core services were active, B/v16 journal had no import/syntax/API-limit errors, attention stayed `P0=0/P1=4/P2=2`, and guard still pointed to the old A/v11 balance cooldown.
- 2026-06-02 latest live note: while waiting for the same P0-A cooldown, A/v11 market microstructure gate was extracted and deployed as `20260602-220700-strategy-a-ea58015`. At `22:09 CST`, six core services were active, A/v11 journal had no import/syntax/API-limit errors, attention stayed `P0=0/P1=4/P2=2`, and guard still pointed to the old A/v11 balance cooldown.
- 2026-06-02 latest live note: C/v14 ATR market microstructure gate was extracted and deployed as `20260602-221943-strategy-c-1594290`. At `22:23 CST`, six core services were active, C/v14 journal had no import/syntax/API-limit errors, attention stayed `P0=0/P1=4/P2=2`, and guard still pointed to the old A/v11 balance cooldown.
- 2026-06-02 latest live note: at `22:31 CST`, cooldown expiry still triggered a public `418/-1003` from `public:C/v14 /fapi/v1/klines`; at `22:48 CST`, another public `418` occurred during recovery-ramp rollout before all services had loaded the new guard. Shared recovery-ramp guard was then deployed through A/B/C/account/sentinel and market-data-cache was restarted. Current cooldown is to `23:12:59`; wait for quiet/recovery behavior before judging P0-A.
- 2026-06-02 latest live note: at `23:13 CST`, the cooldown boundary still triggered `A/v11 GET /fapi/v2/balance` `429/-1003` from `crypto-account-snapshot.service`. Root cause for this specific miss was an old account snapshot process that had not restarted since `19:22 CST`; the post-ban recovery code was on disk but not loaded in memory. The account service was manually restarted at `23:29 CST`, now logs `status=cooldown`, and the guard cooldown is `2026-06-03T03:13:00+08:00`. `release_manager.py` now import-smokes the account snapshot module before restarting account service. Do not force Binance calls before the new cooldown and recovery window; P0-A remains incomplete until user-data-stream/central account-state and a clean observation window land.
- 2026-06-03 latest live note: per user instruction, Testnet is now in construction mode. All Binance-facing services plus system-alerts/data-maintenance were stopped, and active runtime data was reset from zero using `runtime_data_reset.py`. The reset archived `/opt/crypto-auto-trader/runtime/event_store.sqlite3` and selected runtime/log/scanner files to `/opt/crypto-auto-trader/archive/testnet_data_reset/20260603-000807`, then cleared `events`, `sentinel_scans`, and `account_snapshots` to `0` while preserving attention tables. DB `quick_check` is `ok`, size reduced from about `774MB` to about `9MB`. Do not restart scanners/account/sentinel until P0-A/P0-B construction work is ready for a fresh run.
- 2026-06-03 latest construction update: P0-A central account-state foundation is now on disk. `core/account_state.py` defines `runtime/account_state_latest.json` with atomic writes and Binance-like conversion; `core/account_state_cache.py` reads it first and falls back to legacy snapshots; `account_state_service.py` can write central state and optionally mirror legacy snapshot/DB during migration; `crypto-account-state.service` is a draft unit. Tencent deploy `20260603-004512-account-state-037f51e` uploaded/import-smoked it, but no service was started.
- 2026-06-03 latest construction update: `ExecutionEngine` confirmation now has a central account-state proof path. It accepts central positions only when the account-state timestamp is newer than the order submission time, then falls back to the existing exchange REST confirmation if central state is absent/stale/too old. Tencent deploy `20260603-005527-account-state-30466b9` uploaded/import-smoked this code; all services remain intentionally paused.
- 2026-06-03 latest construction update: queue + user-stream-only passed a clean observation window and market-data-cache/sentinel can run clean through the central queue, but scanner Kline fresh-run still triggers public `HTTP 418/-1003` at the previous 15s cadence. Public producers are stopped again. The active blocker is public queue cooldown until `2026-06-03 07:18:57 +08`; after that, retry uses the newly deployed 60s executor and 180s queue waits, starting with at most one public producer. The queue client now fail-closes before enqueue during cooldown, so services should not keep extending a known cooldown.
- 2026-06-01 later update: signed REST now also has a global file-backed guard, so one service hitting a Binance ban should make sibling services back off instead of extending the ban. This reduces risk but does not replace the future websocket/user-data-stream migration.
- B/v16's legacy close-confirm alert cleared by the 2026-05-30 check; the code still must surface any new unresolved close-confirm or open-sizing mismatch failures as bad alerts. As of 2026-06-01 22:16 CST, B/v16 approved candidates are back to full-live monitoring/P2 after historical `-4164` min-notional rows were reclassified as resolved execution-rule noise; future non-preflight `OPEN_FAILED`, hard-stop pressure, PnL decay, and close-confirm failures still require attention.
- A/v11 approved trailing-pullback candidates remain P1 rollback-watch as of 2026-06-01 22:16 CST, now based on real 72h post-approval PnL after reading nested close PnL from `payload.raw`, not on a missing-PnL report artifact. Do not auto-rollback without operator decision, but treat this as the current highest strategy attention item.
- Strategy evolution attention should treat the two B/v16 2026-05-27 candidates as resolved after the 2026-05-31 full-live approval. The two A/v11 trailing-pullback candidates are resolved from the attention ledger after full-live approval, and `EXP-20260523-v11-replacement-quality` remains a guarded/small-live monitoring item rather than a full-rollout approval.
- User-confirmed archived P0 system items: `入口页刷新失败`, `总入口页面偏旧`, and the 2026-05-26 21:56 CST OOM alert. They remain in SQLite acknowledgement history but no longer occupy P0.
- B/v16 perceived two-day inactivity was a visibility issue: the event store showed a 2026-05-26 09:21:29 +08:00 `XRPUSDT` short open plus earlier same-day opens.
- C/v14 reporting now uses entry-candidate wording. The 2026-05-25 daily review shows 57 entry candidates versus 46378 raw signals; raw data is still used for funnel analysis, but should not be read as executable signal count.
- C/v14 has an open P2 stale-open attention item as of 2026-05-30 because no new open has occurred for more than 48h while service heartbeat remains current; treat it as a funnel/market-fit review item, not a service outage.
- Account snapshots now normalize live position direction through `core/position_utils.py`. Binance testnet rows with contradictory `positionSide` are interpreted by matching raw unrealized PnL against entry/mark first; the raw side and inference source remain in the snapshot for audit.

## Not Done / Next

- Current long-term P0/P1/P2 order is canonicalized in `记忆文档/FUTURE_EXECUTION_PLAN.md`; older unchecked phase items are historical context unless they still appear in that queue.
- User acknowledgement workflow has both a script path (`部署工具/acknowledge_attention_items.py`) and a portal/API path (`crypto-attention-api.service` + `/api/attention/ack`). Continue verifying that browser-side acknowledgements write to SQLite and refresh `research_memory/attention/open_items.json`.
- B/v16 approved strategy-evolution candidates (`EXP-20260527-v16-atr-stop-bands`, `EXP-20260527-v16-overheat-cap-85`) were explicitly approved by the user on 2026-05-31 and are no longer review-only. Continue monitoring live sample count, B/v16 PnL, hard-stop rate, and close-confirm failures after rollout.
- A/v11 approved trailing-pullback rollout is the current P1 rollback-watch. The dedicated rollout review now compares 24h/72h/168h real PnL, forced-close contribution, cost estimate, and top losers; next decision is whether selected `15m=1.0 ATR` / `30m=0.8 ATR` should continue, narrow, or roll back. Do not auto-rollback without operator-quality decision.
- A/v11 rollout review now carries its own decision packet, exit-model attribution, fee/slippage sensitivity, and first local-cache replay/fill comparison, but the actual operator decision remains pending until fresh post-restart report data and broader Kline coverage exist.
- Current P1 rollback-watch matrix should be read as "pause further expansion and review quality", not as automatic rollback. A stronger rollback action requires mature 72h/168h evidence, unresolved close failure, account-risk escalation, or explicit operator decision.
- Current offline decision packets are now generated inside `strategy_evolution_gate.py` and rendered by `rollback_watch_review.py`. A P0/P1 strategy item should no longer be considered operator-ready unless its packet includes change, expected advantage, risk, evidence maturity, rollback path, close-failure attribution when applicable, and the report-only automation status.
- Execute `记忆文档/FUTURE_EXECUTION_PLAN.md` in order: dual-server architecture migration (Phase 0.5 ✅), strategy truth ledger (Phase 1 ✅), command-center quality board (Phase 2 ✅), sentinel quality review (Phase 6 ✅), A/B/C shadow experiments (Phase 3/4/5 ✅), recovery-position policy review (Phase 7 ✅), promotion gate hardening (Phase 8 ✅), and testnet-to-live transition (Phase 9 ✅).
- Architecture: Tencent runs the external-data paper mainline: scanner/v11, scanner_v14, scanner_v16, market_data_cache, market_microstructure, paper_exchange_refresh timer, and system_alerts. Binance queue/account-snapshot/user-stream/old sentinel services are not part of the current mainline. Aliyun runs report/analysis refresh plus attention-api service (port 8090).
- Analysis/report pipeline runs on Aliyun from synced Tencent data; generated portal/reports are synced back to Tencent for viewing. Tencent keeps live trading/API-dependent services and local SQLite ingestion.
- Aliyun daily shadow-review (`run_shadow_review.sh`) now runs the full report chain through decision attention and portal generation before reverse-syncing to Tencent; hourly analysis refresh remains separate.
- A/v11 API key updated on 2026-05-28: new Testnet account with 5000 USDT, dual-side position mode enabled. Previous account was invalidated by Binance Testnet platform.
- Sentinel scans separated into dedicated `sentinel_scans` table with date column (YYYY-MM-DD) for efficient daily queries and cleanup.
- Phase 2 portal upgraded: `portal_dashboard.py` now shows "策略质量看板" with active-strategy PnL, recovery PnL, PF, win rate, payoff ratio from truth ledger.
- Phase 6 sentinel review deployed: `sentinel_quality_review.py` runs on Aliyun, measures sentinel signal contribution, forward returns, large-move scan coverage, missed-big-move attribution, first-pass unscanned timing/universe split, and watchlist-history availability. The next gap is using newly persisted watchlist snapshots to prove watchlist inclusion versus scanner/mirror truncation and joining results to full replay/fill outcomes.
- Phase 7 recovery-position review has a report-only first pass in `strategy_truth_ledger.py` and the portal, now including takeover path MFE/MAE, drawdown-from-MFE facts, same-strategy signal evidence, strategy-specific recovery exit evidence, and first local-cache bar replay evidence. Remaining Phase 7 gaps are long Kline windows, fuller replay/fill output, and governance before any automatic recovery-exit rule.
- P1-C replay/fill now has conservative local depth assumptions in counterfactual reports: visible order-book levels can be capped, visible liquidity can be discounted, and OB available quantity/fill ratio are shown in JSON/Markdown/portal. This remains report-only and uses only local/mirrored depth snapshots; no Binance API call or live order behavior changed.
- P1-C/P1-E depth snapshots now have a queue-safe plan/ingest path: `research_depth_backfill.py` can plan depth requests without network, optionally submit them through the central queue in staged runs, and merge DONE responses into `research_store/depth_snapshots` / `runtime/depth_cache`. Real coverage still waits for a staged queue run; this is not historical depth backfill.
- P1-C replay/fill also has first queue-ahead and market-impact assumptions: report-only counterfactuals can reserve visible queue volume ahead of our order and add side-aware entry impact bps. This makes fill evidence more conservative but still not a true queue-priority/market-impact engine.
- P1-B/P1-C B/v16 rollout replay now models B/v16 protective exits too: `core.replay_fill` has optional close-price hard-bottom and profit-retrace guards, and `b_v16_rollout_review.py` enables the live B/v16 constants (`10%` leveraged hard bottom, `30 USDT` best-profit floor, `25%` retrace). This is report-only and local/mirrored-data only; no live exit behavior changed.
- P1-C/P1-E replay readiness is now explicit: `replay_readiness_review.py` is the unified post-ingest gate for Kline 30-day acceptance, depth coverage, A/B rollout replay completion, and recovery replay completion. It will remain `data_gap` or `waiting_for_samples` until staged queue ingestion creates real long-history/depth partitions and enough post-refresh samples.
- 2026-06-08 update: replay readiness attribution now separates true Kline/depth `data_gap` from paper-event `context_gap`. Short Aliyun report refreshes no longer reclassify seeded 30-day Kline coverage as missing, paper-exchange maintenance/bootstrap events are excluded from rollout evidence, and old `paper_exchange` samples are treated as context/open-pairing gaps instead of fake replay-ready evidence. Automatic rollback and automatic tuning remain disabled until fresh contextual paired samples plus data readiness pass.
- P1-E research-store retention is now explicit and safe-by-default: `research_store_retention.py` only plans by default, and `--apply` archives old partitions to `research_store_archive/` instead of deleting them. Remaining warehouse storage work is Parquet compaction/row-group optimization after real staged backfill creates larger partitions.
- P1-E research-store compaction is now explicit and safe-by-default: `research_store_compaction.py` only plans by default, and `--apply` backs up old partition contents before rewriting. It prepares for larger post-backfill partitions but should not be used as automatic destructive cleanup.
- Long-term skeleton review is now explicit: `long_term_skeleton_review.py` and the portal matrix show all canonical P0/P1/P2 modules have first-version bones. Current state is skeleton complete but staged-validation blocked, not production acceptance.
- 2026-06-04 update: P0-B scan-level exact-case coverage now includes B/v16 loss-blacklist pre-filter rejects and A/v11 VPB blacklist pre-filter rejects. This prepares A/B staged scanner rows for strict fresh-window parity without changing strategy thresholds, risk, sizing, leverage, stops, or order behavior.
- 2026-06-04 update: Tencent staged scanner evidence window `11:24:30`-`11:53:20 CST` accepted 9 fresh scan-level exact cases (`scan_exact_case_coverage_pct=100`, `acceptance_status=accepted`) from A/B/C scanner code. This is scan-flow first-version evidence only; open/close natural rows remain post-launch evidence. A/v11 Binance client now lazy-loads exchangeInfo so pre-filter-only staged runs do not queue public metadata requests at initialization.
- Phase 8 promotion gate hardening has a report-only audit layer in `strategy_evolution_gate.py`, `rollback_watch_review.py`, and the portal. Differentiated thresholds by strategy/change type, cross-regime robustness scoring, post-approval PF/cost/fill-liquidity stress, and rollback governance readiness are implemented as report-only evidence. Remaining Phase 8 gaps are true depth/queue-priority paper fill simulation and an explicitly approved automatic rollback procedure before any auto-upgrade or auto-rollback logic.
- Strategy evolution still needs stronger promotion rigor before auto-upgrade: walk-forward windows, paper fill simulation, fee/slippage modeling, regime segmentation, and post-change rollback rules.
- Decision-entry redesign is now a priority: the first screen should show an executive summary, strategy action state, live PnL, risk, and whether a better strategy candidate needs approval. Details remain available only as drilldowns.
- Decision report PnL rule: expandable strategy details may show exchange snapshot unrealized PnL as current live-state evidence, and fees must be labeled by trust level. Funding rates are no longer part of the report/bill PnL口径. Without fill commission ledger, show taker-fee estimates instead of claiming exact after-cost PnL.
- Next architecture work: unified replay/live path, Parquet/DuckDB research warehouse, and gray/rollback-aware evolution gate. This is expected to improve iteration speed and decision accuracy more than further manual threshold tuning.
- SQLite growth remains under normal monitoring: the 2026-05-29 read-only check showed Tencent DB about 212MB and Aliyun mirror about 215MB, both `quick_check ok` with `freelist_count=0`. No immediate VACUUM/destructive cleanup is needed, but system alerts and data maintenance should continue watching growth.
- After the 2026-05-27 decommission, do not recreate Polymarket code, reports, service units, or command-center cards unless the user explicitly reopens that project.
- After the 2026-05-27 order-failure hardening, monitor fresh `OPEN_FAILED` events by exact Binance code. Rule-driven rejections should appear as `OPEN_SKIPPED/execution_preflight`; any remaining `OPEN_FAILED` means a real API/exchange/account condition still needs diagnosis.
- GitHub CI is not configured yet.
- The Git change ledger is enforced locally by `git_change_guard.py`; remote CI enforcement is not configured yet.
- Deployment tooling exists, but production deploys still require explicit operator judgement; secrets and server-side environment files remain outside Git.
- Multi-machine deployment secrets must stay outside Git; use server-side environment files or GitHub Actions secrets only if CI/deploy is added.
- Historical Git commits and old notes may still contain credential text from before redaction. Treat those credentials as exposed if still valid; rotate them before broader collaborator access or rewrite history if necessary.

## GitHub Migration Rules

- Repository remote: `git@github.com:EdisonY/AutoTrading.git`.
- Local repository initialized on branch `main` and pushed to GitHub. `main` tracks `origin/main`.
- Current machine public key fingerprint: `SHA256:qxGXZU6nLnc0OtXLjRVjvH2NZv7p8c36rvRdTndJy+o` from `C:\Users\Eels\.ssh\id_rsa.pub`.
- Commit code, configs, docs, systemd units, compact research memory, and long-lived project memory.
- Do not commit `.env`, API keys, SSH keys, SQLite DBs, runtime snapshots, logs, reports, server mirrors, or bulky generated backtest/report data.
- Before pushing from a new machine, provide or configure: GitHub SSH access, target branch policy, server SSH access, server environment files, and any CI secrets separately from the repo.
