# Runtime strategy analysis
numpy>=2.2,<3
pandas>=2.2,<4

# Local deployment and server synchronization tools
paramiko>=3.4,<5

# Python 3.10 compatibility for config parsing on the current live node
tomli>=2.0,<3; python_version < "3.11"
