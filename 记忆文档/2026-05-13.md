# 2026-05-13 日志

## 复盘 10:23

### 三账号状态
| 账号 | 策略 | 余额 | 持仓数 | 未实现PnL |
|------|------|------|--------|-----------|
| A | v11 | 3,102 USDT | 70笔 | -277 |
| B | v16 | 4,015 USDT | 51笔 | -720 |
| C | v14 | 3,473 USDT | 34笔 | +425 |

### 发现问题
1. **v16严重bug**: BinanceClientV2 缺少 `get_klines()` 和 `agg_trades()` 方法，导致 fetch_klines 全部报 WARNING，所有信号（CVD/ORB/Delta）无法计算，v16 **零开仓**。需在 binance_client_v2.py 补充这两个方法。
2. **v14保证金不足**: C账号可用余额仅14 USDT，持续报 -2019 Margin insufficient，34笔持仓已占满保证金。
3. **v11 大亏仓**: CRVUSDT SHORT -54.72, STXUSDT SHORT -31.45, COLLECTUSDT SHORT -23.99，需关注是否触发MAX_LOSS(30%)。
4. **v16 大亏仓**: GUAUSDT SHORT -463, BNBUSDT SHORT -230, UBUSDT SHORT -230, BTCUSDT SHORT -242，市场近期大涨，空头被套严重。

### 待处理
- [ ] 修复 binance_client_v2.py，补充 get_klines() + agg_trades() 方法
- [ ] v14 考虑手动平几个亏损仓释放保证金
- [ ] v16 修复后重启 crypto-scanner-v16.service

### 已修复（11:08）
1. ✅ **binance_client_v2.py 补充 3 个方法**：`get_tickers()`、`get_klines()`、`agg_trades()`，均为公开接口走 `_request("GET", ...)` 封装
2. ✅ **scanner_v16.py 修复 pd.Series 索引 bug**：`calc_atr(...)[-1]`/`calc_rsi(...)[-1]` → `.iloc[-1]`（4处），pd.Series 不支持 `[-1]` 作为 key 查找
3. ✅ **v16 服务重启成功**：`crypto-scanner-v16.service` active，第一轮 50 币 × 2 周期扫描完成，无 ERROR/WARNING，`v16资金: $1000.00 | 持仓: 0`

### 待处理
- [ ] v14 考虑手动平几个亏损仓释放保证金
- [ ] v16 大亏仓（GUAUSDT SHORT -463 等）需关注

## v16 全部平仓（11:51）
- 批量平仓 v16（账号B）51 笔持仓，全部成功
- 平仓后余额：3,203 USDT 可用，剩余持仓 0
- v16 scanner 继续运行，仅新信号开仓

## v16 零开仓诊断（15:17）
**根因分析**：
1. **MIN_VOL_RATIO=1.2 过滤太严**：50 币中 23 个被 vol_ratio 过滤掉（Testnet 流动性低，大量币 vol_ratio<1.0）
2. **CVD 信号几乎全 0**：`fetch_cvd()` 用 `agg_trades(limit=30)` 取 30 笔交易，Testnet 交易稀疏导致 CVD 计算无意义，20 分大头信号丢失
3. **信号分数不够**：通过过滤的币最高才 30 分（Delta底背离20+EMA10），差 5 分到 SCORE_MIN=35
4. **唯一触发开仓也失败了**：NAORISUSDT short 35分，但报 -1111 Precision error（数量精度问题）

## 实盘CVD信号实施（17:42）

### 架构
- **问题**：阿里云服务器(39.105.156.210)无法直接访问 `fapi.binance.com`（DNS解析被墙/运营商拦截），但 `testnet.binancefuture.com` 正常
- **方案B2**：本地Windows启动HTTP转发代理(`binance_proxy.py`) + SSH反向隧道 → 服务器通过 `localhost:18081` 访问实盘API
- **链路**：服务器`localhost:18081` → SSH隧道 → 本地`binance_proxy.py`(18081) → `fapi.binance.com`

### 修改文件
1. **scanner_v16.py**：
   - `fetch_cvd()` 改为通过SSH隧道代理(`http://127.0.0.1:18081/proxy`)请求实盘aggTrades
   - 新增 `CVDRateLimiter` 类：8次/秒限速 + 60秒缓存 + 代理可用性检测
   - 新增 `_fetch_live_agg_trades()` 通过代理获取实盘数据
   - 代理不可用时自动降级（CVD归零，不阻断其他信号）
   - `MIN_VOL_RATIO` 从 1.2 降到 0.3（Testnet 15m K线成交量稀疏）
2. **binance_proxy.py**（新增）：本地Binance实盘API转发代理，路径 `/proxy/fapi/v1/...`

### 验证结果
- ✅ 隧道连通：服务器通过 `localhost:18081` 成功获取实盘 aggTrades
- ✅ CVD 有方向性：BTC CVD=-0.45, ETH CVD=-0.84（市场下跌时段，卖方主导）
- ✅ 通过过滤 19 币（从0→19），GTCUSDT 净分+40（CVD买入+Delta底背离+RSI超卖）
- ✅ 限速统计：7次请求/7币缓存

### 注意事项
- SSH反向隧道依赖本地电脑在线，电脑关机则CVD信号归零（其他信号不受影响）
- 长期方案：购买境外VPS做永久代理中转（$3-5/月）
- 遗留问题：-1111 Precision error 开仓精度问题待修复

## 晚间复盘 18:20

### 三账号当前状态

| 账号 | 策略 | 余额 | 可用 | 持仓数 | 未实现PnL | 总PnL | 胜率 | 近3天PnL |
|------|------|------|------|--------|-----------|-------|------|----------|
| A | v11 | $3,112 | $0 | 70 | -$312 | +$122 | 41.7% | +$357 |
| B | v16 | ~$3,203 | $3,203 | 0 | $0 | +$209 | 55.1% | -$12 |
| C | v14 | ~$68 | $68 | 34 | +$519 | -$472 | 33.1% | -$293 |

### 服务状态
- v11 scanner: ✅ active (刚重启)
- v16 scanner: ❌ inactive (停了！v13服务仍停)
- v14 scanner: ✅ active (连续亏损6次冷却中，剩余107分钟)
- dashboard-A: ✅ active (8081)
- dashboard-B: ❌ inactive (8082停)
- dashboard-C: ✅ active (8084)

### 关键问题
1. **A账号70仓未实现-$312**：UBUSDT SHORT -187, AKTUSDT SHORT -176, BTCDOMUSDT SHORT -66 亏最多；可用余额$0，保证金占满
2. **B账号v16 scanner停了**：crypto-scanner-v13.service inactive，需重启；但B已无持仓，余额$3203
3. **C账号34仓未实现+$519**：SIRENUSDT LONG +148, GUAUSDT LONG +144, BROCCOLIF3BUSDT LONG +121 盈利大头；但可用仅$68，开新仓困难
4. **v14连续亏损冷却**：6连亏触发120分钟冷却，剩余约90分钟
5. **A/C大量僵尸仓**：A有70仓、C有34仓，很多是恢复持仓，可能需要清理

## v16 持久化 + 精度修复（18:41）

### 修改内容
1. **持仓持久化**：`positions_v16.json` 保存到 `scanner_data_v16/`，开仓/平仓/状态变更时自动保存
2. **(symbol, side) 复合 key**：修复旧版用 symbol 做 key 导致同币种双向持仓冲突的 bug
3. **交易所余额初始化**：capital 不再硬编码 1000，从 `get_balance()` 获取 USDT 可用余额
4. **重启恢复**：`_sync_exchange_state()` + `_load_positions()` 双重恢复（本地文件优先 + 交易所补缺）
5. **ExchangeInfoCache 精度修复**：缓存 705 交易对的 qtyPrecision/pricePrecision/stepSize，`round_qty()` 按 stepSize 向下取整，修复 -1111 Precision Error
6. **Python 3.13 兼容**：`utcnow()`/`utcfromtimestamp()` → `datetime.now(timezone.utc)`/`datetime.fromtimestamp(tz=timezone.utc)`

### 验证结果
- ✅ 重启后恢复 3 个交易所持仓（PLAYUSDT/XNYUSDT/VELVETUSDT short）
- ✅ COLLECTUSDT short 成功开仓（qty=7343，qtyPrec=0 取整为整数），**不再报 -1111**
- ✅ 初始资金从交易所获取：$3078.06
- ✅ 持久化文件 `positions_v16.json` 内容完整（4持仓全量信息）
- ✅ 无 DeprecationWarning
