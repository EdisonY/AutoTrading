# 2026-05-17 项目记忆

## 实时复盘结论

### 服务器与服务状态
- 服务器：`39.105.156.210`
- 项目路径：`/opt/crypto-auto-trader`
- Python：`/root/miniconda3/bin/python3`
- 今日已确认并重启：
  - `crypto-scanner` active
  - `crypto-scanner-v14` active
  - `crypto-scanner-v16` active
  - `crypto-dashboard-b` active/enabled

### 复盘数据摘要
- 从部署至 2026-05-17 01:01 的全量日志复盘：
  - 已平仓交易：3775笔
  - 已实现PnL：+803.18 USDT
  - 当时未实现PnL：+1392.93 USDT
  - 实现+未实现合计：+2196.11 USDT
- 主要问题：
  - A账号持仓过多且可用余额为0
  - A/C空头历史明显拖累
  - B/v16胜率较好但样本较小
  - 1h明显优于15m，尤其v14：1h为正，15m大幅拖累
  - `OPEN_FAILED` 大量来自保证金不足，污染日志且浪费API

## 已完成优化

### P0/P1（控仓、保证金、周期和空头门槛）
- 已修改并部署：
  - `F:\AutoTrading\策略文件\scanner.py`
  - `F:\AutoTrading\策略文件\scanner_v14.py`
  - `F:\AutoTrading\策略文件\scanner_v16.py`
- 未新增黑名单（用户明确要求“不做黑名单”）。
- 全部扫描器加入开仓前置风控：
  - 总持仓数超过上限后只管理已有仓位，不再新开仓
  - 开仓后至少保留 `max(300USDT, 25%权益)` 可用保证金
- A/v11：
  - `MAX_TOTAL_POSITIONS=30`
  - `MAX_POSITIONS_PER_TF=2`
  - `SCORE_THRESHOLDS={"15m":105,"30m":90}`
  - `SHORT_ENTRY_PENALTY=20`
- B/v16：
  - `TIMEFRAMES=["1h","15m"]`
  - `MAX_TOTAL_POSITIONS=20`
  - `SCORE_THRESHOLDS={"1h":40,"15m":55}`
  - `SHORT_ENTRY_PENALTY=10`
- C/v14：
  - `TIMEFRAMES=["1h","15m"]`
  - `MAX_TOTAL_POSITIONS=20`
  - `MAX_POSITIONS_PER_TF=2`
  - `SCORE_THRESHOLDS={"15m":55,"1h":35}`
  - `SHORT_ENTRY_PENALTY=15`
  - 修复开仓循环中 `side` 使用顺序，避免持仓判断引用旧变量

### P2（风控强化）
- v11/v14/v16 均新增盈利回撤保护：
  - 浮盈超过 `50 USDT` 后启用
  - 从最高浮盈回撤 `35%` 触发保护平仓
- v11/v14/v16 新增单方向持仓上限：
  - `MAX_POS_PER_SIDE=12`
- 止损/硬底后同币种更长冷却：
  - v11/v14：`SYMBOL_SL_BAN_HOURS=72`
  - v16：`SYMBOL_SL_BAN_CYCLES=72`（约8小时，按7分钟扫描周期）
- v14同赛道持仓上限：
  - `MAX_POS_PER_SECTOR=2`
- v11/v14 对止损币种做当日计数，达到上限后跳过新开。

### P3（工程修复）
- v16 补齐复盘日志：
  - `scanner_data_v16/events.jsonl`
  - `logs_v16/signals.jsonl`
  - `logs_v16/system.jsonl`
- v16 现在记录：
  - `OPEN`
  - `OPEN_FAILED`
  - `OPEN_SKIPPED`
  - `CLOSE`
  - 信号日志
  - 系统心跳
- Dashboard B 修复：
  - `F:\AutoTrading\Dashboard\dashboard_api_b.py`
  - 服务器路径：`/opt/crypto-auto-trader/web/dashboard_api_b.py`
  - 已改为优先读取 `scanner_data_v16` / `logs_v16`
  - 兼容回退到 v13/v12
  - `crypto-dashboard-b.service` 已 enable + restart，端口8082健康检查正常

## 部署验证
- 本地 `py_compile` 通过：
  - `scanner.py`
  - `scanner_v14.py`
  - `scanner_v16.py`
  - `dashboard_api_b.py`
- 服务器 `py_compile` 通过：
  - `/opt/crypto-auto-trader/scanner.py`
  - `/opt/crypto-auto-trader/scanner_v14.py`
  - `/opt/crypto-auto-trader/scanner_v16.py`
  - `/opt/crypto-auto-trader/web/dashboard_api_b.py`
- 服务状态：
  - `crypto-scanner` active
  - `crypto-scanner-v14` active
  - `crypto-scanner-v16` active
  - `crypto-dashboard-b` active
- Dashboard B 健康检查：
  - `http://127.0.0.1:8082/api/health` 返回 `{"status":"ok","account":"B"}`
  - `/api/stats` 可读取 v16 历史交易统计

## 今日实时快照
- 2026-05-17 10:28:41：
  - 总余额：8545.83 USDT
  - 总可用：1201.70 USDT
  - 保证金占用：7343.75 USDT
  - 总持仓：112
  - 多单/空单：24/88
  - 总浮盈亏：+1261.10 USDT
- 分账号：
  - A/v11：余额1968.74，可用0.00，持仓58，浮盈+735.93
  - B/v16：余额3004.81，可用300.48，持仓21，浮亏-322.76
  - C/v14：余额3572.27，可用901.22，持仓33，浮盈+847.93

## 注意事项
- 今日所有优化都没有新增黑名单。
- 现有仓位未被强制平仓；新逻辑主要限制后续新开仓，并保护浮盈。
- A账号仍是主要风险点：可用余额为0、空头数量很高。
- v16 新 events/signals/system 日志要等下一轮扫描或交易事件发生后持续增量落盘。
# 2026-05-17 18:20 v13下线清理 + v16策略优化

- 用户要求：删除并停掉 v13 相关策略和文件，并执行 v16 全部优化建议。
- 已删除/停用：
  - 本地删除 `F:\AutoTrading\策略文件\scanner_v13.py`。
  - 服务器停止/禁用并移除 `crypto-scanner-v13.service` systemd unit。
  - 服务器删除 `/opt/crypto-auto-trader/scanner_v13.py`、`run_scanner_v13.sh`、`logs_v13/`、`scanner_data_v13/`、`backtest_v13*.py`、`backtest_results/v13v2_*`、`__pycache__/scanner_v13*.pyc`。
  - 远端残留扫描：`find /opt/crypto-auto-trader -maxdepth 3 (*v13* / logs_v13 / scanner_data_v13)` 无结果。
- v16 优化已部署：
  - 修复硬底止损 bug：`MAX_LOSS_PCT` 触发后返回 `hit=True`，会真实平仓。
  - `analyze_symbol(symbol, bar)` 按真实周期取 K 线，不再把 1h 信号写死为 15m 数据。
  - 15m 不再独立开仓：`ENTRY_TIMEFRAMES=["1h"]`，15m 仅作为 1h 同向确认。
  - 固定止损尾部控制：`SL_MULT=2.0`，`MAX_LOSS_PCT=10.0`，`TRAIL_ACTIVATE=1.0`。
  - 盈利保护提前：`PROFIT_PROTECT_MIN_USDT=30.0`，`PROFIT_PROTECT_RETRACE=0.25`。
  - 分数重校准：OFI/RSI/EMA 权重下调，减少高分虚胖。
  - 不新增黑名单；历史差币只进观察降权 `WATCHLIST_SYMBOLS`，扣 `10` 分。
- Dashboard 更新：
  - `F:\AutoTrading\Dashboard\dashboard_api_b.py` 只读取 v16，不再回退 v13/v12。
  - B Dashboard 和三账号 Dashboard 的 B 标签改为 v16。
- 验证：
  - 本地 `python -m py_compile scanner_v16.py dashboard_api_b.py` 通过。
  - 服务器 `/root/miniconda3/bin/python3 -m py_compile scanner_v16.py web/dashboard_api_b.py` 通过。
  - 服务器 `crypto-scanner-v16`、`crypto-dashboard-b`、`crypto-dashboard-dual` active。
  - `http://127.0.0.1:8082/api/health` 返回 `{"status":"ok","account":"B"}`。
# 2026-05-17 23:20 v14 15m 与硬顶尾部优化

- 用户要求：优化 v14 的 15m 和硬顶尾部。
- 已修改并部署 `F:\AutoTrading\策略文件\scanner_v14.py` 到服务器 `/opt/crypto-auto-trader/scanner_v14.py`。
- 15m 优化：
  - `ENTRY_TIMEFRAMES=["1h"]`，15m 不再独立开仓。
  - 新增 `CONFIRM_TIMEFRAME="15m"`、`CONFIRM_MIN_SCORE=25`。
  - 1h 开仓前必须通过 15m 同向确认。
  - `SCORE_THRESHOLDS={"15m":65,"1h":40}`，提高信号质量要求。
- 硬顶尾部优化：
  - `MAX_LOSS_PCT=12.0`。
  - 新增 `_enforce_hard_stop_on_exchange()`，每轮直接扫交易所持仓，防止本地内存未恢复满仓导致硬顶漏管。
  - 阶梯止损提前：`1.8ATR` 到成本，`3.0ATR` 到 `1.5ATR`，`5.0ATR` 到 `2.2ATR`。
  - 浮动保护提前：`TRAILING_ACTIVATE={"15m":1.0,"1h":1.2}`，`TRAILING_PULLBACK={"15m":0.8,"1h":1.0}`。
  - 盈利回撤保护提前：`PROFIT_PROTECT_MIN_USDT=30`，`PROFIT_PROTECT_RETRACE=0.25`。
- 验证：
  - 本地 `py_compile` 通过。
  - 服务器 `py_compile` 通过。
  - `crypto-scanner-v14.service` restart 后 active。
  - C/v14 复查：持仓 28，多/空 9/19，浮盈约 `+1043.83 USDT`，超过新硬顶 12% 的亏损仓位为 0。
